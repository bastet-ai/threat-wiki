---
title: threat.wiki
---

# threat.wiki

Threat intelligence notes, group profiles, named-person records, and defensive guidance.

## Recent entries
- [**Post-npm-12 execution migration is no longer hypothetical: Checkmarx's `indexed-btree` teardown — a malicious npm package with NO install hooks at all, the loader hidden inside `BTree.prototype.set()` firing at application runtime, beacons to a hard-coded Slack channel + Telegram bot, next-stage blobs pulled via EtherHiding from a Sepolia contract, then the malware DELETES its own artifacts and STRIPS ITS OWN TRIGGER from the package code — and the operation's stated realized profit: €230,933.57 / 109 ETH, the first npm supply-chain operation on-wiki with a dollar figure**](ops/equation-of-compromise-npm-mathjs-clone-campaign-sepolia-contracts-slack-c2-github-actions-download-farm-jfrog-september-2026.md#september-22-checkmarx-runtime-loader-overlap) — Checkmarx via The Hacker News Sep 22, full text captured by this wiki. **This wiki's registry forensics join the stories:** all eleven names in Checkmarx's list are `0.0.1-security` holders batch-created in a ~40-second window on **Sep 3** (10:12:31–10:13:17 UTC) — the neutralization happened nineteen days BEFORE the blog cycle; and three names (`indexed-btree`, `btree-core`, `mutex-forge`) sit on BOTH Checkmarx's runtime-loader list and JFrog's Equation-of-Compromise download-farm target history (38.0M/37.7M fake downloads) + IoC table, matching version ranges — same package inventory, inflated then weaponized (overlap recorded, single-operator identity NOT asserted); npm user `charlessadler25` joins the disposable-persona pile. Durable: install-time scanning and `allowScripts` approval are structurally blind to a loader in a normal-looking class method — runtime/behavioral detection is the countermeasure this wiki's npm explicit-trust pattern page predicted; self-erasing triggers make the registry `time` block the only timeline — the exact page predicted — Checkmarx via The Hacker News + npm registry forensics (this wiki), Sep 22, 2026
- [**SharePoint CVE-2026-65660: Microsoft's own advisory says SPOOFING (CVSS 6.5, no integrity impact), Microsoft's own CVE record says REMOTE CODE EXECUTION, NVD says 8.8 — same flaw, same CWE-94, two triages; and the full exploit markup is now public (SafeControls-escape via unescaped quotes in ToolPane `Register` reconstruction → arbitrary .NET class after the type check → `XamlServices.Parse` deserialization, working in-memory webshell published)**](ops/microsoft-sharepoint-cve-2026-55040-cve-2026-63520-rce-chain-vulncheck.md#september-22-sibling-cve-65660) — Viettel Cyber Security researcher Dinh Ho Anh Khoa (the ToolShell Pwn2Own demonstrator who also disclosed CVE-2026-55040 on this page), write-up Sep 22 via The Hacker News, full text captured. Patched since Aug 11 CU (which also disables the vulnerable function by default); pre-auth path only on servers with anonymous page access that ALSO missed the June 9 auth-bypass fix; no ITW, not KEV at this wiki's Sep 22 check. Durable: **never triage from one Microsoft artifact — advisory vs CVE record vs NVD disagree by 2.3 CVSS points and one severity class; treat "spoofing" bands on web-app CVEs as untrusted until the writeup lands.** Same week as WordPress Comment2Shell = second sanitize-then-reformat / representation-vs-rendering divergence — hunt ToolPane web-part markup with quote injection; unexpected `XamlServices.Parse` children — The Hacker News / Viettel Cyber Security, Sep 22, 2026
- [**SideCopy (Pakistan-linked, Transparent Tribe / APT36 umbrella, aka TAG-140) expands from Indian government/defense to INDIAN ACADEMIC INSTITUTIONS — Trellix: spear-phished ZIP → PDF-icon LNK (`commskll.docx.lnk`) → HTA fetched from `docsportal[.]in` via `mshta.exe` (self-deleting after staging) → dropper DLL → XAML-in-memory/.NET-deserialization second stage → the group's in-house **ReverseRAT** (used since early 2021), traffic encrypted under hard-coded key `NMXIKS09?:709,!~lnsYUS`, exfil over **TCP/5863** to `dns.educationportals[.]biz` → `45.61.157[.]22`**](actors/sidecopy.md#sep22-trellix-academia) — Trellix report via The Hacker News Sep 22, full text captured by this wiki (Trellix canonical URL pending backfill). This wiki's liveness check: **both domains already fail to resolve, 45.61.157.22:5863 times out — infrastructure burned by disclosure time; the durable IoCs are the key, port, filenames and chain, not the domains.** Durable read: same LNK→mshta→HTA→deserialization chain Seqrite logged for XENOFISCAL in June — target-set expansion is the signal, the kit is the same; hunt mshta to `.in`/`.biz` domains, self-deleting HTA writes, Run keys launching `startT.hta`-class files, egress to TCP/5863 — Trellix via The Hacker News, Sep 22, 2026
- [**Second backup-agent local privilege escalation in one week, this one under ACTIVE exploitation: Veeam Agent for Microsoft Windows CVE-2026-32996 (Arctic Wolf bulletin Sep 16, active-exploitation warning Sep 22) — the backup service caches an elevated SYSTEM-level principal against a CLIENT-CONTROLLED session UID not bound to the requesting user or connection, and writes the valid elevated UIDs in the clear to `C:\ProgramData\Veeam\Endpoint\Svc.VeeamEndpointBackup.log`, which every standard user can read: read the log → replay the UID over `\\.\pipe\Veeam\VAW\ServiceConnectionPipe` → commands run as SYSTEM (public GitHub PoC Sep 14, no reliable workaround; fix = VBR 13.0.2.29+ → agent 13.0.3.1220)**](tools/veeam-agent-windows-cve-2026-32996-session-uid-log-leak-system-lpe-active-exploitation-arcticwolf-september-2026.md) — Arctic Wolf canonical bulletin + The Hacker News full text both fetched by this wiki, Sep 22. Durable reads: (1) a world-readable log line is a CREDENTIAL if a service accepts it as one — no memory corruption, no token theft, the secret is simply logged; (2) an elevated session cache keyed on caller-supplied identity is a confused deputy by construction; (3) backup agents are a standing LPE surface — SYSTEM-resident, local-IPC-reachable, deployed everywhere, hardened like a utility and attacked like a domain admin (Acronis CVE-2026-87886 KEV'd Sep 16 with vendor-confirmed targeted exploitation; Veeam now actively exploited the same week). Hunt: **standard-user READS of that exact log path are the pre-exploit signal** — file-access auditing on `Svc.VeeamEndpointBackup.log`; pipe connections from non-Veeam binaries; SYSTEM children parented by the backup service with non-elevated lineage. No KEV row yet (this wiki KEV check 2026.09.21 / 1,717), no actor, no victim count — treat "active exploitation" as the IR vendor's reported observation until IOC/attributed detail lands — Arctic Wolf via The Hacker News, Sep 22, 2026
- [**WordPress core shipped TWO admin-session-reuse RCE chains in the same 7.1.1 release: this wiki's Click2Shell page gets its sibling — Patchstack's CVE-2026-93485 "Comment2Shell" (CVSS 7.1, affected 4.7→7.1) — an anonymous comment survives save-time sanitization via a line break inside an allowed tag's attribute, the RENDER-TIME reformat splits the tag into a live event handler that fires on page load, and when a logged-in admin merely VIEWS the page the script uses the admin's own session to upload a plugin web shell — comment moderation is off by default and bypassable, so Patchstack's formulation stands: "moderation isn't a security control"**](ops/wordpress-click2shell-forced-theme-install-dom-selection-csrf-chainable-rce-pwnai-september-2026.md#september-22-sibling-flaw-comment2shell) — The Hacker News Sep 22 full text captured by this wiki (researcher write-up Rafie Muhammad, Sep 21). The durable class: *sanitize-then-reformat* — a security check that inspects one representation of untrusted data while the renderer emits another reopens the exact hole; same "two components read the same value differently" family as Click2Shell's DOM-selection bug and the OX Security four-CVE page. Fixed branches 7.1.1 / 7.0.5 / 6.9.8 / back to 4.7.36; no ITW, not KEV at this wiki's Sep 22 check; interim control = close comments; hunt = unexpected plugin uploads correlated with admin views of comment-bearing posts — The Hacker News / Patchstack, Sep 22, 2026
- [**The npm malicious-package pipeline, stopwatched end to end: holder creation → GHSA +37 s → OSV import +15 min → ossf/malicious-packages ingest commit +15.5 min → MAL-ID assignment +17 min — fully automated delete→advise→mirror→assign with no human step — captured via `test-react-app-in/out/way` (`MAL-2026-16374/16375/16376`), three names DELETED MID-DETECTION that no public report ever saw (zero surviving versions, sha256 tarball origins in their GHSAs the only artifact evidence, downloads 0/0/1 = dead on arrival); and the baseline correction underneath it: the same 03:15–03:17 UTC batch-mirrored three August-era Amazon-Inspector names that had sat GHSA-less 34–45 days — `de-morgan` (45 downloads/week) and `emoji-prints-fancy` (48/week) among them — so the real OSV→GHSA latency is 3–4 h for LIVE names but ~1 month+ for anything arriving via other finders: GHSA-only consumers (Dependabot, most scanners) were blind to actively-downloading packages for five to six weeks while OSV consumers were warned**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-22-twelfth-sweep) (OSV direct-ID sweep from 16374 + GitHub Advisory API + ossf/malicious-packages commit history + npm registry time blocks — this wiki, ~05:15–05:45 UTC Sep 22): durable reads — watch BOTH feeds not just GitHub's; `test-*`-shaped holder creations are the next tell to watch for (pipeline test traffic vs attacker staging is indistinguishable from public data); Equation-of-Compromise inventory correction lands on the trio page (`modern-events` was an npm holder since April, NOT campaign-survivor — while `quick-events`, `matrixflow-js`, `events-sync` and the inflated PuP carriers are ALL still live: removal ≠ remediation, ninth confirmation); survivors byte-identical into day nine (`googleadmanager` ~204 h, `module-sso-integration` ~138 h, Telegram fleet day eight, Burp collector #3 still answering 200); OSV high-water **16376** (resume 16377, 16377–16395 empty); KEV unchanged 2026.09.21/1,717 — this wiki, Sep 22, 2026
- [**Registry hygiene arrives in batch wavefronts, minutes wide and MONTHS late: in one five-minute window (23:12–23:17 UTC) npm security-holder-ed FIVE of the seven ViteVenom/ChainVeil names whose advisories had been sitting since JULY 14 — and the same window holder-created `@vite-tab/tabui`, an EIGHTH Vite-shaped name that appears in no July report and has no surviving versions: its exposure record was deleted with it — while the `jpmaia@uolinc.com` publisher email this wiki pulled from the registry JOINS the `@uol-afiliados` beacon fleet and the `@asdfaskdjfksadhfkasf/nadaver2` random-scope twin to the `@uh-platform` neighbor fleet = one account behind three “separate” fleets; `@baanx/solana-lib` finally has its GHSA and the mathmain trio got mirrors within 40 s–30 min of their holder creations**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-21-tenth-sweep) (GitHub Advisory API + npm registry time blocks + OSV, this wiki ~01:15–01:45 UTC Sep 22): durable reads — the `time` document is the audit trail of when a name stopped being resquattable (69-day advise→neutralize gap on `@vite-mcp/vite-type` while Checkmarx’s July “removed” claim left the name squat-armed); OSV→GHSA mirror latency is now measurably 3–4 h = when downstream scanners learn a name; Telegram fleet byte-identical day seven, `googleadmanager` ~196 h, all survivors unchanged; OSV high-water `MAL-2026-16373` (resume 16374); KEV unchanged 2026.09.21/1,717 (npm registry + GitHub Advisory API + OSV — this wiki, Sep 22, 2026)
- [**A Microsoft-ATTESTED driver whose only executed job is killing 145 named security processes: the fake LastPass Authenticator campaign (LastPass + Delphos Labs, Sep 17, relayed Sep 21) delivers a 148 MB junk-padded ZIP with a genuine `vsdbg.exe` beside an attacker `vsdbg.dll` (side-load), escalates to SYSTEM, and installs `Alinubx.sys` — a RENAME of `CcProtect.sys` from the CnCrypt disk encryptor, already on LOLDrivers as a process killer with public PoC — and the rename alone dropped VirusTotal from ~7/70 engines to ZERO while Microsoft’s WHCP attestation (signed Mar 2023) still passed and the vulnerable-driver blocklist carried NEITHER file**](tools/alinubx-sys-rapuncel-fake-lastpass-byovd-stealer-lastpass-delphos-september-2026.md) (The Hacker News full-text captured by this wiki, Sep 22): durable reads — attestation pipelines and blocklist pipelines are separate systems with different harm definitions, and BYOVD falls between them (Microsoft: abusable-driver behavior “does not meet its definition of a security vulnerability” because the driver isn’t a Microsoft component); Chrome/Edge app-bound encryption defeated by INJECTING THE BROWSER and asking its own service to decrypt — the trust boundary is walked through, not broken; the driver re-kills protections and re-runs the stealer every reboot = kernel-compromised, rebuild don’t clean; hunt `NvFsFilter` service / `nvfsflt64.sys` / signer metadata `Henan Dafeng Software`/`CnCrypt` / driver-load-then-security-process-death, and note the 40-brand impersonation server + ~300 fake GitHub repos (Arctic Wolf) make search-top-result “product downloads” hosted on GitHub hostile-by-default — LastPass + Delphos Labs via The Hacker News, Sep 21–22, 2026
- [**The advisory feed is now measurably a BUILD TRIGGER for this actor class, on both registries in one evening: `@uol-afiliados/affiliated-config-lib` (OSV `MAL-2026-16370`) joins the beacon-fleet template as member ten — an 8-line `index.js` preinstall curling `$(hostname).$(whoami).afiliados.hqbv58hgt1sjk7vf7ru4o0mwzn5etahz.oastify.com`, the class's THIRD distinct live Burp Collaborator collector in four days, aimed at a Brazilian-corporate internal scope — while on PyPI `starlette-healthchecks` uploaded version 1.3.2 THREE HOURS FOURTEEN MINUTES AFTER its own advisory landed, the same republish-after-naming loop this wiki caught three-for-three in `@baanx` on npm; plus PyPI `pullgetsage` zips your Telegram Desktop `tdata` (session keys = account takeover) to a Cloudflare Worker this wiki verified answering 405, re-armed with a 2-hour version cadence while advised**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-21-ninth-sweep) (OSV `MAL-2026-16370` 21:31 UTC + this wiki's direct tarball analysis, registry/PyPI/endpoint checks ~23:00–23:35 UTC, Sep 21): durable reads — **the Collaborator subdomain string is the durable cluster key** (survives renaming/version bumps, wildcard-resolves so resolver-log hunts need no label grammar — three live collectors now: `ii473egh…`, `pa33pg1o…`, `hqbv58hg…`); scope name = targeting statement (`@uol-afiliados` ≈ UOL Brazil, next to Thai-fintech `@baanx` and T-Bank-shaped `@pwaplatform`); `bytepack-probe-a7x3` is the time-block method in miniature — a **9-minute lifetime** Sep 7 with an HTTPS-tarball dependency pointing at REAL resolving ByteDance-shaped infra (`src-ssrf.bytedance.net`, CNAME `…toutiao-internal-all3…`), advised 14 days later: `dependencies:` entries whose spec is a URL not a semver range = free static hunt. Re-checks: `@uh-platform` five + `gemini-computer-use` (its `smart-server.online/install.sh` answers 200 NOW) all still live; Telegram fleet day six byte-identical; KEV unchanged 2026.09.21/1,717; OSV high-water **16370** (resume at 16371); no GHSA mirrors for any of the evening's names (OSV + npm/PyPI registries + live endpoint probes — this wiki, Sep 21, 2026)
- [**The npm takedown race, resolved in one evening in both directions: npm DELETED the mathmain/mathsbase/math-universe trio ~2.5 h after JFrog's "Equation of Compromise" went live (all three names now `0.0.1-security` holders, `repository: npm/security-holder`, OSV mirrors landing within minutes = coordinated registry action, not operator self-unpublish) — while the `algamil7x` cluster operator did the OPPOSITE, publishing INTO the advisory feed: `@baanx` re-armed all three names to 9.9.11 two hours before their advisories, `@insiderintelligence/componentlibrary` republished the same day it was named, and a NEW five-package `@uh-platform/*` neighbor fleet shares ONE hardcoded Burp Collaborator subdomain across dependency-confusion-shaped beacons — all still live at check; plus `gemini-computer-use` opens a fresh delivery class: no install hook at all, the `npx` entrypoint itself curls `smart-server.online/install.sh | bash` — install-time scanning catches nothing when nothing runs at install**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-21-eighth-sweep) (OSV/Amazon Inspector batch 17:14–17:21 UTC + this wiki's registry liveness checks ~20:20 UTC, Sep 21): durable reads — **an npm takedown's metadata signature** (fresh `created`, single `0.0.1-security`, empty maintainers, `npm/security-holder` repo) is distinguishable from the operator's own delete-and-recreate only by publisher + content — the SAME shape means neutralize or re-arm depending on who did it; **advisories as version-bump prompt** (`@baanx` 3-for-3 republish-after-naming); **AI-tool name-squatting as npx-execution bait** = third AI-adjacent delivery class this month; the trio's removal is NOT an all-clear — the farm's `events-sync` (40.7 M fake downloads, zero GHSA), the farm accounts, `modern-events`/`quick-events`/`matrixflow-js` and every inflated PuP were untouched, and the Sepolia contracts + Slack agent still task any infected host (LICENSE-marker grep is now the primary hunt). Same sweep (eighth, ~20:15–21:15 UTC): KEV FINALLY MOVED — **2026.09.21 / 1,717: CVE-2026-7273 Zyxel GS1900 stack-overflow → OS command exec (LAN-unauth, due Sep 24)**, four months after Zyxel's own advisory; the three kernel rows still sit without attribution and the public-exploit quartet remains unlisted (CISA kept cataloging past the BOD — the kernel-trio silence is specific, not institutional); survivors: `googleadmanager` ~203 h, `module-sso-integration` ~81 h, Telegram fleet day five→six, `@baanx/solana-lib` still GHSA-less; OSV high-water **16369** (resume at 16370) (OSV + npm registry + GitHub Advisory API + CISA JSON — this wiki, Sep 21, 2026)

## Sections
- **Ops** — campaign timelines, compromise chains, and sequencing
- **Tools** — malware, payloads, implants, and attacker infrastructure
- **Groups** — crews, cluster names, and shared operational personas
- **People** — publicly identified individuals or project personas when public sourcing supports it
- **Patterns** — reusable defender heuristics
- **Notes** — taxonomy, usage, and editorial guidance
