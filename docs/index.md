---
title: threat.wiki
---

# threat.wiki

Threat intelligence notes, group profiles, named-person records, and defensive guidance.

## Recent entries
- [**OSV moves an EIGHTH time in ~48 hours — 27 records (`MAL-2026-16446`–`16472`) in two structurally different waves — wave 1 is 15 ghsa-origin records for already-holder-created names whose surviving `time` blocks carry the forensic tell: five utility squats (`better-md`, `debounce-throttle-base`, `iso-datetime-core`, `semver-bump-io`, `solo-async-pipe`) registered their 1.0.0s inside NINE SECONDS (2026-05-14 15:02:10–15:02:30 UTC) = one scripted batch six weeks before the advisory; wave 2 is the live-artifact wave, headlined by the stream's FIRST Chrome DevTools Protocol hijacker family (`godxxx`/`godzz`/`godzzz`: require-time `cdp_inject.js` attaches to `127.0.0.1:9222`, injects into every open Chrome page, scrapes page + editor contents to `ai-script.test0ing7.workers.dev` — WHICH THIS WIKI PROBES STILL LIVE GET 200/POST 500 — while disabling TLS verification process-wide and shipping a live Groq key in cleartext)**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-23-twenty-fourth-sweep) + [twenty-fourth-sweep detail](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-23-twenty-fourth-sweep) (OSV direct-ID + npm/RubyGems registry forensics + live C2 probes, this wiki ~05:15–06:00 UTC Sep 23). Same batch: **`hachutis` LIVE at 455 downloads/week** — three undeclared prebuilt executables incl. an 81 MB Bun-compiled binary embedding `already-query-bacteria-agreed.trycloudflare.com`, declared bin pointing at a file absent from the tarball, README telling installers to run the binary to "activate the package in our systems" = JS decoy around an opaque native payload, zero GHSA; **`helpersutils-dev-tools` LIVE at 193 dl/wk** require-time Image beacon to `5.189.173.113:8899` (**TCP-OPEN at check**), publisher `bugbountykiller@outlook.com`; `wurl_show_data` 3.1.42.99 RubyGems dependency-confusion probe with homepage `dependency-confusion-test.oob.izz.lol` (507 dl, summary "a POC:)"); **STATE CHANGE: the `turbo-ws` git-tarball rail is BROKEN — repo `trktgq0wbre1/node-net-pool` now 404** (package live but new installs fail resolution). Survivor pass: `@gsutevil/hta-ui` BB fraud UI LIVE day two; `npmjs.it.com` C2 now on HTTPS, still 400 `invalid agent_id` ~7.5 days; `googleadmanager` 404 third sweep no holder; `193.149.176.186:8888` TCP-OPEN third sweep. KEV unchanged 2026.09.22/1,721 (Sep 25 due dates <2 days out); high-water **MAL-2026-16472**, resume 16473 — OSV + npm/RubyGems registries + live C2 probes (this wiki), Sep 23, 2026
- [**OSV moves a SEVENTH time in ~48 hours — 13 records (`MAL-2026-16433`–`16445`), all Amazon-Inspector — and the headline is the THIRD artifact of the `npmjs.it.com` Gradle-masquerade RCE-agent family: `@vitemirrorte/element-plus-vite-cli` 2.9.1, same full remote-control agent (register/exec/download/upload/delete/move/ps/ls against the still-live `npmjs.it.com` endpoint — this wiki re-probes `POST /api/register` still answering 400 `invalid agent_id` seven days on), with two escalations: the activation gate now fingerprints a NAMED OPEN-SOURCE PROJECT FAMILY (`mall4cloud-react`, a widely-forked e-commerce suite, verified by file-content hashes) and the second stage's AES-256-GCM key is DERIVED FROM THE VICTIM'S OWN WORKSPACE FILES (`decode-pipeline.cjs`) = the payload is cryptographically undecryptable by any scanner that doesn't hold the victim's repo**](ops/npmjs-it-com-gradle-masquerade-full-rce-agent-pair-september-2026.md#september-23-third-family-member) — same 90-minute intake window carries four new shapes: **`n8n-nodes-healthmon` + `n8n-nodes-metricsagent` = a two-name backdoor pair in the n8n-community-node costume sharing one listener grammar** (postinstall starts `0.0.0.0:41111`, JSON field `c` → `child_process.exec`, no auth, re-installs on every n8n start, metricsagent's default 'Health Monitor' command is container-escape recon — both names died BEFORE advising, delete-only = re-registerable; with `data-transformer-utils` still LIVE, the n8n namespace is a confirmed costume class: three packages in ~24 h); **`my-company-device` = an `npx`-invoked backdoor that injects a hardcoded `sunyiting-macmini` ssh-ed25519 key into `~/.ssh/authorized_keys` over Tailscale and dispatches work through THE VICTIM'S OWN `codex`/`claude` CLI sessions** (caller-supplied argv/cwd/env, whitelist from the incoming request, survives uninstall — LIVE 26 days pre-advisory, zero GHSA: the operator doesn't ship a RAT, it rents your already-authenticated coding agents); **`moidevl` = an exam-cheating overlay that DPAPI-decrypts and steals OpenAI/ChatGPT cookies from Chrome/Edge/Brave, runs as a `WDA_EXCLUDEFROMCAPTURE`-hidden `SearchApp.exe`, and POSTs the exam screen to the non-registrable `ipc.shadxino.internal`** (LIVE since Sep 1); and **`turbo-ws` declares its only dependency as a live GitHub-branch tarball from an unrelated account while advertising "zero dependencies"** (repo verified 200 at check — whoever holds `trktgq0wbre1` owns every future installer). Plus a six-name `webhook.site`/`dnshook.site` dependency-confusion probe fleet (two `alrwsy1 <xxmsm75@gmail.com>` names sharing one webhook UUID, LIVE; `cat*` pair deleted one second apart) and `internallib_v497`/`_v550` bait whose curl-pipe payload URL is a PRIVATE RFC1918 address. Survivor pass: `googleadmanager` 404 a second sweep with NO holder (NOT takedown), `@gsutevil/hta-ui` Banco do Brasil fraud UI STILL LIVE day one, `193.149.176.186:8888` still TCP-OPEN; KEV unchanged 2026.09.22/1,721; high-water **MAL-2026-16445**, resume 16446 — OSV + npm registry + live C2 probes (this wiki), Sep 23, 2026
- [**The npm malware stream's biggest night ends with the weirdest artifact: OSV moves a SIXTH time in ~36 hours (`MAL-2026-16419`–`16432`) and record 16419 is `@gsutevil/hta-stage` — a Windows HTA/WSH installation stage that disables AMSI-for-WSH by registry (dual WScript+WMI write, split-array value name), kills prior agents by uninstalling the Windows Installer product "GSUT Guest", then silently `msiexec /i <base>/guest.msi?t=<token>` from a base URL the hosting webpage supplies at runtime, every sensitive string rebuilt from char-arrays at runtime — and its SCOPE-MATE, published 66 seconds earlier and STILL LIVE at this wiki's check, is `@gsutevil/hta-ui`: a pulled-and-read Banco do Brasil phishing landing kit — pt-BR "Seg.BB — Diagnóstico do Módulo de Segurança" page with real BB logo assets and the genuine SAC number 0800 729 0722, a consent funnel the code comments call "Funil GSUT", described in its own package.json as "GSUT HTA landing templates (static UI only — ADR 0084)"**](tools/gsut-hta-stage-loader-banco-do-brasil-landing-templates-npm-september-2026.md) — npm here is the operator's **builder infrastructure, not the victim path**: nobody `npm install`s a loader that ships as an `.hta` behind a fake bank page; Amazon Inspector's package scanner caught what no endpoint-only victim would report. Publisher email on the live UI package: `cryptodomespag <robertasilvan172@gmail.com>`. `gsut[.]com` now serves a **registrar parking lander while the loader's API paths still answer** (`/v1/hta/event` GET 200 lander-HTML, POST 405 — front door alive, origin gone; hunt the URI grammar, not the domain); **no `guest.msi` hash exists anywhere = the carried payload is unknowable at capture**. Asymmetric npm disposition worth watching: the LOADER was neutralized (maintainer → `npm-support`, tarball 404) but the FRAUD UI with Banco do Brasil trademarks is still downloadable. Same move window: `@wizloft/harness-*` — a thirteen-plus-name scope with EIGHT genuine alpha releases Aug 17–Sep 6 holder-erased inside five minutes (00:20:15–00:24:17 UTC; compromised legit project or dressed-up malicious dev — versions gone, unknowable), plus dual-registry `kerokwis` (RubyGems listed + PyPI 404 = second RubyGems-latency data point); **and `googleadmanager` 404s for the first time in ~231 h — recorded as no-longer-resolving, NOT takedown (bare-404 rule), while all five `@tvg-mar/*` names and the n8n post-exploitation package sit LIVE with `193.149.176.186:8888` still TCP-OPEN 6 h post-GHSA**. KEV unchanged 2026.09.22/1,721; high-water **MAL-2026-16432**, resume 16433 — OSV + npm/PyPI/RubyGems registries + live gsut.com probes (this wiki), Sep 23, 2026
- [**OSV moves a FIFTH time in one calendar day (9 records, `MAL-2026-16410`–`16418`) and the flagship is this cluster's biggest single batch ever: FIVE new `algamil7x` DNS-beacon members in one scope `@tvg-mar/` — `promos-context`/`promos-gtm`/`storyblok-bridge`/`tvg-promos-atomic-ui`/`utils` — ALL published under a FIFTH publisher email ON the attacker domain (`betfairpoc@algamil7x.xyz`, the `<brand>poc@` grammar now covering gambling too), scripted batch-create at 10:23:34–10:24:24 UTC then the malicious `9.9.10` REPUBLISHED individually across the next 8 hours (13:54/14:46/16:54/17:56/18:39) while the advisories all landed 21:50 UTC = `created` marks the squat, per-version timestamps mark the arming — all five LIVE, zero GHSA at check**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-22-twenty-first-sweep) — same batch's heaviest artifact, read in full from the live tarball by this wiki: **`n8n-nodes-data-transformer-utils` 1.0.0 is not another beacon — it is a rebuilt post-exploitation tool aimed at containerized n8n hosts**: plain-JS postinstall steals `env | sort` + SSH private keys + K8s service-account tokens + `/home/node/.n8n/` config to **`193.149.176.186:8888/lombard/n8n-rce` (C2 TCP-OPEN at this wiki's probe)** then ATTACKS THE VICTIM'S OWN NETWORK from the host — raw-socket Redis `INFO`/`KEYS *` + value dumps against hardcoded **Tailscale CGNAT address `100.72.158.36:6379`** and Docker-bridge `172.18.0.1:6379`, etcd `:2379` recursive key dump, fixed portscan of **Vault `:8200` / Consul `:8500` / kube-apiserver `:6443` / kubelet `:10250`** — the hardcoded private targets cannot be the attacker's = the operator's own lab addresses, an engagement tool republished as a package; plus `eslint-config-compact-utils` (deleted-while-advised, but its CI/repo-identity beacon endpoint `cbrsuo9293.execute-api.us-east-1.amazonaws.com/c` **answers HTTP 200 today** — AWS API Gateway routes outlive the package the way Vercel deployments do) and `auclean` (PyPI, kam193 campaign `2026-09-auclean`, whose own text names the target: a specific DACON AI-audio competition's infrastructure = kam193 campaign slugs are becoming a targeting-intelligence source). KEV unchanged 2026.09.22/1,721; high-water **MAL-2026-16418**, resume 16419; `ubiquiti-agents-link-mcp` `0.2.1` published 22:25 UTC added `cat .env` + `cat package.json` + `ls /` to its exfil while live — OSV + npm/PyPI/RubyGems registry + live C2 probes (this wiki), Sep 22, 2026
- [**CISA moved KEV today and the entire four-row batch is network security products under exploitation (catalog 2026.09.22, 1,721, all due 2026-09-25): F5 BIG-IP APM CVE-2026-94127 — unauth heap overflow in the TMM data plane when an access policy + OAuth profile sit on the same virtual server, CVSS 9.8, F5 confirms in-the-wild, ENG hotfixes + emergency iRule only, appliance mode exposed — and a Check Point PAIR in one advisory: CVE-2026-85102 Gateway/Spark VPN certificate-validation RCE patched Sep 9 with no exploitation known then exploited ANYWAY from Sep 12 (ONE-DAY wave against Spark customers from anonymization infra; observed attacker cert subjects `CN=vpn,OU=users,O=global`, `CN=vpn-user...`, `CN=vpnuser...`) + CVE-2026-93616 Security Management pre-auth path traversal → arbitrary script execution + arbitrary Java class load, a ZERO-DAY whose only observed attacks were a handful on Jul 23 and whose fix shipped only today — and Check Point explicitly warns **LivePatch Take 28/29 does NOT address it**](ops/cisa-kev-f5-bigip-apm-checkpoint-pair-arista-velocloud-september-22-2026.md) — KEV JSON pulled by this wiki ~20:41 UTC + Check Point advisory full text captured + F5 detail via SecurityOnline (my.f5.com SPA unretrievable, K000162605 backfill pending). Durable reads: one-day AND zero-day exploitation of security appliances in the same vendor's single advisory = both ends of the exploitation clock firing routinely; certificate-subject fields are cheap durable IoCs — index them from VPN logs now; the exposure unit is the CONFIG COMBINATION (APM+OAuth profile, cert-auth Edge mode, management-web reachability) not the version; and this is Check Point Security Management's second actively-exploited KEV-class flaw in eight days after CVE-2026-91843 — posture answer moves from patch-promptly to assume-targeted. The batch drains this wiki's KEV lag queue (VCO-93952 row landed): remaining = Veeam CVE-2026-32996 + the attribution-less Sep 18 kernel trio; NVD still Awaiting Analysis on all four rows — this wiki, Sep 22, 2026
- [**A product's own UI as the delivery rail: the npm name `ubiquiti-agents-link-mcp` was registered Sep 22 19:36 UTC on the claim that Ubiquiti's UniFi Site Manager UI and agent relays instruct operators to run `npx ubiquiti-agents-link-mcp@latest` while the name sat UNCLAIMED — whoever registers a name a product tells humans to execute gets code execution at operator privileges on every machine that obeys; the registrant self-labels a security research placeholder (runs `id`, POSTs username + hostname to a Burp Collaborator host; `0.2.0`, published 58 min after `0.0.1`, ADDED `ls -la` + cwd to the exfil), and OpenSSF Package Analysis flagged it malicious in FOURTEEN MINUTES**](patterns/product-instructed-npx-command-unclaimed-package-name-squat-september-2026.md) (`MAL-2026-16409`, published 19:50:54 UTC; zero GHSA at check). This wiki pulled and read all three tarballs directly: no install hooks — execution needs the operator to actually run the npx command, which is exactly the demonstrated attack; the `oastify[.]com` collector answers HTTP 200 at check = still POST-ready. The Ubiquiti UI claim is UNCORROBORATED by any public source at capture — treat as the publisher's assertion; the monitoring item is Ubiquiti's disposition (confirm + take the name back, or silence). Durable pattern independent of this case: audit any product instruction (`npx`/`pip install`/`go install` in docs, UI strings, error messages, support macros) for registry names the vendor does not OWN — an unclaimed name in a trusted instruction is a squatting primitive with the vendor's authority baked in, strictly stronger than LLM phantom squatting because no model has to hallucinate it; and self-declared benign labels remain untrusted metadata by this wiki's standing rule (fifth `*.oastify.com` sighting across four unrelated clusters this month) — this wiki registry + tarball forensics, Sep 22, 2026
- [**The Graphalgo npm malware family is now a Go/Terraform malware family: Aikido documents the FIRST systematic malware distribution through Terraform providers — `gocommunity-io/dockerd` + typosquat `kreuzwenker/docker` (one transposed letter off the 56 M-download `kreuzwerker/docker`) — whose entry points fire ONLY when the SHA256 of the concatenated `containerName`+`networkID` Terraform variables equals a hardcoded hash that doubles as the AES key, unzipping an `import-resource.sqlite3` (ZIP magic in a database costume) and executing it via a detached `go run .`; plus two Go Modules (`gocommunity.io/orderedbtree`, and `gogets.dev/btreex` with commits FORGED to November 2025 — the Go proxy treats git commit dates as authoritative, so the lie is what pkg.go.dev displays) and TWO fake vanity-domain fake Go ecosystems (`gogets[.]dev`, `gocommunity[.]io`, both LIVE at this wiki's check) that accept no external packages and exist only to make the module paths look legitimate**](ops/graphalgo-go-terraform-cross-ecosystem-spread-aikido-september-2026.md) — Aikido Sep 22, full text captured by this wiki. **The join to this wiki's own record: the ECDH public key `302a300506032b656e...526702` appears in npm Graphalgo payloads since April 2026, first in `modern-events` — a package sitting in our Equation-of-Compromise inventory (XRAY-968383)** — crypto identity crosses ecosystems, so family tracking pivots on keys/contracts/channels, not registries. Second stage: dual-channel C2 (Slack `frontend-devs`@portfolio-devs check-in → `qa-announcements`@portfolio-testers tasking + **Arbitrum Sepolia** contract `0xAD02b5cD...036C` with a `setCPubKey` method and a shared wallet WRITE-PERMISSIONED to one key-value store = disruption-resistant on-chain C2; 3-s chain / 10-s Slack polls; per-victim ECDH so infected clients cannot read each other's tasking; C2 headers select Go or JavaScript execution or self-delete). Census: 18 hostnames / 725 check-ins since Jul 16 (3 Win / 5 Linux / 10 macOS) = small and TARGETED; Terraform users chosen as the shortest path from workstation to production cloud credentials. This wiki verified: both providers still listed on registry.terraform.io and both fake ecosystems HTTP 200 at ~19:20 UTC. Hunt: `go run .` detached under terraform/node parents, JSON-RPC to Arbitrum Sepolia (new chain in the on-chain-C2 family beside ChainScript/GoCaracal/Aeternum), `api.slack.com` from CI, `.sqlite3` files starting `PK`, trigger constant `b9966e37...` in vendored providers; treat unknown vanity-domain Go module hosts that refuse external publishing as targeting artifacts — Aikido + this wiki's live checks, Sep 22, 2026
- [**OSV moves a THIRD time in one day — 21 records (`MAL-2026-16388`–`16408`) — headline is this wiki's own cluster-grammar call: `oracle-redis` 5.11.3 is the `ulid-xyz` transitive-arming design reborn item-for-item (verbatim-copy `ioredis` entry package + exactly TWO unused caret dependencies, `redis-type-intel ^1.10.5` + `pino-ulid ^2.12.2`, existing only to be installed) under the SAME name-fragment grammar — and it is LIVE at 65 downloads/week; the chain is disarmed only because npm holdered `redis-type-intel` (no version satisfies the range = resolution failure), while `pino-ulid` sits delete-only with an `unpublished` time block = a LIVE FUSE: re-register 2.12.x and the live entry package re-arms without a single change to itself**](ops/ulid-xyz-transitive-delivery-chain-microsoftsystem64-dprk-september-2026.md#september-22-oracle-redis-rebuild) + [nineteenth-sweep detail](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-22-nineteenth-sweep) (Amazon Inspector via OSV + npm registry time-block forensics, this wiki ~19:00–19:35 UTC Sep 22). Same batch: **`noverojava` becomes the THIRD live publisher declaring the same unpinned `github:tenka-san/libsignal-node`** (after `@mr-supun-fernando/supunmd-bail` + `@mikudeveloper/baileys`) — one GitHub account flip now arms installers of three separately-advertised packages; fork re-verified benign at check (parent `canove/libsignal-node`, one added commit `chore: suppress logs`) = standing dead-switch, three-name count is a floor; and **`nodetokyo` = a require-time payload that silently installs Python 3.12, hooks the keyboard system-wide, POSTs the clipboard every 300 ms + full-screen screenshots every 30 s to `nodetokyo.vercel.app/api` — the package lived 12 hours on Aug 28-29 and the advisory landed a month later, but THIS WIKI PROBES THE C2 STILL LIVE (HTTP 405, POST-ready)**: free-tier serverless C2 outlives the package and any past installer is still streaming. GHSA-bot batch 16391–16405 holdered 15 names in one 18:11–18:23 UTC window (four `ndmc`-themed + four keyboard-walk gibberish names = disposable-name generator tells, watch the grammar); kam193 PyPI `2026-09-snap-queue` trio all 404 = deleted pre-advisory (bare-404 rule applies, no takedown claim); correction: `chai-logger` unpublished 17:47 UTC while advised. KEV unchanged 2026.09.21/1,717 fifth check; `googleadmanager` ~226 h byte-identical; OSV high-water **MAL-2026-16408**, resume 16409 — OSV + npm registry + live C2 probes (this wiki), Sep 22, 2026
- [**OSV moves twice in one afternoon: five more records (`MAL-2026-16383`–`16387`) carry two clusters — a three-name `gldriver` image-host dropper family (`mxdriver`, `tldriver`, `tlxbnhd`) published in an 11-minute window and synchronized-self-deleted FOUR SECONDS apart (created 03:39:57/03:46:34/03:50:46, ALL unpublished 07:37:56/57/58 UTC per this wiki's time-block pulls — an operator-side orchestrator signature, not registry action), whose payload host `api.imghippo.com/files/hOG8244hc.png` (.png disguise → `gldriver_pre_*.exe` writes opened via `mspaint`/`open`/`xdg-open`) now returns **403 — no hash survives anywhere, so what the dropper carried is permanently unknowable** — and two Baileys-sphere names introducing a NEW delivery rail: `@mr-supun-fernando/supunmd-bail` declares `"libsignal": "github:tenka-san/libsignal-node"` with NO SHA/tag/integrity, so `npm install` clones whatever HEAD says and runs its lifecycle scripts: **zero malicious code in the npm tarball = invisible to tarball scanning and advisory-keyed consumers** (repo verified a benign fork at check — mutability is the vulnerability)**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-22-eighteenth-sweep) + [Baileys page Sep 22 section](ops/baileys-libsignal-node-npm-whatsapp-channel-follow-campaign.md#september-22-osv-follow-up) (OSV direct-ID + npm registry + GitHub verification, this wiki ~17:35–18:05 UTC Sep 22). Companion name `@mikudeveloper/baileys` 1.0.0 = the August forced-follow campaign rebuilt (120-s post-connect `pepek.json` fetch → `QueryIds.FOLLOW` on the installer's own WhatsApp session) — **this wiki verified `pepek.json` LIVE at check (channels "Anomali M" + "mikustore.id"), the package at 113 downloads/week, LIVE, zero GHSA**; both Baileys names live + zero GHSA = GHSA-keyed consumers blind while installs happen. Durable hunt: **any `dependencies` value matching `github:user/repo` without a commit SHA**. Dropper trio downloads 0/0/0 = dead on arrival, names delete-only = re-registerable. KEV unchanged 2026.09.21/1,717 fourth check today; survivors live (`googleadmanager` ~217 h, `chai-logger` installable); OSV high-water **MAL-2026-16387**, resume 16388 — OSV + npm registry + GitHub API (this wiki), Sep 22, 2026
- [**The npm cluster whose C2 domain is also its email domain: OSV resumes after six empty sweeps with FIVE new records and two are new `algamil7x` DNS-beacon members whose registry publisher emails sit ON the attacker domain — `teslapoc@algamil7x.xyz` publishes `@tesla-insurance/vinless-quote` (65 downloads/week, still live) and `vrtpoc@algamil7x.xyz` publishes `@user-services/web-components` (still live) — completing the `<brand>poc@algamil7x.xyz` account pattern beside `emarketerpoc@` (`googleadmanager`, `componentlibrary`) and `metamaskpoc@` (`@baanx` trio): the attacker domain appears simultaneously as DNS C2, publisher email, and (via scope-squat grammar) the lure**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-22-seventeenth-sweep) (`MAL-2026-16378/16379`, Amazon Inspector, OSV published 13:57 UTC Sep 22; vinless-quote's `9.9.10` was republished ~12.5 h BEFORE its own advisory = the advisories-as-version-bump-prompt loop again; both names carry no GHSA at check = OSV-keyed consumers only). Same batch: `chai-logger` 3.0.2 (4 MB obfuscator.io blob at import, verbatim-pino filler, axios declared solely to arm the blob, still installable) and — the durable new class — **`cisco-github-simple` + `take-home-caller-id`: preinstall `/etc/passwd`+`/etc/hosts` recon POSTs to two FRESH Burp Collaborator collectors that the OPERATOR self-deleted four minutes after creation (registry time blocks: 11:06:33→11:10:33, 12:15:12→12:19:20 UTC, no security-holder, no GHSA — `affects=` queries empty): a publish-and-delete name never reaches any advisory-keyed consumer, so the OSV record's embedded analysis is the entire public artifact, and the names sit re-registerable** — this wiki, OSV + npm registry time-block forensics, Sep 22, 2026. Durable: **self-declared benign labels are a cluster tell, not exculpation** (`poc` accounts here, bug-bounty READMEs on `@pwaplatform`, self-labeled XSS "PoC" on `radio-player-theme`) — this actor class consistently dresses target qualification as authorized testing; hunt the maintainer-email domain and `<brand>poc@` grammar, plus resolver logs for `oob.algamil7x.xyz`. Also this sweep: correction on the mathmain trio page — its GHSA mirrors DID exist inside two minutes of each holder creation (the "Zero GHSA" line was a LIST-endpoint artifact); survivor pass twelve ALL live byte-identical (`googleadmanager` ~214 h); KEV unchanged 2026.09.21/1,717 at ~15:40 UTC — third check today, lag queue three deep; OSV high-water **MAL-2026-16382**, resume 16383

## Sections
- **Ops** — campaign timelines, compromise chains, and sequencing
- **Tools** — malware, payloads, implants, and attacker infrastructure
- **Groups** — crews, cluster names, and shared operational personas
- **People** — publicly identified individuals or project personas when public sourcing supports it
- **Patterns** — reusable defender heuristics
- **Notes** — taxonomy, usage, and editorial guidance
