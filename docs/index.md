---
title: threat.wiki
---

# threat.wiki

Threat intelligence notes, group profiles, named-person records, and defensive guidance.

## Recent entries
- [**Self-correction on last night's headline npm story: the real exposure window was 8.5 HOURS, not five days — npm's own registry metadata carries a machine-readable `unpublished` record (timestamp + exact version list) that survives removal, and `element-plus-vite-cli` shows both malicious versions died Sep 16 at 16:34 UTC, five days BEFORE the advisory landed: the five days is detection latency, not exposure**](ops/npmjs-it-com-gradle-masquerade-full-rce-agent-pair-september-2026.md#september-21-third-sweep-follow-up-the-exposure-window-was-85-hours-not-five-days-explicit-unpublish-record-plus-the-ghsa-pair-lands) (this wiki, Sep 21 third sweep): **method durable read — pull the package `time` block for any advised package BEFORE concluding an exposure window; the metadata is a forensic record even after removal** (`created`, per-version stamps, `modified`, `unpublished` all persist when `versions` is empty). Same-sweep wiring: the GitHub mirror batch of Amazon-Inspector npm advisories landed 06:30 UTC (GHSA IDs now citeable for all eight Telegram-fleet names + the Gradle-agent pair + `chai-as-viem`, all CVE-less, API-verified via direct-ID GETs — the `ghsa_id=` list filter is broken for these, use direct GETs); **all nine Telegram chat-1064260758 packages STILL live at a second same-day check with byte-identical timestamps**, plus survivors `googleadmanager` (~160 h), `module-sso-integration` (~46 h), `@baanx/solana-lib` (whose `modified` tick = the OPERATOR's own 9.9.10 republish the day after the first five's advisories, not npm action); `chai-as-viem` (GHSA-c67f-wr24-prw9, critical) **still installable** = third package this week on the `process.env`-POST → `new Function(require, response)` `vercel.app` construction while `chai-as-indexed` is now fully gone — pattern-promotion watch; **CISA KEV post-deadline null result: the three SSVC-"active" kernel rows' Sep 21 BOD passed with NO catalog bump, NO actor attribution, NO fourth row** — three "active" CVEs through a full 72-hour federal deadline with zero public corroboration; watch the Sep 22 batch for the public-exploit quartet (this wiki + npm registry + KEV JSON + GitHub Advisory API, Sep 21, 2026)
- [**The KelpDAO/LayerZero macOS backdoors resurface on a victim with no crypto ties: SentinelOne joins FLATROOF = our on-wiki macOS.Gaslight and documents ROOFDECK — a second-stage implant whose C2 URL is the `website` field of an attacker Nostr profile and whose every command is signature-verified — delivered by fake job-interview repos carrying a weaponized `.terraform.lock.hcl` where `terraform init` executes the attacker's custom provider; attribution now explicit: TraderTraitor (aka UNC4899 / PUKCHONG / Jade Sleet), the Lazarus subgroup behind the $292M KelpDAO theft**](ops/tradertraitor-jade-sleet-flatroof-roofdeck-macos-terraform-lockfile-lures-india-it-provider-sentinelone-september-2026.md) (SentinelOne Labs Sep 18, 2026, published with the authors' LABScon talk; THN Sep 21): the India IT-services victim's MacBook ran Terraform/Ansible against AWS/OVH/OpenStack — **the value of the target is whatever their laptop can reach**; implants sat **dormant 11 days, detonated by Cursor opening the workspace** (IDE-parented first execution is the signal EDRs dismiss as dev noise); **one day after LayerZero's public disclosure the attackers deployed a stripped rebuild that deleted the old binaries** — your IR report is attacker OPSEC input; then abandoned the victim as insufficient value (low-and-slow ≠ abandoned). Durable reads: **lockfiles are phishing attachments — gate `terraform init` to approved provider registries**; Nostr-relay C2 bootstrap + signed commands = takedown-resistant trust model, hunt by behavior (`api.nostr[.]watch`, `/app_version` pinned cert, quarantine-attribute removal, `~/Library/com.apple.iTunesCloud/SystemUpdate` / `com.apple.internal.ck/iSync`); **certificate-identity pivot: mkcert default metadata exposed username `ub` on a QEMU VM across multiple attacker domains** — SentinelOne Labs canonical post + THN, Sep 21, 2026
- [**Two Element Plus-themed npm packages ship a FULL remote-access agent dressed as Gradle instrumentation — ~90 base64 chunks → hidden detached node process → registration + task long-poll to `npmjs.it.com` (a npmjs.com lookalike, LIVE at this wiki's check) — and the import-time path is GATED on nine hardcoded fintech/trading repo sentinel files (`src/api/AITrading.js`, `src/pages/ETF-quant-trading/history.vue`, `orderCenter.vue`): a targeted implant using npm as its delivery rail**](ops/npmjs-it-com-gradle-masquerade-full-rce-agent-pair-september-2026.md) (Amazon Inspector via OSV `MAL-2026-16318`/`MAL-2026-16331`, batch Sep 21 ~04:00 UTC; `@asenfotech/unplugin-element-plus` + `element-plus-vite-cli` v2.9.3/2.9.5): agent commands = `exec`/`ls`/`download`/`upload`/`delete`/`ps`/`move`, persistent agent UUID at `~/.gradle-cache/.aid`, `C2_TLS_INSECURE` + `C2_SERVER_URL` operator knobs; this wiki verified the C2 answers (`POST /api/register` → 400 `invalid agent_id` behind Cloudflare) and both npm names' versions unpublished at check — **packages created Sep 16, advised Sep 21: the 5-day window is the exposure, and the still-live C2 means surviving hosts are still beaconable**. Durable: an npm implant writing to `~/.gradle-cache/tooling-api-runtime.mjs` will be triaged against the WRONG ecosystem — hunt path+node-parent, not name; sentinel-gating means **a quiet install is not a clean install** (fingerprinted repos only); alert on `npmjs.it.com` exactly + lookalike-suffix egress from build hosts. Zero public vendor coverage at capture — the Inspector text is the primary — OSV/Amazon Inspector + live C2/registry probes (this wiki), Sep 21, 2026
- [**One Telegram `chat_id 1064260758` stitches nine dependency-confusion-shaped npm packages into one operator: `siriusbeyond` + `@siriusbeyond/auth|ui|utils`, `commerce-materials`, `byted-commerce-materials`, `starbucks-sdk`, `@dbbhk/ui-components` — all preinstall/postinstall `callback.js 2>/dev/null || true` beaconing host identity + credential-shaped ENV-VAR NAMES (values untouched… except the HSBC-shaped `@dbbhk` variant, which reads IMDS + ECS task creds + `~/.aws/credentials` + the K8s service-account token = actual theft), several self-labeled "dependency confusion PoC / security research"**](ops/telegram-chatid-1064260758-dependency-confusion-recon-cluster-september-2026.md) (Amazon Inspector via OSV `MAL-2026-163xx`, batch Sep 21 ~04:00 UTC): `99.0.0` on plausible internal scopes = canonical confusion shape; DNS-tunnel fallback base64s host/user/CI into `<blob>.dc-callback.example.com` lookups (parent doesn't resolve — the QUERY is the durable indicator). **All nine still LIVE on npm at this wiki's Sep 21 check** (created Sep 20 00:29–09:27 UTC), third consecutive day of advised-and-serving alongside `googleadmanager` (156 h) + `module-sso-integration` + NEW algamil7x sixth member `@baanx/solana-lib` (also live, tarball-verified `bxsol.<user>.<host>.<cwd>` DNS beacon). Durable reads: **env-var NAME disclosure is a targeting map — treat the fleet as pre-stage**; pivot clusters on Telegram chat IDs like C2 IPs; **ten self-labeled "PoCs" all exfil to one private chat = the label devalues itself**; hunt `api.telegram.org/bot*` from build hosts (no legitimate npm dep needs it) — OSV/Amazon Inspector + live registry re-checks (this wiki), Sep 21, 2026
- [**Unbound's DNSSEC validator has a Critical (9.1 maintainer-scored) heap overflow reachable from ANY malicious zone — CVE-2026-81642: a DNSKEY whose owner name is a compression pointer into its own RDATA overflows the digest buffer while the validator does its job; every Unbound ≤ 1.26.0 affected, INCLUDING the July 1.25.2 and August 1.26.0 "security releases" — fixed 1.26.1**](tools/unbound-dnssec-dnskey-compression-pointer-heap-overflow-cve-2026-81642-nlnetlabs-september-2026.md) (NLnet Labs advisory Sep 16, 2026, captured this sweep): no on-path position, no client interaction, no victim trust required — **an attacker registers a zone and the vulnerable resolver fetches and digests the poisoned DNSKEY because validation is the point of running it**; DoS confirmed, **RCE possible through attacker-controlled data**, no configuration condition published (validation-off reachability not ruled out by NLnet). Same release fixes **8 more flaws including CVE-2026-82717**, a CNAME-synthesis heap corruption **reported by Ben Morris of Anthropic** (progressive corruption, RCE under certain systems/compilation options) — another data point on the frontier-AI-assisted memory-bug-discovery wave. No ITW, CISA SSVC "none," **no public PoC as of Sep 16** — but compression-pointer DNSKEYs are trivially fuzzable once disclosed, so the PoC→KEV clock is the thing to watch. Defender reads: **inventory `unbound -V` on every host that answers DNS** (recursors, appliances, mail gateways, container images); a rooted resolver RCE is a pre-auth foothold where exfil and C2 ride your own resolver, invisible to per-host monitoring; **being "recently security-patched" ≠ current** (1.25.2 and 1.26.0 were security releases for OTHER bugs); patch trigger is **Unbound 1.26.1, not the CVE feed** (NVD still "Awaiting Analysis") — the HEIF-Heist "your distro's version is your exposure" lesson again (NLnet Labs via The Hacker News, Sep 16–17, 2026)
- [**RatHat: Android spyware that survives its own uninstall by self-pairing to ADB — Accessibility grant unlocks Developer Options, enables Wireless Debugging, reads the 6-digit pairing code off the screen, and stages shell-privilege native daemons OUTSIDE the app lifecycle that silently reinstall the malware — then drives the victim's screen with a live cloud-LLM loop over the serialized Accessibility tree**](tools/rathat-android-adb-self-pairing-genai-driven-spyware-zimperium-september-2026.md) (Zimperium zLabs via THN, Sep 18, 2026; assessed China-based operators; smishing + malvertising → deceptive download portals): the escalation chain is the durable intel — **Accessibility → Developer Options → Wireless Debugging → pairing code → ADB shell → out-of-lifecycle daemon + FRP reverse tunnel** = an operator with general-purpose shell that uninstall doesn't touch; collection covers overlays, MediaProjection screen capture, SMS, lock-screen PIN, a **hardware-level on-screen keylogger**, address-bar URLs. The **GenAI navigation loop** asks the LLM only benign localization questions (resolve target coordinates as JSON, read on-screen text, SCROLL_DOWN) with the malicious sequencing client-side — which is why the AI calls blend into normal app traffic. Four named pipeline-killer anti-analysis tricks (directory-declared files / ZIP encryption-bit entries / `0x9999` manifest chunks / invalid `element_width` opcodes) mean **"analysis timed out" must never equal "clean."** Hunt: Wireless-Debugging toggles by non-admin apps, accessibility-service use co-timed with ADB pairing dialogs, shell-level daemons surviving uninstall, periodic full-Accessibility-tree XML egress bursts (Zimperium zLabs / The Hacker News, Sep 18, 2026)
- [**The pwaplatform "canary" gets its second independent read: Amazon Inspector's per-version analyses folded into OSV `MAL-2026-16299` at 17:37 UTC — and Inspector's own text calls the scope + 99.0.x version + Collaborator channel "consistent with a dependency-confusion attack against an internal scope," noting the README's authorization claim unverified. Both recon packages STILL LIVE at this wiki's 19:30 UTC re-check**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-20-third-sweep-follow-up-amazon-inspectors-own-writeups-land-in-osv-independently-confirming-the-canary-shape-read-both-packages-still-live) — `@pwaplatform/module-sso-integration` live ~8.5h post-advisory (latest 99.0.1, no npm takedown, no T-Bank/npm public response surfaced); `@insiderintelligence/googleadmanager` live ~98h (latest 9.9.10, timestamp unchanged). `scope:pwaplatform` search returns **zero other packages** — the canary is the scope's only content. Triage-provenance durable read: MAL-2026-16299 is no longer one ML-heuristic verdict but **two independent analyses with matching mechanics**, and Inspector independently states the artifact supports BOTH the authorized-canary story and the attack-recon story — the distinction rests entirely on the publisher's word (this wiki's second-sweep conclusion, now corroborated by a second source). GHSA: critical, ranges pinned `=99.0.0`/`=99.0.1`, no patched version = malware-class "fix" is an unpublish npm hasn't performed (GitHub Advisories / OSV / ossf-malicious-packages commits 61aa5005+4abb1831 + live npm registry re-checks, this wiki, Sep 20)
- [**BlueKit: the first Browser-in-the-Middle PhaaS — the genuine login page runs in an attacker-hosted remote browser and only rendered pixels stream to the victim — and in 38% of observed compromises the kit auto-enrolled its own TOTP on the account, so password rotation + session revoke leaves the attacker in**](ops/bluekit-browser-in-the-middle-phaas-totp-enrollment-persistence-spycloud-september-2026.md) (SpyCloud Labs, Sep 15, 2026): past AiTM's proxy tells — BitM loads the **real** Microsoft/SSO page inside attacker infrastructure and relays interactions (interaction-recording/replay), so "the login page came from the real origin" is true and useless as a check. Panel: 80+ templates incl. device-code and BitM flows, **automated domain purchasing/registration**, SMS + AI-assistant delivery; visitor-qualification gates (custom CAPTCHA, browser fingerprint, **WebRTC/STUN IP enumeration**, RAM/CPU checks, IP reputation) to block scanners before serving. SpyCloud recaptured ~1,000 BlueKit messages = **sessions against 37 orgs** (US/EU defense contractors, law firms, healthcare, food distribution, construction) in two bursts Sep 4 + Sep 10. The durable IR change: **in 38% of compromises the kit drove the authenticated session to enroll its OWN TOTP authenticator** — MFA-valid persistence that **survives a password reset**; session-theft runbooks must **audit registered MFA methods** (new-TOTP-enrollment events outside the user's change window are high-signal). Doraemon branding + Russian-forum ads + CIS-blocking filter suggest Russian-speaking devs (not attributed). Same update: **BlgCloud** leak series — five apparently unaffiliated PwnForums accounts coordinated 15+ French-SaaS-customer breaches under one title format with **no group branding** (ShinyHunters extortion model, post-clout era) — and **LeakBase's late-July revival** on the seized site's own backend database, same copycat-forum playbook as PwnForums/BreachForums (SpyCloud Labs, Sep 15, 2026)
- [**GTIG names the Brazilian payment-switch crew: BREEZE COMET (ex-UNC5669) — Rust/Nim/Go backdoor suite, rogue hardware in retail stores, compromised municipal websites on three continents, and hundreds of fraudulent Pix/STR/Boleto transactions within 24–48 h of reaching the core financial apps**](ops/breeze-comet-gtig-brazilian-financial-switch-intrusions-rust-nim-go-backdoor-suite-september-2026.md) (Google Threat Intelligence Group first full write-up, Sep 1, 2026; captured Sep 20 sweep): financially motivated actor intruding **Brazil's payment rails themselves — Pix, STR, Boleto** — not retail banking: the stated access requirements are **RSFN reach through an entitled entity, mTLS transaction credentials, persistent AD/cloud accounts, and procedural knowledge of anti-fraud controls**. Entry: password spray + IT-support vishing into RMM, **compromised Brazilian small-government websites used as BOTH staging and C2 (same staging behavior replicated on municipal domains in Nigeria, Paraguay, Ghana, Venezuela)**, **rogue hardware devices plugged directly into retail store networks**, and JBoss exploitation (Trend Micro). Tool taxonomy now named: **COBALTSPIN** (Rust reverse-SOCKS5-over-WebSocket tunneler that crosses boundary firewalls with zero host persistence), **LIGHTPAINT** (Java: installs SoftEther, programmatically adds Defender Firewall allow rules, then clears the VPN Platform event log), **MILDFROST** (passive in-JVM JAR backdoor with `DnsCommandBeacon` DNS-tunnel fallback C2), **KICKPLATE** (Nim, impersonates Windows Update Health Tools), **BOATBEAM** (Go fake-IIS on 443 that only activates with a specific session cookie), **REALBREEZE** (custom LDAP brute-forcer). Credential hunting is payment-vocabulary-targeted — file/env searches for **`boleto`, `cnab`, `remessa`, `webhook.*pix`, `instant.*payment`**; CI/CD mined for pipeline creds; malicious **K8s pods** persist and exfil cloud secrets to `dontpad[.]com`. **LLM-generated ops scripts** (recon/credential-validation/deployment code with verbose step banners, Portuguese comments, no human idiosyncrasies). GTIG says the activity **overlaps Plump Spider and SHADOW-AETHER-064** — the cluster our Unit 42 CL-CRI-1163 page already tracks — and the payoff per client forensics: **two waves of hundreds of fraudulent transactions within 24–48 hours** of COBALTSPIN-mediated access to core financial applications, then cross-host event-log clearing. Still no operator identity across the four vendor labels. Hunt: firewall-rule adds + VPN installs + VPN-event-log clears on one host; cookie-differential behavior on unmanaged 443 listeners; the payment-terms grep as a near-signature detection (Google Threat Intelligence Group, Sep 1, 2026)
- [**The `algamil7x` npm cluster: five corporate-scope-squatted packages beacon installer identity over DNS labels to one attacker domain — recon, not payload**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md) (GitHub Advisories sourced to Amazon Inspector, all published Sep 18, 2026): `@tink/tink-link-core` (impersonating the real Tink open-banking SDK), `@shared-web/utils`, `@shared-web/assets`, `@shared-runtime/modules@9.9.10`, `@insiderintelligence/googleadmanager` all run the same install-script/require-time family that collects `os.userInfo().username` + `os.hostname()` + cwd basename and issues `dns.resolve4()` for `<prefix>.<user>.<host>.<cwd>.<ts>.oob.algamil7x.xyz` — **the data lands in the attacker's own authoritative DNS logs; there is no HTTP egress to block**. This is **target qualification**: every install tells the operator who/where/which-project before any payload is committed, so removal alone doesn't close exposure — the operator already learned you exist. Shared obfuscation lineage (hex/char-code byte arrays + `module.constructor._load` in numbered `lib/<hexish>.js`); `@shared-web/assets` also fires on plain `require`, defeating npm ≥12 install-script gates. Live registry re-check (this wiki, Sep 20): **survivor `@insiderintelligence/googleadmanager` still resolves (latest 9.9.10)** ~36h post-advisory — and the same Sep 18–19 advisory burst carried a neighboring dependency-confusion wave: `chai-as-indexed` POSTs **full `process.env`** to a base64-concealed **`vercel.app`** endpoint and pipes the response into `new Function('require', …)` = server-supplied code with real require at import time; `x509-escaping` exfils `/etc/passwd`+`/etc/hosts` to a **Burp Collaborator (`oastify.com`) subdomain** — attacker C2 living on infrastructure no egress policy blocks; `internallib_v949` squats an internal name and pipe-to-shells a reverse shell. Hunt in **DNS logs** for the label grammar + flag build hosts querying `*.oastify.com`; audit any scoped install under plausible-internal names not on your allow-list. No attribution exists or is implied. Monitor: survivor's takedown, domain rotation, follow-on payload wave at orgs that beaconed)

## Sections
- **Ops** — campaign timelines, compromise chains, and sequencing
- **Tools** — malware, payloads, implants, and attacker infrastructure
- **Groups** — crews, cluster names, and shared operational personas
- **People** — publicly identified individuals or project personas when public sourcing supports it
- **Patterns** — reusable defender heuristics
- **Notes** — taxonomy, usage, and editorial guidance
