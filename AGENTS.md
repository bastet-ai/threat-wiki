# AGENTS.md — threat.wiki

## Purpose
This repo is a public threat-intelligence wiki. Prefer durable, actionable, source-linked content over raw notes.

## Agent workflow
- Read this file at the start of each task.
- Update this file whenever you learn something important about the repo, workflow, build, or collaborator preferences.
- After every meaningful repo update, create a git commit and push it to `origin` unless the user explicitly tells you not to.
- Use clear, non-interactive git commands and keep commit messages specific to the change.

## Recursive self-improvement
Follow the [Recurse.bot guide](https://recurse.bot/) approach: treat `AGENTS.md` as the project memory for future agents.

- Record wins to repeat and mistakes to avoid.
- Capture exact build, test, and publish commands that were actually verified.
- Note project-specific conventions, taxonomy decisions, and stable public paths.
- Record collaborator preferences that materially improve future handoffs.
- Keep entries concise, concrete, and easy to scan.

## Writing conventions
- Keep links clickable and explicit in Markdown.
- Use short, descriptive page titles.
- Prefer bullets over long paragraphs for ops, tooling, and motivations.
- Add a `Tags` section to group/people/ops/tool pages when possible.
- Use plain language and avoid overclaiming attribution.
- Prefer names used by the operators, maintainers, projects, or other firsthand sources over later threat-intel vendor branding when a durable public source supports that choice.
- When alternative names matter, attribute them to the vendor or report that coined or used them and link the source.
- A `People` page may use a GitHub username or project persona as the title when that is the clearest publicly supported identifier.
- If a human identity is not clearly supported by a public source, do not invent one.

## Content structure
- **Groups**: named crews, clusters, and shared operational personas
- **People**: publicly identified individuals with direct operational relevance
- **Ops**: compromise chains, operator workflows, and campaign sequencing
- **Tools**: malware, worms, payloads, and attacker infrastructure
- **Patterns**: reusable defender heuristics and recurring tradecraft
- **Notes**: taxonomy, page usage, and editorial guidance
- **Blog**: short updates or summaries that can feed the landing page

## MkDocs / GitHub Pages lessons learned
- Use `theme.custom_dir` for template overrides; do not add a non-MkDocs `overrides:` key to `mkdocs.yml`.
- Keep the Pages workflow strict-friendly; any config warning can fail the deploy.
- Include an RSS feed (`docs/feed.xml`) if you want a simple subscription surface.
- `docs/blog/index.md` is a hand-curated discovery surface and can link directly to notable group, ops, or tool pages without requiring a separate `docs/blog/*.md` post.
- `docs/feed.xml` is manually maintained; linking a new page from `docs/blog/index.md` does not update the feed automatically.
- `hooks/tag_index.py` rewrites page `## Tags` lists into clickable links at build time and regenerates `docs/notes/tag-index.md`; commit the regenerated page when tags change.
- Keep the landing page updated with a manual “Recent entries” section capped at 10 links.
- If Pages 404s, check the Actions workflow status first; a failed build can look like a site or cert problem.
- As of 2026-03-26, `uvx --from mkdocs-material mkdocs build --strict` emits a `uvx` warning that `mkdocs` comes from the `mkdocs` dependency, but the command still exits `0` and completes the build.
- As of 2026-03-26, `uvx --from mkdocs-material mkdocs build --strict` reports `docs/blog/2026-03-26-teampcp.md` as outside nav, but this is currently info-only and does not fail the local build.

## Verified commands
- `python3 scripts/generate_drafts_from_todo.py`
- `uvx --from mkdocs-material mkdocs build --strict`
- `npm run test:sources` checks the WebLogic source section renders clickable Markdown links and that each external source returns HTTP 200 with the expected CVE token.
- `./contribute.sh --dry-run`

## Maintenance rules
- HackerOne public program checks are currently paused; do not re-enable without explicit instruction.
- `TODO.md` at the repo root is the internal profiling backlog for future `Groups`, `People`, and `Ops` coverage; it is intentionally outside the published docs.
- `drafts/` contains unpublished scaffold pages generated from `TODO.md`; do not confuse them with sourced public wiki content in `docs/`.
- `scripts/select_next_draft.py` picks the next unpublished backlog draft by priority (`ops`, then `groups`, then `people`) based on whether a matching public page exists yet.
- When adding a new group, people, ops, or tool page, update:
  - `mkdocs.yml` nav
  - `docs/index.md` recent entries
  - blog index if it is a notable writeup
- Keep page paths stable once linked publicly.
- Seqrite APT Team posts can be useful primary sources for targeted espionage chains; keep China-linked attribution caveated when Seqrite only gives moderate confidence and no named group.
- Wordfence vulnerability intelligence can be useful for active WordPress plugin exploitation; if direct Wordfence pages return empty locally, use NVD plus secondary reporting that quotes Wordfence telemetry and keep source caveats clear.
- Prefer one well-structured page per group/campaign over scattered notes.
- Maintain `docs/notes/editorial-checklist.md` as a living checklist when the site evolves.
- Maintain `docs/notes/source-index.md` as the canonical list of subscribed RSS/Atom and primary-source feeds.
- The current taxonomy is `Groups`, `People`, `Ops`, `Tools`, `Patterns`, `Notes`, and `Blog`.
- Group pages currently live under `docs/actors/`; keep that path stable unless there is an explicit migration plan.
- There is no top-level `Orgs` section today; document organizations inside the relevant `Ops`, `Groups`, or `Notes` page unless the taxonomy changes.
- When writing an `Ops` page, explicitly investigate whether there is a missing companion `People` or `Groups` page that should be added in the same change.
- Unit 42's CL-CRI-1089 is currently best handled as operation coverage unless multiple sources establish a stable named group profile; Operation FlutterBridge / FlutterShell coverage lives at `docs/ops/operation-flutterbridge-fluttershell-macos-malvertising.md`.
- SideCopy is a stable public group name; Operation XENOFISCAL coverage lives at `docs/ops/operation-xenofiscal-sidecopy-xenorat.md` with a companion `docs/actors/sidecopy.md` profile. Keep Seqrite's medium-to-high confidence wording attached to the source.
- If an `Ops` page overlaps unresolved actor-alias questions, publish the operation first and keep alias caveats attributed in the page unless a separate `Groups` or `People` profile is strongly sourced.
- For `Handala` coverage, use `Handala` as the page title and attribute vendor names (`Void Manticore`, `Storm-0842`, `Red Sandstorm`, `Banished Kitten`) plus linked personas (`Karma`, `Homeland Justice`) inside the page; the March 19, 2026 DOJ domain-seizure release is the most durable official source tying those persona domains to one `MOIS` playbook.
- Use `./contribute.sh` for Codex contribution sweeps; `./contribute.sh 10` should run 10 sequential one-contribution passes that each read `CONTRIBUTING.md`, make a focused addition, and commit/push the result.
- If a recursive `./contribute.sh` run spends several minutes only searching and has not written any repo files yet, stop it and publish the selected draft directly rather than letting a stalled nested Codex session block the backlog.
- Local terminal safety prompts can block bulk HTML scans that include `.dev` domains such as Socket or SafeDep; split source checks or use available RSS/feed paths rather than leaving a cron scan pending approval.
- Microsoft Security Blog article pages may return HTTP 403 to simple `urllib` fetches; a normal browser User-Agent with `curl -o` to a temp file worked for the June 2026 Miasma post. Do not pipe fetched HTML directly into an interpreter.
- JFrog Security Research may publish high-signal real-time posts outside the JFrog Blog RSS feed; include `https://research.jfrog.com/` HTML checks for items such as IronWorm when monitoring supply-chain malware.
- StepSecurity RSS can surface urgent npm worm activity quickly; `binding.gyp`/`node-gyp` install-time execution deserves separate scrutiny from obvious `package.json` lifecycle hooks.
- Google Cloud / Mandiant Threat Intelligence may publish high-signal operation/tool chains where the durable value is the intrusion sequence rather than a full actor profile; for UNC6692 / SNOW, current coverage lives at `docs/ops/unc6692-snow-malware-social-engineering.md` without a separate group profile until broader public actor history justifies one.
- AutoJack / AutoGen Studio coverage lives at `docs/patterns/agent-localhost-control-plane-rce.md`; keep Microsoft stable-release caveats separate from The Hacker News' PyPI pre-release finding (`0.4.3.dev1` / `0.4.3.dev2`) and preserve the broader lesson that localhost is not a security boundary for browsing agents.
- GMO Flatt Security Research can provide durable AI-agent / GitHub Actions trust-boundary research; Claude Code GitHub Action prompt-injection coverage lives at `docs/patterns/claude-code-github-action-prompt-injection.md` and should be treated as a reusable pattern, not a named intrusion.
- Proofpoint Threat Insight is useful for email-driven actor cluster profiles; TA4922 coverage lives at `docs/actors/ta4922.md`. Keep Proofpoint's financially motivated Chinese-speaking cybercrime assessment separate from Silver Fox / Void Arachne overlap unless future sources establish stronger aliasing.
- Arctic Wolf Labs can surface identity-first PhaaS operations where the durable value is the authentication-flow abuse and SaaS expansion rather than malware; Kali365 coverage lives at `docs/ops/kali365-device-code-phishing-expansion.md`.
- Hunt.io and SentinelOne are useful for exposed attacker-infrastructure and cloud-worm follow-ups; PCPJack coverage lives at `docs/ops/pcpjack-cloud-smtp-relay-network.md`, with TeamPCP relationship caveated as adjacency / artifact-removal behavior rather than confirmed shared control.
- ReliaQuest Threat Research can surface durable incident-response-backed actor clusters and IIS/web-shell tradecraft; OP-512 coverage lives at `docs/actors/op-512.md`, with China-linked attribution kept to ReliaQuest's moderate-to-high-confidence assessment and aliasing kept separate until future public reporting supports it.
- Microsoft Security Blog can provide primary-source AI-agent CI/CD trust-boundary research; the June 5, 2026 Claude Code Action case adds `/proc/self/environ` Read-tool exposure to `docs/patterns/claude-code-github-action-prompt-injection.md` alongside the earlier GMO Flatt GitHub App / issue-triage boundary coverage.
- CISA KEV additions for internet-facing managed-file-transfer or edge services can justify concise ops pages even when public impact is availability-only; SolarWinds Serv-U CVE-2026-28318 coverage lives at `docs/ops/solarwinds-serv-u-cve-2026-28318-exploitation.md`.
- Cisco SD-WAN Manager exploitation advisories can justify concise edge/control-plane ops pages when Cisco confirms exploitation and no direct fix/workaround exists; CVE-2026-20245 coverage lives at `docs/ops/cisco-catalyst-sd-wan-manager-cve-2026-20245-exploitation.md`.
- Hunt.io TeamPCP Python toolkit / FIRESCALE analysis is durable actor-profile material: keep the post-delivery toolkit details on `docs/actors/teampcp.md` unless a future wave warrants a separate ops page.
- StepSecurity's June 6 Azure/durabletask follow-up adds a distinct Miasma blast-radius lesson: repository-level AI-assistant/editor persistence can lead to GitHub disabling upstream Microsoft repositories and breaking downstream workflows that reference mutable GitHub Action tags such as `Azure/functions-action@v1`; keep this on `docs/ops/binding-gyp-npm-cicd-worm.md` and cross-reference the broad Mini Shai-Hulud page rather than creating a separate Microsoft-only page unless more primary Microsoft/GitHub postmortem detail emerges.
- Google Cloud / Mandiant's June 5, 2026 UNC3753 law-firm campaign is stable actor-profile material; coverage lives at `docs/actors/unc3753.md` with aliases Luna Moth, Chatty Spider, and Silent Ransom Group, and should be kept separate from BlackFile / UNC6671 unless future public reporting establishes overlap.
- OX Security's June 4, 2026 Miasma / `binding.gyp` update adds measurement and hunting detail to `docs/ops/binding-gyp-npm-cicd-worm.md`: 57 packages, 152,376 weekly downloads, 647,204 monthly downloads, 118+ GitHub repos with stolen credentials, and an en-dash repository-description variant `Miasma – The Spreading Blight`; add punctuation variants to hunts instead of creating a new page.
- SafeDep's June 5, 2026 Miasma source-repository analysis adds a reusable pattern: repository-local config can auto-execute malware through Claude Code / Gemini `SessionStart`, Cursor `alwaysApply`, VS Code `folderOpen`, and `npm test`; coverage lives at `docs/patterns/developer-tool-config-auto-execution.md` and the Miasma-specific details stay on `docs/ops/binding-gyp-npm-cicd-worm.md`.
- OX Security's March 27, 2026 Telnyx report is useful TeamPCP backfill: coverage lives at `docs/ops/telnyx-pypi-teampcp-compromise.md`; keep it tied to OX's attribution and Telnyx's caveat that only the Python package, not Telnyx service infrastructure, was compromised.
- Kaspersky Securelist can surface Windows exploit-detection items worth concise ops coverage even before final vendor patch naming; MiniPlasma coverage lives at `docs/ops/miniplasma-windows-cloud-filter-lpe-exploitation.md` and should keep public-exploit / patch-window language caveated to Kaspersky's June 2026 reporting.
- Hunt.io exposed-infrastructure writeups can justify ops coverage when they reveal durable C2 and intrusion sequencing; Oman government Iranian-nexus webshell coverage lives at `docs/ops/oman-government-iranian-nexus-webshell-c2.md`. Do not reproduce exposed victim PII; keep Hunt.io's no-group-level-attribution caveat intact when discussing APT34 / MuddyWater overlap.
- Hunt.io's April 2026 Axios / `plain-crypto-js` analysis is useful payload backfill for `docs/ops/operation-dangerouspassword-axios-npm-compromise.md`: keep ESET's Operation DangerousPassword / Lazarus framing and Socket's package-release detail, while attributing TA444 / BlueNoroff infrastructure-overlap claims specifically to Hunt.io.
- Hunt.io smishing-infrastructure writeups can justify concise ops coverage when they expose reusable campaign pivots; 19-country government / postal / telecom smishing coverage lives at `docs/ops/huntio-global-smishing-government-postal-telecom.md`. Prefer shared fingerprints such as the 128-character metadata hash and repeated assets over long static domain lists.
- Socket's June 7, 2026 Hades report adds a PyPI branch to Miasma / Mini Shai-Hulud coverage: 37 malicious wheels across 19 packages used `*-setup.pth` Python-startup execution, Bun v1.3.13, and `_index.js` credential theft. Coverage stays on `docs/ops/binding-gyp-npm-cicd-worm.md` under `#june-7-hades-pypi-wheel-wave` rather than a separate page unless the PyPI branch develops independently.
- StepSecurity's June 8, 2026 Hades graph-ML / bioinformatics follow-up is a major evolution of the same page: keep it on `docs/ops/binding-gyp-npm-cicd-worm.md` under `#june-8-hades-graph-ml-import-hook-wave`; important deltas are `__init__.py` import-hook execution, Bun v1.3.14, LLM-analysis prompt-injection evasion, macOS/Windows runner-memory scrapers, `DontRevokeOrItGoesBoom` / `TheBeautifulSnadsOfTime` / `firedalazer` GitHub C2, SSH/SCP lateral movement, and `gh-token-monitor` wiper-deterrent persistence.
- StepSecurity's June 8, 2026 `Pythagora-io/gpt-pilot` force-push report stays on `docs/ops/binding-gyp-npm-cicd-worm.md` under `#june-8-pythagora-iogpt-pilot-force-push-attempt`; treat it as Shai-Hulud-family source-repository takeover coverage with original-actor vs copycat caveat, and keep the defender lesson focused on branch protection plus lint/format gates as tripwires.
- Unit 42 identity/social-engineering guidance can justify reusable pattern pages when it adds measurable telemetry and hardening steps; Microsoft Teams external-chat phishing coverage lives at `docs/patterns/microsoft-teams-external-chat-phishing.md` with emphasis on open federation, unmanaged accounts, typosquatted/partner tenants, identity-event correlation, and chat removal/reporting.
- Public local-root exploit writeups can justify concise ops coverage when they add realistic post-compromise escalation risk for shared Linux/container hosts; CVE-2026-23111 coverage lives at `docs/ops/linux-nftables-cve-2026-23111-public-lpe-exploits.md` and should keep the no-remote-vector / no-active-exploitation caveat clear unless later reporting changes it.
- Microsoft Security Blog AI-brand social-engineering roundups can justify pattern coverage when they include concrete phishing, malvertising, code-hosting, signing, and IoC detail; AI-brand impersonation coverage lives at `docs/patterns/ai-brand-impersonation-phishing-malvertising.md`. Keep the "brand abuse, not AI-service compromise" caveat explicit.
- Trend Micro Research can provide durable follow-up on active vulnerability reuse where the main lesson is patch-management blind spots and cluster separation; WinRAR CVE-2025-8088 follow-up lives on `docs/ops/gamaredon-gammaphish-gammaworm-gammasteel-chain.md` with companion `docs/actors/uac-0226-shadow-earth-066.md`. Keep UAC-0226 / SHADOW-EARTH-066 separate from Gamaredon / Earth Dahu unless future primary sources merge them.
- Google Chrome Releases can justify concise ops coverage when Google confirms in-the-wild exploitation of browser zero-days even before actor or target detail is public; Chrome V8 CVE-2026-11645 coverage lives at `docs/ops/chrome-v8-cve-2026-11645-exploitation.md`.
- CleverHans Lab / arXiv can provide durable AI-security research to fold into patterns rather than actor pages; adaptive local-LLM worm research is tracked on `docs/patterns/ai-augmented-adversary-operations.md` as a defender pattern, not as in-the-wild attribution.
- Volexity is useful for incident-response-backed appliance and edge-device espionage coverage; VerdantBamboo / WARP PANDA / UNC5221 coverage lives at `docs/actors/verdantbamboo.md` with companion operation coverage at `docs/ops/verdantbamboo-appliance-brickstorm-operation.md`. Keep Clay Typhoon aliasing attributed to secondary/source-specific reporting unless a primary source in the page supports it.
- Proofpoint can surface durable DPRK/developer-targeting repository-phishing clusters; UNK_DeadDrop coverage lives at `docs/ops/unk-deaddrop-developer-repository-phishing.md`. Treat it as a distinct Proofpoint cluster from Contagious Interview unless future public telemetry establishes direct overlap, and keep the defender focus on GitHub/GitLab lures, VS Code / Cursor `folderOpen` tasks, malicious VSIX persistence, Overlord payloads, and cryptocurrency-wallet theft.
- Check Point Research can provide primary active-exploitation coverage for edge/VPN flaws; Remote Access VPN / Mobile Access IKEv1 CVE-2026-50751 coverage lives at `docs/ops/check-point-vpn-cve-2026-50751-exploitation.md`. Keep Check Point's medium-confidence financially motivated / Qilin ransomware-affiliate assessment caveated, and start defender review windows at the vendor's earliest observed exploitation date when supplied.
- Hunt.io exposed-toolkit reports can justify management-plane ops coverage when they reveal downstream blast radius; Quest KACE SMA CVE-2025-32975 coverage lives at `docs/ops/quest-kace-sma-cve-2025-32975-exploitation.md`. Do not reproduce exposed client lists or personal data from leaked directories; keep defender focus on patch levels, appliance admin takeover, MSP/customer scoping, and reported tooling.
- CISA KEV additions for AI gateway / MCP command-execution flaws can justify concise ops coverage when they confirm exploitation; LiteLLM CVE-2026-42271 coverage lives at `docs/ops/litellm-cve-2026-42271-mcp-stdio-command-injection.md` and should stay linked to the broader MCP stdio command-execution pattern.
- CISA KEV additions for network-infrastructure tunnel/overlay flaws can justify concise ops coverage even when impact is integrity/path manipulation rather than code execution; Arista EOS CVE-2026-7473 coverage lives at `docs/ops/arista-eos-cve-2026-7473-tunnel-decap-exploitation.md`, and should emphasize tunnel endpoint inventory plus ACL/TCAM mitigation rather than a software-upgrade path.
- Unit 42 cloud-control-plane research can justify pattern pages when it yields reusable detection / hardening guidance without a named intrusion; cloud logging tampering coverage lives at `docs/patterns/cloud-logging-control-plane-tampering.md` and treats AWS CloudTrail / Google Cloud Logging route, destination, KMS, and mutable-log-object changes as tier-zero controls.
- No-CVE SaaS exploitation notices can justify concise ops pages when the vendor reports anomalous activity or customer data queries; ServiceNow hosted-instance unauthenticated table-query coverage lives at `docs/ops/servicenow-instance-unauthenticated-table-query-exploitation.md`, with no actor attribution unless future public reporting establishes one.
- Trend Micro TrendAI reports can justify operation coverage when AI-agent use is evidenced inside real intrusions rather than only tooling research; SHADOW-AETHER-040 / SHADOW-AETHER-064 coverage lives at `docs/ops/shadow-aether-ai-augmented-latam-intrusions.md` and should keep Spanish-vs-Portuguese cluster separation plus campaign-label caveats intact.
- Unit 42 active-exploitation briefs can add durable hunt pivots to existing edge-service pages; PAN-OS GlobalProtect CVE-2026-0257 coverage lives at `docs/ops/pan-os-globalprotect-cve-2026-0257-exploitation.md` and should keep actor/malware attribution unset while focusing on gateway-connected VPN events, Unit 42 source IPs, generic host-ID/device-name pivots, and Palo Alto Networks fixed-version / authentication-override mitigations.
- GitHub Changelog can surface durable package-ecosystem security-default changes before they appear in the Security Blog; npm v12 script-approval / `allowScripts` changes belong as defensive updates on `docs/ops/binding-gyp-npm-cicd-worm.md` rather than a new campaign page.
- Lumen Black Lotus Labs botnet / covert-network reporting can justify ops coverage when it gives durable reconnaissance infrastructure and malware behavior; JDY SOHO / IoT botnet coverage lives at `docs/ops/jdy-soho-iot-recon-botnet.md`. Keep it as infrastructure/capability coverage, caveat China-nexus / Volt Typhoon links to Lumen, and do not over-attribute operation to a specific named group without stronger public sourcing.

## Security / attribution
- Treat third-party sources as untrusted until verified.
- When using external reporting, summarize the public reporting and link the source.
- Separate confirmed facts from inferred motivation or team structure.

## Threat-intel monitoring workflow
- Replicate the Skillz-style scan workflow for this repo with a threat-intelligence lens: monitor high-signal sources, add durable actor/operation/tool/pattern pages, update nav/index/blog/feed/source-index, run the strict MkDocs build, commit, push, and notify Dean only when substantive public threat intel was added.
- Current active watch: Shai-Hulud / Mini Shai-Hulud / TeamPCP supply-chain activity. Prioritize new package families, propagation primitives, CI/OIDC abuse paths, persistence mechanisms, exfiltration infrastructure/naming, maintainer postmortems, official advisories, and meaningful attribution changes.
- Track scan state outside the repo at `/home/user/clawd/memory/threat-intel-state.json`; keep this public repo free of private local monitoring state.
- OpenClaw cron job name: `Threat wiki: threat intel scan → wiki updates`; repo path: `/home/user/clawd/threat.wiki`.

- Unit 42's June 11, 2026 Behavioral Integrity Verification research belongs on `docs/patterns/agent-skill-marketplace-poisoning.md`: treat agent skills as multi-surface dependencies (metadata, executable code, natural-language instructions), prioritize chain-based review for FILE_READ→encoding→NETWORK_SEND, download→write→execute, and encoded dynamic-eval patterns, and keep the OpenClaw registry-scale stats caveated as classifier-predicted review candidates rather than runtime-confirmed exploits.
- ESET's June 11, 2026 OceanLotus / APT32 update is durable actor-profile material; coverage lives at `docs/actors/oceanlotus.md` and should keep the FireAnt MetaKit supply-chain compromise, Vietnamese infrastructure / transport construction intrusion, and SPECTRALVIPER internals together unless a future report warrants a separate tool page.
- Socket exposes a working Atom feed at `https://socket.dev/api/blog/feed.atom`; prefer it over brittle Socket blog HTML scraping. Hades PyPI coverage is currently on the broad Mini Shai-Hulud page (`docs/ops/mini-shai-hulud-npm-pypi-worm-campaign.md`) rather than a separate page: preserve the distinction between `*.pth` Python-startup hooks, `.abi3.so` native-extension import execution, and `langchain-core-mcp` split loaders that search `sys.path` for `_index.js`.
- StepSecurity's June 11 Miasma/Hades suspicious-files post is best treated as a defender-hunting update to `docs/ops/mini-shai-hulud-npm-pypi-worm-campaign.md` and `docs/patterns/developer-tool-config-auto-execution.md`: scan developer endpoints/repositories for quiet project-tree triggers (`binding.gyp`, injected Python `__init__.py`, `.vscode/tasks.json`, `.claude/setup.mjs`) because manifest/lockfile-only review misses open-time and import-time execution lanes.
- Boost Security Labs' June 11 Sleeper Squats writeup is durable CI/CD identity-boundary material; coverage lives at `docs/patterns/github-actions-oidc-subject-claim-collisions.md`. Keep the key lesson focused on unambiguous OIDC `sub` delimiters, immutable ID claims such as `repository_owner_id`, and avoiding broad string-only trust policies rather than treating the short-lived hyphen-format GitHub rollout as an active compromise.
- CISA KEV plus watchTowr exploit analysis can justify concise edge-appliance ops coverage; Ivanti Sentry CVE-2026-10520 coverage lives at `docs/ops/ivanti-sentry-cve-2026-10520-exploitation.md` and should keep the unmanaged/exposed Sentry caveat, fixed R10.5.2/R10.6.2/R10.7.1 versions, companion CVE-2026-10523 authentication bypass, and `/mics/api/v2/sentry/mics-config/handleMessage` detection pivot together.
- Google Cloud / Mandiant's June 11, 2026 Oracle PeopleSoft CVE-2026-35273 report is durable zero-day/extortion coverage: operation coverage lives at `docs/ops/oracle-peoplesoft-cve-2026-35273-shinyhunters.md` with a narrow companion actor stub at `docs/actors/shinyhunters.md`; keep UNC6240 / ShinyHunters attribution tied to Mandiant, emphasize PSEMHUB and `/PSIGW/HttpListeningConnector` exposure control, and avoid importing unrelated ShinyHunters ecosystem claims without primary sourcing.
- Trend Micro's June 18, 2026 PeopleTools CVE-2026-35273 analysis is useful technical backfill for `docs/ops/oracle-peoplesoft-cve-2026-35273-shinyhunters.md`: preserve the PSIGW `IBRequest` SSRF to localhost `PSEMHUB`, `validateClient` bypass, `envmetadata/transactions/` staging, `envmetadata/data/environment/` XMLDecoder persistence, restart-triggered in-JVM execution, and the lesson that child-process / beacon-only detections may miss successful exploitation.
- JFrog Security Research's June 11, 2026 Solana FakeFix report is durable cryptocurrency-developer supply-chain coverage: operation coverage lives at `docs/ops/solana-fakefix-npm-pypi-developer-stealer.md`; keep the defender lesson focused on GitHub issue-spam patched-SDK lures, npm `postinstall`, PyPI `__init__.py` import-time execution, Telegram C2, fake MEV private-key prompts, and CMS-themed Windows Deno loader persistence rather than treating it as Shai-Hulud lineage without sourcing.
- Check Point Research's June 12, 2026 LangGraph checkpointer disclosure is durable AI-agent framework trust-boundary coverage: pattern coverage lives at `docs/patterns/langgraph-checkpointer-injection-rce.md`; keep it scoped to self-hosted LangGraph SQLite / Redis checkpointers, `get_state_history()` user-controlled filter keys, SQLite SQLi plus unsafe `msgpack` deserialization, Redis RediSearch query injection, and patch targets `langgraph-checkpoint-sqlite` 3.0.1+, `langgraph` 1.0.10+, and `@langchain/langgraph-checkpoint-redis` 1.0.2+.
- Tenet Security's June 9, 2026 Sentry Agentjacking research is durable MCP/tool-output-injection pattern coverage: `docs/patterns/sentry-mcp-agentjacking.md` should keep the caveat that Sentry DSNs are intentionally public and this is not a Sentry exploit, while emphasizing public observability-event data becoming fake remediation instructions that agents may execute via `npx`/package-manager/shell tools.
- Trend Micro's May 13, 2026 TeamPCP KICS / elementary-data analysis is useful backfill for existing TeamPCP pages: keep `elementary-data==0.23.3` coverage with the Bitwarden / Checkmarx Shai-Hulud Third Coming page unless a future source warrants a standalone operation page; the durable lesson is PR-comment-to-GitHub-Actions `run:` injection causing project-signed PyPI/GHCR releases, plus `.pth` startup execution and AWS Secrets Manager / SSM live enumeration.
- Trend Micro's Void Dokkaebi / Famous Chollima InvisibleFerret update is durable actor-profile material; coverage lives at `docs/actors/void-dokkaebi.md`. Keep the core lesson focused on fake recruiter developer targeting, BeaverTail evolution, Cython-compiled `.pyd` / `.so` Python extension modules, launcher-script supplied C2, wallet-extension trojanization, and macOS Chrome downgrade behavior.
- SafeDep's June 12, 2026 `astro.config.mjs` / blockchain-C2 PR writeup is durable source-repository supply-chain coverage; operation coverage lives at `docs/ops/astro-config-blockchain-c2-pr-injection.md` and should keep the lesson focused on executable build-tool config, plausible PR narratives that do not match touched files, horizontal-whitespace diff hiding, Tron/BSC transaction-input staging, and PolinRider attribution caveated to SafeDep / OpenSourceMalware linkage.
- Arctic Wolf's June 12, 2026 PAN-OS GlobalProtect CVE-2026-0257 follow-up adds post-VPN-session intrusion detail to `docs/ops/pan-os-globalprotect-cve-2026-0257-exploitation.md`; keep the core lesson focused on authentication-override-cookie exposure conditions, `gateway-connected` triage, VPS/Tor/unusual-geography source pivots, device names such as `GP-CLIENT` / `DESKTOP-GP01` / `kali`, and rapid SMB / NTLM / Impacket-style reconnaissance from VPN-assigned addresses rather than assigning a named actor.
- Sonatype Security Research's June 11, 2026 Atomic Arch report plus StepSecurity's June 12 follow-up are durable AUR supply-chain coverage: operation coverage lives at `docs/ops/atomic-arch-aur-package-hijack.md`; keep the lesson focused on orphaned AUR package adoption, malicious `PKGBUILD` / install-hook npm or Bun fetches, `atomic-lockfile` / `js-digest` / `lockfile-js`, native Linux credential-stealer payloads, and Arch / WSL2 / self-hosted-runner exposure rather than treating it as confirmed Shai-Hulud or TeamPCP lineage without sourcing.
- Socket's June 12, 2026 Chrome live-wallpaper extension report is durable browser-extension adware / ad-fraud coverage: operation coverage lives at `docs/ops/chrome-live-wallpaper-extension-ad-fraud.md`; keep it scoped to 152 Chrome Web Store new-tab extensions, 38 publisher accounts, `tabplugins[.]com` / `yowgames[.]com` / `chromewallpaper[.]com`, forged Google organic attribution, cloaked uninstall redirects, false privacy disclosures, and the `Deleted IndexedDB database:` service-worker fingerprint rather than overstating it as malware or search hijacking.
- Sygnia's June 2026 Velvet Ant / Operation Highland report is durable actor and operation coverage: `docs/actors/velvet-ant.md` and `docs/ops/operation-highland-velvet-ant-authentication-stack-backdoors.md` should keep the lesson focused on segmented-network intrusion paths, GS-Netcat / SOCKS5 / Nginx-FastCGI staging, backdoored `pam_unix.so` and OpenSSH binaries, authentication-stack verification before credential rotation, and Sygnia-scoped China-nexus attribution.
- Sygnia's June 2026 crypto supply-chain analysis is durable pattern coverage: `docs/patterns/crypto-supply-chain-transaction-authority.md` should keep the lesson focused on containment across developer endpoint → repo/CI → automation identity → cloud/Kubernetes → secrets → custody/transaction authority rather than treating it as a named actor or single campaign.
- watchTowr's June 12, 2026 Splunk Enterprise CVE-2026-20253 research is durable security-platform edge exposure coverage: `docs/ops/splunk-enterprise-cve-2026-20253-preauth-file-write-rce.md` should keep the lesson focused on unauthenticated PostgreSQL Sidecar Service access, Splunk Web proxy paths such as `/splunkd/__raw/v1/postgres/recovery/backup`, file-write-to-RCE escalation through Splunk-executed scripts, Splunk Cloud non-exposure caveats, and patch targets 10.2.4 / 10.0.7+ rather than claiming confirmed exploitation.
- Google's June 12, 2026 Outsider Enterprise litigation / safety post is durable PhaaS coverage: `docs/ops/outsider-enterprise-smishing-phaas.md` should keep the lesson focused on China-based Telegram-coordinated SMS phishing-kit distribution, Gemini-assisted phishing-page generation as reported by Google, 9,000 fake websites / 1M+ URLs / 2.5M Android-user messages in two weeks, and Google/FBI/carrier disruption rather than inventing human identities for the operators.
- watchTowr's CVE-2026-41940 cPanel / WHM analysis is useful backfill for `docs/ops/mr-rot13-cpanel-cve-2026-41940-backdoor-campaign.md`: keep the exploit lesson focused on pre-auth `whostmgrsession` creation, stripped `,<ob>` cookie suffixes, HTTP Basic CRLF injection into `/var/cpanel/sessions/raw/`, forged `hasroot` / `tfa_verified` / `cp_security_token` fields, and cPanel's fixed branch list.
- JFrog's June 15, 2026 Shai-Hulud AI-scanner refusal-evasion note is best handled as an update to `docs/ops/mini-shai-hulud-npm-pypi-worm-campaign.md`: emphasize that prompt-like package text can trip model guardrails so a scanner refusal is a null/suspicious result, not a clean verdict; keep deterministic detections for lifecycle hooks, obfuscated `eval`, Bun/runtime fetches, and analyzer-manipulation text.
- StepSecurity / Socket / Snyk's June 17, 2026 Mastra `easy-day-js` incident is durable npm scope-compromise coverage: operation coverage lives at `docs/ops/mastra-easy-day-js-npm-scope-compromise.md`; keep the lesson focused on stale maintainer access, publish-time dependency injection absent from the clean source tree, `easy-day-js@1.11.22` postinstall execution, `23.254.164.92:8000` / `23.254.164.123:443` infrastructure, and cross-platform Node.js persistence rather than treating it as confirmed TeamPCP / Mini Shai-Hulud activity without sourcing.
- Microsoft Security Blog's June 17, 2026 Crypto Clipper report is durable Windows malware / removable-media coverage: operation coverage lives at `docs/ops/crypto-clipper-tor-usb-worm.md`; keep the lesson focused on malicious `.lnk` / USB propagation, scheduled-task persistence, WScript / ActiveX execution, `ugate.exe` portable Tor, `localhost:9050` hidden-service C2, clipboard seed/private-key theft, wallet-address substitution, screenshots, and EVAL tasking rather than treating it as only commodity clipper activity.
- Socket's June 15, 2026 GlassWASM Open VSX report is best handled as an update to `docs/ops/glassworm-developer-supply-chain-botnet.md`: keep Socket's medium-confidence GlassWorm-developer attribution caveated, scope the malicious extensions to Open VSX copies (`exargd/vsblack@0.0.1`, `noellee-doc/flint-debug@0.1.1`) rather than the clean VS Code Marketplace originals, and emphasize TinyGo WebAssembly, Solana SPL Memo dead-drop C2, and editor-extension directory hunting.
- CISA KEV additions for CMS extension unauthenticated RCE can justify concise ops pages when vendor advisories confirm active automated exploitation; Joomla JCE CVE-2026-48907 coverage lives at `docs/ops/joomla-jce-cve-2026-48907-exploitation.md` and should keep the lesson focused on unauthenticated `profiles.import`, rogue JCE editor profiles, PHP uploads in `images` / `media` / `tmp`, patch-before-clean sequencing, and Widget Factory's `2.9.99.6` / legacy-patch guidance.
- Cisco SD-WAN Manager KEV follow-ons can update the existing `docs/ops/cisco-catalyst-sd-wan-manager-cve-2026-20245-exploitation.md` page when they affect the same control-plane/root-impact triage path; CVE-2026-20262 coverage lives there as a June 15 companion section with `/var/log/nms` WAR-upload pivots.
- LiteSpeed user-end cPanel Plugin CVE-2026-54420 coverage lives at `docs/ops/litespeed-cpanel-plugin-cve-2026-54420-exploitation.md`; keep the lesson focused on FTP/web-shell foothold to root on CloudLinux/CageFS shared hosting, plugin `2.4.8` / WHM Plugin `5.3.2.1`, and LiteSpeed's `generateEcCert` + `packageUserSize` log triage rather than treating it as a pre-auth cPanel takeover.
- SafeDep's June 17, 2026 Mastra `easy-day-js` follow-up adds useful pivots to `docs/ops/mastra-easy-day-js-npm-scope-compromise.md`: provenance dropped on attacker-published packages, `sergey2016` / `ehindero2016` email-pattern overlap, 01:12-02:36 UTC republish burst, Hostwinds `23.254.164.0/24`, User-Agent-gated stage-two retrieval, wolfSSL test cert, SHA-256 `221c45a790dec2a296af57969e1165a16f8f49733aeab64c0bbd768d9943badf`, and BlueNoroff / Sapphire Sleet overlap kept caveated as unconfirmed attribution.
- Microsoft Security Blog's June 17, 2026 Mastra follow-up adds useful pivots to `docs/ops/mastra-easy-day-js-npm-scope-compromise.md`: exact observed publish times, 4,572-byte `setup.cjs`, XOR `0x80` `.pkg_logs`, legacy IE8 User-Agent custom ICAP-style exfiltration, Windows reflective .NET loading / `cmd.exe` injection, file/package hashes for `setup.cjs` and `easy-day-js` tarballs, and Microsoft's explicit Sapphire Sleet attribution (keep scoped to Microsoft unless independently corroborated).
- Unit 42's June 16, 2026 Vertex AI `Pickle in the Middle` research is best handled as a reusable cloud/AI pattern at `docs/patterns/vertex-ai-staging-bucket-squatting.md`: deterministic default staging buckets plus missing ownership checks in `google-cloud-aiplatform` `1.139.0` / `1.140.0` enabled bucket squatting, race-replacement of `joblib` / pickle model artifacts, and serving-infrastructure RCE; keep guidance focused on upgrading to `1.148.0+`, explicit organization-owned `staging_bucket` values, bucket-ownership hunts, and model-artifact provenance.
- Socket's June 16, 2026 `shai_hulululud` analysis is reusable AI-scanner anti-analysis pattern coverage at `docs/patterns/ai-scanner-anti-analysis.md`: keep it distinct from confirmed credential-theft packages, and focus on untrusted source comments, safety-triggering text, context flooding, payload-after-noise layouts, staged adversarial content after deobfuscation, executable-code prioritization, and fail-closed scanner behavior.
- CISA added Splunk Enterprise CVE-2026-20253 to KEV on June 18, 2026; keep that update on `docs/ops/splunk-enterprise-cve-2026-20253-preauth-file-write-rce.md`, emphasizing confirmed exploitation, the 2026-07-09 KEV remediation date, immediate isolation/upgrade for internet-facing Splunk Web, and the existing watchTowr `/splunkd/__raw/v1/postgres/recovery/backup` / `restore` pivots.
- SafeDep's June 18, 2026 procwire / routecraft report is durable npm supply-chain coverage at `docs/ops/procwire-routecraft-npm-windows-dropper.md`: keep the lesson focused on Windows-only `preinstall` execution, split-package XOR C2 reconstruction through `endpointmap` / `bytecraft`, `files.catbox.moe` staging, `Microsoft-Delivery-Optimization/10.0` User-Agent gating, updater-like temp executables, Mark-of-the-Web stripping, and treating Windows npm installs as host compromise.
- Hunt.io's June 17 GHOST STADIUM FIFA World Cup phishing report is durable brand-impersonation infrastructure coverage at `docs/ops/ghost-stadium-fifa-world-cup-ticket-phishing.md`: keep the lesson focused on structural kit pivots (`/fifa/` re-hosted React build, Layui, `/fifa/common_main.js`, `fifaindexopen`, same-origin `/as/authorize` / `/register`), Cloudflare-fronted lookalike domains, suspected `AS25820` / `16clouds` origins, and caveated Chinese-speaking operator assessment rather than state attribution.
- Hunt.io's June 18 Ababil of Minab exposed-staging follow-up belongs on `docs/ops/ababil-of-minab-mois-recovery-destruction.md`: keep the public value on operator workflow and infrastructure (`5.255.127[.]55:8020`, Python SimpleHTTP / Flask receiver, `.bash_history`, FileFiend / archive-retrieval corroboration, exposed LA Metro database-backup material) and do not reproduce victim PII, credentials, or file contents from open directories.
- Microsoft Security Blog's June 18, 2026 AutoJack post is reusable AI-agent localhost trust-boundary coverage at `docs/patterns/agent-localhost-control-plane-rce.md`: keep the AutoGen Studio issue caveated as development-branch MCP WebSocket code hardened in commit `b047730` before PyPI release, and focus the durable lesson on authenticating loopback control planes, not trusting Origin/localhost for browsing agents, allowlisting MCP stdio launchers, and isolating agent browsing from developer credentials.
- JFrog Security Research's June 18, 2026 npm v12 analysis is reusable package-manager hardening coverage at `docs/patterns/npm-install-explicit-trust-controls.md`: keep the lesson focused on explicit approvals for lifecycle scripts, Git dependencies, and remote URL dependencies (`allowScripts`, `--allow-git`, `--allow-remote`), with attacker adaptation expected through already-approved script packages and import-time/runtime execution.
- StepSecurity's June 18, 2026 developer-machine package-config guidance belongs on `docs/patterns/npm-install-explicit-trust-controls.md`: internal registry, secure-registry, cooldown, and auth controls only protect hosts configured to use them, so monitor npm/Python package-manager config drift, public-index fallback, endpoint cooldown enforcement, and stale registry tokens across developer laptops and CI runners.
- CISA's June 18, 2026 FortiBleed alert is credential-exposure / edge-access coverage at `docs/ops/fortibleed-fortinet-credential-exposure.md`: keep public value on session termination, VPN/admin credential rotation, PBKDF2 / legacy-hash review, phishing-resistant MFA, public management-interface reduction, and downstream log review; do not reproduce leaked credential lists or victim device data, and keep SOCRadar / 86,644-device figures attributed to secondary reporting.
- Unit 42's June 19, 2026 FortiBleed threat brief broadens the page to cross-service credential spraying: include observed MSSQL attempts, reported Sophos targeting, the password-spray / configuration-theft / offline-cracking feedback loop, and successful-logins-after-failure-bursts hunting; keep Exploit[.]in initial-access-broker claims explicitly unvalidated.
- Klue / Huntress / Salesforce June 2026 reporting is durable SaaS OAuth supply-chain coverage at `docs/ops/klue-salesforce-oauth-token-abuse.md`: keep the lesson focused on compromised legacy vendor-integration credentials, stolen delegated OAuth tokens, connected customer Salesforce/CRM data access, Salesforce disabling the Klue Battlecards app connection, and stale third-party integration lifecycle controls; keep `Icarus` actor attribution caveated until stronger primary reporting appears.
- StepSecurity / JetBrains June 2026 malicious JetBrains AI plugin coverage lives at `docs/ops/jetbrains-ai-plugin-api-key-theft.md`: keep the lesson focused on IDE marketplace trust, raw provider API-key entry, plaintext HTTP exfiltration to `39.107.60[.]51`, affected plugin IDs, JetBrains removal / remote-disable response, and provider key revocation; do not overstate attribution beyond publisher-account and infrastructure facts.
- ESET's June 18, 2026 Gentlemen report is durable ransomware-tooling backfill on `docs/tools/the-gentlemen-ransomware.md`: keep the lesson focused on operator-maintained EDR-killer support for affiliates, GentleKiller variants abusing vulnerable or malicious drivers, integrated HexKiller / ThrottleBlood / HavocKiller tooling, rapid BYOVD PoC adoption, and driver-blocklist / HVCI / tamper-protection defense pivots.
- Dutch Police / Operation Endgame's June 18, 2026 SocGholish action is durable law-enforcement disruption coverage at `docs/ops/operation-endgame-socgholish-disruption.md`: keep the value on legitimate WordPress compromise, traffic-direction / fake-update delivery, 106 servers/domains taken down, 14,971 remediated WordPress sites, Shadowserver notification, WordPress credential / MFA / backdoor-plugin cleanup, and source-attributed naming (`SocGholish` / `FakeUpdates` / Proofpoint `TA569` / law-enforcement Evil Corp linkage).
- Wordfence / The Hacker News June 2026 Gravity SMTP coverage lives at `docs/ops/gravity-smtp-cve-2026-4020-exploitation.md`: treat CVE-2026-4020 as an unauthenticated WordPress email-plugin credential-exposure incident path, with priority on upgrading past 2.1.4, checking `/wp-json/gravitysmtp/v1/tests/mock-data?page=gravitysmtp-settings`, rotating exposed email-provider API/OAuth credentials, and hunting for follow-on WordPress compromise.
- Hunt.io provider/ASN concentration reports can be durable pattern material rather than one-off ops pages; Middle East 1,350+ C2 / 98-provider Host Radar coverage lives at `docs/patterns/malicious-infrastructure-provider-concentration.md`, with provider complicity caveated and defender value focused on ASN/provider enrichment, recurrence tracking, abuse-response context, and egress/hunting pivots.
- SafeDep AI-brand npm scope-squatting reports can justify concise ops coverage when they expose developer credential harvesting; `@withgoogle/stitch-sdk` coverage lives at `docs/ops/withgoogle-stitch-sdk-scope-squat.md`, with focus on product-domain vs npm-scope confusion, install-time script approval, Claude Code / GitHub / npm / Docker credential rotation, and vendor-scope governance.
- CrowdStrike's June 18, 2026 ClickOnce research is reusable Windows execution/persistence pattern coverage at `docs/patterns/clickonce-com-hijacking-abuse.md`: keep the lesson focused on `.application` / `.appref-ms` deployment paths, per-user installs, update-backed persistence, `rundll32.exe` / `dfshim.dll` / `dfsvc.exe` execution chains, and HKCU COM registration for CLSID `{20FD4E26-8E0F-4F73-A0E0-F27B8C57BE6F}` rather than treating it as a named intrusion.
- Arctic Wolf's June 17, 2026 FortiBleed bulletin is useful impact and credential-storage backfill for `docs/ops/fortibleed-fortinet-credential-exposure.md`: keep SOCRadar / Beaumont-Hudson Rock figures source-attributed (30,791+ confirmed working credentials, about 75,000 estimated devices, 194 countries) and preserve the lesson that FortiOS PBKDF2-supporting upgrades may leave legacy administrator hashes until login or password reset.
- ESET FishMonger / SprySOCKS Windows coverage lives at `docs/actors/fishmonger.md` and `docs/tools/sprysocks.md`: keep I-SOON / Winnti / Earth Lusca / TAG-22 / Aquatic Panda / Red Dev 10 aliasing source-attributed; emphasize WIN_DRV kernel-driver stealth, transient `fsdiskbit.sys` / `msidiskserver` loading, Fonts-directory `.dat` containers, TCP traffic diversion, and caveated UEFI bootkit telemetry.
- Google Cloud / GTIG's UNC6508 coverage lives at `docs/actors/unc6508.md`: keep PRC-nexus attribution tied to GTIG's high-confidence assessment; focus on REDCap legacy-version exposure, INFINITERED credential harvesting / upgrade persistence, `help.php`, `REDCAP-TOKEN`, `xc32038474a`, content-compliance rule BCC exfiltration, OBF network use, and administrator credential replay across research environments.
- SafeDep's June 21, 2026 MYRA / `apintergrationpost` report is durable tool coverage at `docs/tools/myra-rat.md`: keep focus on public npm distribution of a Linux RAT, root-required lifecycle scripts, native `LD_PRELOAD` / `memfd` components, `systemd-userdbd` masquerading, cron/profile.d persistence, and screen-streaming pivots rather than treating README red-team framing as authorization.
- QiAnXin XLab AryStinger coverage lives at `docs/ops/arystinger-legacy-router-recon-proxy-network.md`: keep it framed as legacy-router/NAS reconnaissance and relay infrastructure, not a confirmed actor profile; preserve the caveat that XLab's 4,300+ count covers RTL819X routers only and not QNAP NAS infections.
- JFrog's PixelSmash / FFmpeg CVE-2026-8461 coverage lives at `docs/ops/ffmpeg-pixelsmash-cve-2026-8461-media-file-rce.md`: treat it as a media-processing dependency blast-radius issue, preserve JFrog's ASLR-disabled RCE caveat, and keep mitigations focused on upgrading/bundled-FFmpeg inventory, MagicYUV decoder checks, and preview/thumbnail/transcoding workflow containment.
- JFrog's June 22, 2026 `postcss-minify-selector-parser` / `aes-decode-runner-pro` coverage lives at `docs/ops/postcss-minify-selector-parser-npm-rat.md`: treat it as ecosystem-plausible npm impersonation with import-time JavaScript execution, AES-decoded PowerShell staging, `nvidiadriver[.]net` Windows bundle delivery, and a bundled Python 3.10 / Nuitka RAT that steals Chrome credentials and persists via HKCU Run.
- Microsoft Incident Response's June 22, 2026 Cyberattack Series No. 9 coverage lives at `docs/ops/storm-2603-parallel-sharepoint-ransomware-intrusion.md`: treat it as operation / IR-pattern coverage for simultaneous actors in one environment, not a full Storm-2603 actor profile yet; keep the second actor unnamed, preserve Microsoft caveats around LFI probing versus confirmed exploitation, and emphasize telemetry correlation before collapsing artifacts into one attribution story.
- Microsoft Security Blog's June 22, 2026 AI memory guidance is durable agent-security pattern coverage at `docs/patterns/ai-agent-memory-poisoning.md`: treat persistent memory as behavior-shaping control state, and keep defender guidance focused on provenance, explicit memory-write intent, retrieval-time risk checks, lifecycle audit logs, `MemoryUpdated` telemetry, and poisoned-memory cleanup rather than treating it as a named intrusion.
- Unit 42's June 22, 2026 global namespace bucket-hijacking research is durable cloud-storage pattern coverage at `docs/patterns/cloud-bucket-namespace-hijacking.md`: treat deleted/recreated bucket names as a possible data-stream redirection path for Cloud Logging, Pub/Sub, Storage Transfer Service, S3 replication, Data Firehose, and Azure Monitor diagnostics; keep the no-known-in-the-wild caveat, and focus guidance on storage-delete least privilege, trusted-boundary data perimeters, bucket/storage-account deletion alerts, and destination-owner verification.
- Check Point Research's June 17, 2026 fake-reputation clipboard-hijacker report is durable social-proof-abuse malware coverage at `docs/ops/fake-reputation-crypto-clipboard-hijacker.md`: keep the lesson focused on manipulated GitHub / SourceForge / YouTube / VirusTotal / news-site trust signals, Windows `%APPDATA%\silke\silke.exe` persistence, macOS `unlocker.command` quarantine removal plus LaunchAgent persistence, and the need to treat community reputation as weak context rather than a clean verdict.
- Trend Micro's June 23, 2026 Langflow CVE-2026-33017 cryptominer report is durable exposed-AI-application exploitation coverage at `docs/ops/langflow-cve-2026-33017-cryptominer-ssh-worm.md`: keep the lesson focused on unauthenticated Langflow `build_public_tmp` code execution, `isp.sh` / `lambsys`, XMRig `procq`, cron plus `init_rmount` watchdogs, `chattr` persistence, and SSH-key / `known_hosts` lateral-movement scoping rather than treating it as only commodity mining noise.
- Snyk's June 23, 2026 developer-environment measurements and JFrog's June 23 agent-plugin repository guidance are useful backfill for `docs/patterns/agent-skill-marketplace-poisoning.md`: treat MCP servers, skills, hooks, slash commands, subagents, and plugin manifests as executable supply-chain inputs on developer endpoints; prefer immutable reviewed internal artifacts over mutable public Git branches; and inventory/monitor local agent runtimes before code reaches CI.
- SafeDep's June 23, 2026 wshu.net campaign coverage lives at `docs/ops/wshu-net-npm-credential-stealer-campaign.md`: keep the lesson focused on historical-version review, scrubbed latest tags, disposable publisher-email pivots, runtime triggers that survive `--ignore-scripts`, Rust infostealer credential scope, user-level systemd persistence, and developer/CI host compromise response rather than only package removal.
- AIR Security's June 22, 2026 skill experiment is useful backfill for `docs/patterns/agent-skill-marketplace-poisoning.md`: keep AIR's 26,000-agent / full-control scale claims caveated as vendor-reported, but preserve the durable lesson that clean submitted skills can pass scanners while delegating real setup instructions to mutable external documentation on product-adjacent domains that changes after review.
- CISA's June 23, 2026 KEV additions for Ubiquiti UniFi OS CVE-2026-34908 / CVE-2026-34909 / CVE-2026-34910 and Lantronix EDS5000 CVE-2025-67038 are concise edge / ICS management-plane ops coverage at `docs/ops/ubiquiti-unifi-os-cve-2026-34908-34909-34910-exploitation.md` and `docs/ops/lantronix-eds5000-cve-2025-67038-exploitation.md`; keep Ubiquiti Mirai/botnet language tied to PwnDefend honeypot reporting, avoid reproducing full exploit paths, and focus both pages on exposure control, patching, failed-auth/traversal log review, and root-command-execution triage.
- Unit 42's June 23, 2026 OpenClaw / ClawHub follow-up is best handled as an update to `docs/patterns/agent-skill-marketplace-poisoning.md`: preserve the five-unblocked-skills framing, ClawHavoc-style macOS infostealer delivery via paste-site prerequisite blocks, `omnicogg` 22 MB README padding scanner evasion, `money-radar` runtime affiliate-link injection from remote JSON, and `letssendit` agentic Solana front-running; avoid treating agent marketplace scanner pass/fail as a trust verdict.
- Kaspersky's June 22, 2026 WhatsApp VBScript / ManageEngine Endpoint Central campaign is durable messaging-app malware coverage at `docs/ops/whatsapp-vbscript-manageengine-rmm-campaign.md`: keep the lesson focused on compromised WhatsApp-account contact trust, finance-themed `.vbs` / `.vbe` lures, WhatsApp-spawned `WScript.exe`, hidden `C:\Users\Public\Documents` staging, UAC-prompt registry loops, silent ManageEngine RMM enrollment, and Kaspersky's low-confidence Chinese-speaking-operator / ValleyRAT-Gh0st-overlap caveats.
- Hunt.io exposed-botnet writeups can justify ops coverage when they reveal durable infection, C2, and monetization mechanics; xlabs_v1 coverage lives at `docs/ops/xlabs-v1-ddos-for-hire-iot-botnet.md`. Keep the lesson focused on ADB-on-TCP/5555 IoT infection, Speedtest bandwidth tiering, game-server flood variants, `/bin/bash` process masquerading, TCP/26721 fallback re-entry, and Hunt.io-attributed `Tadashi` handle caveats rather than asserting a real-world identity or nationality.
- Seqrite targeted-sector malware reports can justify concise ops pages when they include concrete chains and caveated attribution; Thailand healthcare RAR / Python stealer coverage lives at `docs/ops/thailand-healthcare-rar-python-stealer.md`. Keep it scoped to healthcare-themed RAR lures, Rouki-obfuscated batch loaders, GitHub-hosted payloads, `WindowSecuryt.bat` Startup-folder persistence, bundled `C:\Users\Public\Desktops` Python execution, `sim.py` information theft, and Telegram exfiltration attempts; preserve Seqrite's no-attribution caveat.
- Cisco Unified CM CVE-2026-20230 coverage lives at `docs/ops/cisco-unified-cm-cve-2026-20230-file-write-exploitation.md`; keep the lesson focused on WebDialer-enabled Unified CM / Unified CM SME exposure, unauthenticated SSRF to file write and possible root escalation, Cisco `14SU6` / release-15 COP or `15SU5` fixed-version guidance, and the caveat that active exploitation is currently Defused Cyber / The Hacker News telemetry rather than Cisco-confirmed exploitation.
- Microsoft Security Blog / DCU disruption posts can justify concise commodity-malware ops pages when they add concrete intrusion and defender detail; StealC / Amadey coverage lives at `docs/ops/stealc-amadey-infrastructure-disruption.md`. Keep the value focused on infostealer-to-enterprise credential risk, StealC browser / wallet / cookie / App-Bound Encryption bypass behavior, Amadey scheduled-task persistence and follow-on capabilities, and the June 24, 2026 Microsoft / Europol / industry action against 200+ C2 domains and IPs.
- JFrog Security Research's June 24, 2026 `html-to-gutenberg` / `fetch-page-assets` report is durable npm/editor-auto-execution coverage at `docs/ops/html-to-gutenberg-fetch-page-assets-vscode-blockchain-stealer.md`; keep the lesson focused on package-directory VS Code `runOn: "folderOpen"` tasks, fake-font JavaScript, Tron/Aptos/BSC transaction dead drops, `socket.io` backdoor control, Python credential/wallet theft, and the fact that lifecycle-script hardening does not cover trusted-workspace task execution.
- StepSecurity's June 24, 2026 `codfish/semantic-release-action` report is durable GitHub Actions tag-retargeting coverage at `docs/ops/codfish-semantic-release-action-tag-compromise.md`; keep the lesson focused on malicious-window workflow-run triage, mutable action tags, Docker-to-composite action swaps, `if: always()` Bun execution, OIDC / PAT theft, and release-runner credential rotation rather than treating later-clean tag resolution as proof of no exposure.
- Novee Security's June 2026 Cordyceps CI/CD workflow-composition research is durable backfill for `docs/patterns/deployment-poisoning-github-actions.md`: treat pull-request comments, branch names, labels, artifacts, deployment fields, and generated workflow state as untrusted even when a downstream workflow runs on the default branch with secrets, write tokens, approvals, or cloud credentials.
